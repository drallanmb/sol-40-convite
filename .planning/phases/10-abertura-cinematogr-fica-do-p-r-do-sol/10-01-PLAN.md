---
phase: 10-abertura-cinematogr-fica-do-p-r-do-sol
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/cinematicIntro.ts
  - src/lib/cinematicIntro.test.ts
  - src/routes/Home.tsx
  - src/components/invite/Hero.tsx
  - src/index.css
  - tests/cinematic-intro.spec.ts
autonomous: true
requirements:
  - INTRO-01
  - INTRO-02
must_haves:
  truths:
    - "Na primeira renderização elegível, somente o céu final está visível: hero completo, mar, copy e ações não piscam antes da abertura."
    - "O único disco solar visual começa totalmente acima da viewport, mantém tamanho/halo constantes e termina sobre seu wrapper responsivo com erro máximo de 1 CSS px."
    - "D-04: a descida cinematográfica dura ~2s, começa devagar, acelera no meio e desacelera no pouso, usando somente transform por 2000ms com cubic-bezier(.65, 0, .35, 1); o reveal usa 260ms e não aplica opacidade ao sol."
    - "Entradas com reduced motion ou hash inicial não elegível começam diretamente em complete, sem WAAPI nem fade."
  artifacts:
    - path: "src/lib/cinematicIntro.ts"
      provides: "Tipos, constantes e políticas puras da abertura"
      exports:
        - IntroPhase
        - CINEMATIC_INTRO_DURATION_MS
        - CINEMATIC_INTRO_REVEAL_MS
        - CINEMATIC_INTRO_EASING
        - CINEMATIC_INTRO_SCROLL_THRESHOLD_PX
        - isEligibleHeroHash
        - resolveInitialIntroPhase
        - hasIntentionalIntroScroll
        - homeSectionIdFromHash
    - path: "src/lib/cinematicIntro.test.ts"
      provides: "Wave 0 unitária para allowlist, estado inicial, fragmentos e limiar de scroll"
    - path: "src/components/invite/Hero.tsx"
      provides: "Wrapper-alvo medível e único disco solar animado"
    - path: "tests/cinematic-intro.spec.ts"
      provides: "Wave 0 browser para primeiro frame, geometria e timing"
  key_links:
    - from: "src/routes/Home.tsx"
      to: "src/lib/cinematicIntro.ts"
      via: "estado inicial síncrono a partir do hash capturado e de useReducedMotion"
      pattern: "resolveInitialIntroPhase"
    - from: "src/routes/Home.tsx"
      to: "src/components/invite/Hero.tsx"
      via: "introPhase/runGeneration/onDescentComplete"
      pattern: "introPhase"
    - from: "src/components/invite/Hero.tsx"
      to: "src/index.css"
      via: "data-intro-phase e classes de reveal; o sol fica fora do fade"
      pattern: "data-intro-phase"
    - from: "tests/cinematic-intro.spec.ts"
      to: "src/components/invite/Hero.tsx"
      via: "data-testid hero-sun-target/hero-sun-visual e controle WAAPI antes do app"
      pattern: "hero-sun-(target|visual)"
  flagged_assumptions:
    - requirement_id: INTRO-01
      category: unclassified
      status: unresolved
      flagged: true
      statement: "O spec-less edge probe retornou 'unclassified — review manually' para INTRO-01; não existe verificação ou resolução de edge fornecida, portanto o plano usa D-01–D-07/D-19/D-22/D-23 e geometria browser como contrato explícito sem fingir que o probe foi resolvido."
    - requirement_id: INTRO-02
      category: unclassified
      status: unresolved
      flagged: true
      statement: "O spec-less edge probe retornou 'unclassified — review manually' para INTRO-02; não existe verificação ou resolução de edge fornecida, portanto o plano mantém a lacuna sinalizada e cobre os comportamentos conhecidos por D-13–D-18/D-24."
  prohibitions:
    - id: P-10-01
      requirement_id: INTRO-01
      category: transparency
      statement: "INTRO-01 MUST NOT turn the cinematic opening into a loading gate, progress experience, waiting message, spinner, or substitute hero whose handoff can mislead the guest about whether the invitation is ready."
      status: unverified
      flagged: true
    - id: P-10-02
      requirement_id: INTRO-02
      category: safety
      statement: "INTRO-02 MUST NOT make invisible header or hero controls focusable/clickable while hiding them, trap keyboard focus, cover the skip link, or intercept pointer input intended for the invitation."
      status: unverified
      flagged: true
    - id: P-10-03
      requirement_id: INTRO-02
      category: safety
      statement: "INTRO-02 MUST NOT play the broad solar descent or the reveal fade for a guest whose system requests reduced motion, and MUST NOT satisfy that preference by hiding final content or artwork."
      status: unverified
      flagged: true
---

<objective>
Construir o núcleo visual e mensurável da abertura: política de elegibilidade síncrona, um único sol canônico e a transição contínua até a geometria real do hero.

Purpose: satisfazer INTRO-01 sem clone, coordenadas responsivas duplicadas, flash do hero ou biblioteca de animação, ao mesmo tempo preparando o fallback imediato de INTRO-02.
Output: helper puro testado, estado inicial em Home, wrapper/disco canônico no Hero, estilos de fase e primeira fatia Playwright.
</objective>

<execution_context>
@/Users/allanmesquitabrito/.claude/gsd-core/workflows/execute-plan.md
@/Users/allanmesquitabrito/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-abertura-cinematogr-fica-do-p-r-do-sol/10-CONTEXT.md
@.planning/phases/10-abertura-cinematogr-fica-do-p-r-do-sol/10-RESEARCH.md
@.planning/phases/10-abertura-cinematogr-fica-do-p-r-do-sol/10-PATTERNS.md
@.planning/phases/10-abertura-cinematogr-fica-do-p-r-do-sol/10-VALIDATION.md
@DESIGN.md
@src/routes/Home.tsx
@src/components/invite/Hero.tsx
@src/components/invite/SeaWaves.tsx
@src/hooks/useReducedMotion.ts
@src/index.css
@playwright.config.ts

<interfaces>
Criar em `src/lib/cinematicIntro.ts`:
- `export type IntroPhase = 'descending' | 'revealing' | 'complete'`
- `export const CINEMATIC_INTRO_DURATION_MS = 2000`
- `export const CINEMATIC_INTRO_REVEAL_MS = 260`
- `export const CINEMATIC_INTRO_EASING = 'cubic-bezier(.65, 0, .35, 1)'`
- `export const CINEMATIC_INTRO_SCROLL_THRESHOLD_PX = 4`
- `export function isEligibleHeroHash(hash: string): boolean`
- `export function resolveInitialIntroPhase(hash: string, reducedMotion: boolean): IntroPhase`
- `export function hasIntentionalIntroScroll(startScrollY: number, currentScrollY: number): boolean`
- `export function homeSectionIdFromHash(hash: string): string | null`

Evoluir `Hero` para receber:
- `introPhase: IntroPhase`
- `introRunGeneration: number`
- `onIntroDescentComplete: () => void`

Seletores observáveis:
- `[data-intro-phase]`
- `[data-testid="hero-sun-target"]`
- `[data-testid="hero-sun-visual"]`
- `[data-intro-reveal]`

Helpers de teste em `tests/cinematic-intro.spec.ts`:
- `installCinematicIntroControl(page: Page): Promise<void>`
- `finishLatestCinematicIntro(page: Page): Promise<void>`
- `readSunGeometry(page: Page): Promise<{ target: DOMRectShape; visual: DOMRectShape }>`
- `expectSunGeometryAligned(page: Page): Promise<void>`
- `Window.__cinematicIntroAnimations: Animation[]`
- `Window.__cinematicIntroSunNodes: Element[]`
</interfaces>
</context>

<prohibition_probe_notes>
Stage 1 aplicou a pergunta adversarial separadamente a INTRO-01 e INTRO-02. Stage 2 descartou concerns rotineiros de correção (transform arithmetic, null refs, resize precision) e manteve exatamente três constraints de produto/safety em `must_haves.prohibitions`: não virar loading enganoso, não tornar controles invisíveis interativos/prender foco e não ignorar reduced motion. Canon referral: selector/HTML injection por fragmento é OWASP canon — coberto pelo threat model e pela allowlist/getElementById, não cunhado como proibição bespoke. Todos os três itens mantidos estão descriptor-less, `status: unverified`, `flagged: true`; nenhum descritor mecânico foi inventado.
</prohibition_probe_notes>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fixar a política pura de elegibilidade e navegação</name>
  <files>src/lib/cinematicIntro.ts, src/lib/cinematicIntro.test.ts</files>
  <read_first>.planning/phases/10-abertura-cinematogr-fica-do-p-r-do-sol/10-CONTEXT.md, .planning/phases/10-abertura-cinematogr-fica-do-p-r-do-sol/10-RESEARCH.md § Pattern 1/Pattern 6, .planning/phases/10-abertura-cinematogr-fica-do-p-r-do-sol/10-VALIDATION.md, src/content/event.ts</read_first>
  <behavior>
    - `isEligibleHeroHash('')` e `isEligibleHeroHash('#inicio')` retornam true; `#programacao`, `#aracaju`, fragmentos desconhecidos e variantes de caixa retornam false.
    - `resolveInitialIntroPhase` retorna descending somente para hash elegível com movimento permitido; qualquer reduced motion ou hash não elegível retorna complete.
    - `hasIntentionalIntroScroll` usa diferença absoluta e só retorna true em 4 CSS px ou mais.
    - `homeSectionIdFromHash` devolve apenas IDs exatos existentes em `SECTION_IDS`; nunca interpreta HTML nem constrói seletor CSS.
  </behavior>
  <action>Crie os contratos e constantes exatos listados em `<interfaces>`; importe `SECTION_IDS` para formar a allowlist de fragmentos conhecidos sem repetir IDs. A combinação de elegibilidade exata, fragmentos conhecidos e limiar absoluto torna a política suficientemente não trivial para usar a extração opcional autorizada por 10-PATTERNS. Escreva primeiro `src/lib/cinematicIntro.test.ts`, execute RED, implemente e execute GREEN. Mantenha esta tarefa independente da árvore React: a integração de `Home` com as props novas de `Hero` acontece atomicamente na Tarefa 2, quando ambos os lados do contrato existem. Não use storage/cookie/sessão (D-08) e não adicione dependência.</action>
  <acceptance_criteria>`src/lib/cinematicIntro.test.ts` prova os quatro casos acima; 2000/260/4 existem somente nas constantes exportadas; nenhuma ocorrência de localStorage, sessionStorage ou cookie é adicionada aos arquivos da tarefa; o build permanece válido antes de qualquer mudança em `Home` ou `Hero`.</acceptance_criteria>
  <verify>
    <automated>npx vitest run src/lib/cinematicIntro.test.ts &amp;&amp; npm run build</automated>
  </verify>
  <done>A política de entrada é determinística, allowlisted e compilável como unidade independente, pronta para ser consumida atomicamente pela árvore React.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Animar o único sol até o alvo renderizado e substituir o stagger antigo</name>
  <files>src/routes/Home.tsx, src/components/invite/Hero.tsx, src/index.css, tests/cinematic-intro.spec.ts</files>
  <read_first>.planning/phases/10-abertura-cinematogr-fica-do-p-r-do-sol/10-CONTEXT.md D-01–D-12/D-17/D-19–D-24, .planning/phases/10-abertura-cinematogr-fica-do-p-r-do-sol/10-RESEARCH.md § Pattern 1/Pattern 2/Pattern 3/Determinism Strategy, .planning/phases/10-abertura-cinematogr-fica-do-p-r-do-sol/10-PATTERNS.md § Home/§ Hero/§ index.css/§ tests, src/routes/Home.tsx, src/components/invite/Hero.tsx, src/components/invite/SeaWaves.tsx, src/hooks/useReducedMotion.ts, src/index.css, src/main.tsx, tests/release.spec.ts, playwright.config.ts</read_first>
  <behavior>
    - `Home` decide `introPhase` no initializer síncrono: montagem elegível inicia descending; reduced motion ou hash não elegível inicia complete sem flash.
    - Uma entrada elegível pausa com céu estável e grupos de reveal ocultos desde o primeiro commit; o disco visível está inteiramente acima da viewport antes de avançar.
    - O mesmo nó `[data-testid="hero-sun-visual"]` usa uma única animação WAAPI de transform, duration 2000 e easing `cubic-bezier(.65, 0, .35, 1)`.
    - Após finish, centros, largura e altura do visual e do target diferem no máximo 1 CSS px, e o nó visual é o mesmo antes/depois.
    - Em revealing, mar/copy/actions/meta usam opacity por 260ms, o sol não usa opacity/transição e as ondas já possuem suas animações 22s/30s/38s.
  </behavior>
  <action>Escreva primeiro em `tests/cinematic-intro.spec.ts` os casos focados `first frame`, `geometry` e `timing`, instalando `page.addInitScript()` antes de `goto` para envolver somente `Element.prototype.animate` do elemento `data-testid="hero-sun-visual"`, pausar e expor o handle. Integre os dois lados do contrato React nesta mesma tarefa: em `Home`, leia `useLocation()` e `useReducedMotion()`, capture a elegibilidade da montagem inicial e inicialize `introPhase` dentro do initializer de `useState`, nunca em `useEffect`, para que D-01/D-08/D-12/D-17/D-21 valham já no primeiro render. Adicione `introRunGeneration`, callback idempotente que muda descending→revealing, timer de exatamente 260ms que muda revealing→complete e cleanup do timer. Se reduced motion se tornar ativo durante uma execução, mude diretamente para complete sem fade. Defina e consuma simultaneamente em `HeroProps` `introPhase`, `introRunGeneration` e `onIntroDescentComplete`, passando os três valores de `Home` para `Hero`; não deixe um commit intermediário com props inexistentes ou sem consumidor. Não observe `location.key` para replay nesta tarefa (D-09/D-10).

Em `Hero`, preserve exatamente `left-1/2`, `top-[62%]`, `sm:top-[59%]`, `w-[clamp(260px,28vw,480px)]`, `--color-sun` e `0 0 100px var(--color-sun-halo)`, mas mova posicionamento/centragem para o wrapper target e fundo/halo para um filho visual `h-full w-full rounded-full`. Esse filho é o único sol renderizado (D-03/D-06/D-22/D-24). Durante descending, mantenha o filho oculto até a WAAPI estar aplicada; no primeiro RAF criado por `useLayoutEffect`, leia `target.getBoundingClientRect()`, anime o filho de `translate3d(0, -(rect.bottom + rect.height)px, 0)` até `translate3d(0, 0, 0)` por 2000ms/easing exato/fill both e então o torne visível. Isso mantém disco e halo totalmente fora da tela antes de entrar (D-02), mede resize/orientação ocorrido antes do RAF (D-23) e evita flash. Se target/visual estiver ausente, `Element.animate` não estiver disponível ou a geometria não for finita/positiva, chame `onIntroDescentComplete` uma única vez para que conteúdo e navegação nunca permaneçam ocultos. Faça cleanup simétrico compatível com StrictMode: cancele RAF, nulifique `onfinish`, cancele Animation e impeça callback após dispose. Ao terminar, remova o efeito WAAPI apenas quando a base visual já coincide com o endpoint e chame o callback uma vez.

Remova do hero as classes `.hero-sky-enter`, `.hero-sun-enter`, `.hero-enter--*` e os keyframes hero antigos sem tocar nas animações de programa/gifts/admin. Agrupe somente `SeaWaves`, copy/actions e meta com `[data-intro-reveal]`; céu e sol ficam fora. Em CSS, descending usa `visibility:hidden; opacity:0` apenas nos grupos, revealing os torna visible/opacity 1 com 260ms, complete fica estável, e o branch reduced motion remove a transição. Não crie spinner, progresso, copy de espera, fundo neutro, clone/portal ou animação de cor/tamanho/halo (D-01/D-03/D-05/D-07/D-19/D-20/D-21).</action>
  <acceptance_criteria>Os três testes Playwright focados passam; `Home` nunca inicia complete para uma montagem elegível sem reduced motion, nunca inicia descending para `/#programacao` e compila contra as três props concretas de `Hero`; `hero-sun-visual` possui exatamente uma animação de entrada com duration/easing esperados e keyframes contendo transform sem opacity/filter/width/height; o target e visual convergem em ≤1 CSS px; nenhum seletor/keyframe antigo do hero permanece em uso; build TypeScript passa e as ondas continuam montadas durante descending.</acceptance_criteria>
  <verify>
    <automated>npm run build &amp;&amp; npx playwright test tests/cinematic-intro.spec.ts --project=emulated-chromium-desktop -g "first frame|geometry|timing"</automated>
  </verify>
  <done>O sol canônico desce do topo até seu próprio alvo responsivo e inicia um único reveal sem swap, blink ou segunda coreografia.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| URL fragment → intro policy | `location.hash` é texto externo usado apenas por comparações exatas e lookup de ID conhecido. |
| Browser layout/WAAPI → React phase | Geometria e callbacks assíncronos atravessam lifecycle React/StrictMode e não podem sobreviver ao dono. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-10-01-T | Tampering | `src/lib/cinematicIntro.ts` hash policy | medium | mitigate | Allowlist exata para elegibilidade e IDs derivados somente de `SECTION_IDS`; nenhum HTML/selector interpolado. |
| T-10-01-D | Denial of Service | `Hero` RAF/Animation lifecycle | medium | mitigate | Um RAF e uma Animation por run, completion idempotente e cleanup simétrico sob StrictMode/unmount. |
| T-10-01-S | Spoofing | primeiro frame/reveal | low | accept | A cena reutiliza céu/sol canônicos e não exibe loading, progresso ou mensagem enganosa; browser tests observam o estado inicial. |
</threat_model>

<verification>
Rodar `npx vitest run src/lib/cinematicIntro.test.ts`, depois `npm run build && npx playwright test tests/cinematic-intro.spec.ts --project=emulated-chromium-desktop -g "first frame|geometry|timing"`.
</verification>

<success_criteria>
- INTRO-01 possui um único nó solar cuja geometria final é a do CSS existente e cujo erro de pouso é ≤1 CSS px.
- D-01–D-07 e D-19–D-23 estão codificadas no primeiro frame, na WAAPI e nos grupos de reveal.
- INTRO-02 já evita toda animação quando reduced motion está ativo.
- Nenhuma dependência, API externa, storage ou segunda arte do hero foi introduzida.
</success_criteria>

## Artifacts this phase produces

- `src/lib/cinematicIntro.ts`
- `src/lib/cinematicIntro.test.ts`
- `tests/cinematic-intro.spec.ts`
- `IntroPhase`
- `CINEMATIC_INTRO_DURATION_MS`
- `CINEMATIC_INTRO_REVEAL_MS`
- `CINEMATIC_INTRO_EASING`
- `CINEMATIC_INTRO_SCROLL_THRESHOLD_PX`
- `isEligibleHeroHash`
- `resolveInitialIntroPhase`
- `hasIntentionalIntroScroll`
- `homeSectionIdFromHash`
- `HeroProps` com `introPhase`, `introRunGeneration` e `onIntroDescentComplete`
- Seletores `data-intro-phase`, `data-testid="hero-sun-target"`, `data-testid="hero-sun-visual"` e `data-intro-reveal`
- Helpers Playwright `installCinematicIntroControl`, `finishLatestCinematicIntro`, `readSunGeometry` e `expectSunGeometryAligned`
- Campos Window de teste `__cinematicIntroAnimations` e `__cinematicIntroSunNodes`

<output>Create `10-01-SUMMARY.md` in the phase directory when done.</output>
