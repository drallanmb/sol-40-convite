---
phase: 10-abertura-cinematogr-fica-do-p-r-do-sol
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/cinematicIntro.ts
  - src/lib/cinematicIntro.test.ts
  - src/routes/Home.tsx
  - src/components/layout/Shell.tsx
  - src/components/invite/Hero.tsx
  - src/index.css
  - tests/cinematic-intro-visual.spec.ts
  - .planning/phases/10-abertura-cinematogr-fica-do-p-r-do-sol/artifacts/intro-keyframes-desktop.png
  - .planning/phases/10-abertura-cinematogr-fica-do-p-r-do-sol/artifacts/intro-keyframes-mobile.png
autonomous: false
requirements: [INTRO-01, INTRO-02]

must_haves:
  truths:
    - "Em 1280x800 e 320x760, os frames 0%, 40%, 70%, 88% e 100% mostram uma única paisagem contínua com céu, horizonte, mar, profundidade e silhuetas reconhecíveis; nenhum frame inicial volta ao conceito sky-only rejeitado (D-01, D-02, D-03, D-04, D-05)."
    - "O storyboard usa um único sol canônico em arco diagonal, recuo sutil de câmera, halo e reflexo como ponte até o hero final; o frame 100% é o próprio enquadramento real do hero, não uma troca de fundo ou de nó (D-06, D-07, D-08, D-09, D-10, D-19)."
    - "A composição mobile é desenhada separadamente, com arco compacto, horizonte mais baixo, reflexo vertical e palmeiras laterais, e não é um crop do desktop (D-16)."
    - "Durante playback provisório bem-sucedido, os grupos primary e secondary começam invisíveis e inertes; primary torna-se não-inerte no seu próprio início visível, antes de secondary/CTAs, que só ficam focáveis no respectivo início visível. Skip link, topbar e qualquer controle já realmente visível permanecem disponíveis; nunca existe CTA invisível focável (D-12, D-13, D-14, D-15, D-26, D-27)."
    - "A execução para em um checkpoint humano bloqueante; testes e screenshots provam determinismo, mas não podem declarar sozinhos que a direção está cinematográfica."
    - "Enquanto o visual aguarda aprovação, nenhuma camada decorativa captura interação; reduced motion, entrada inelegível por frequência/fragmento e qualquer falha WAAPI/controller convergem imediatamente ao hero final totalmente visível, não-inerte e operável. O seek existe somente como instrumentação externa injetada por Playwright, nunca como modo, bridge ou namespace compilado no app (D-18, D-20, D-21, D-22, D-26, D-27, D-28, D-29)."
  artifacts:
    - path: "src/components/invite/Hero.tsx"
      provides: "Cena editorial contínua, sol canônico e grupos semânticos com visibilidade e inert coordenados"
      contains: "data-intro-copy"
    - path: "tests/cinematic-intro-visual.spec.ts"
      provides: "Instrumentação WAAPI injetada, seek determinístico, prova de ausência da namespace no build normal e geração das duas contact sheets"
      contains: "addInitScript"
    - path: ".planning/phases/10-abertura-cinematogr-fica-do-p-r-do-sol/artifacts/intro-keyframes-desktop.png"
      provides: "Contact sheet 1280x800 em 0/40/70/88/100%"
    - path: ".planning/phases/10-abertura-cinematogr-fica-do-p-r-do-sol/artifacts/intro-keyframes-mobile.png"
      provides: "Contact sheet 320x760 em 0/40/70/88/100%"
  key_links:
    - from: "src/components/invite/Hero.tsx"
      to: "src/index.css"
      via: "atributos semânticos data-intro-layer/data-intro-copy, estado inert por grupo e variáveis por composição"
      pattern: "data-intro-(scene|layer|copy|sun)"
    - from: "src/routes/Home.tsx"
      to: "src/components/invite/Hero.tsx"
      via: "IntroState complete/fail-open força os dois grupos finais visíveis e não-inertes; playing permite apenas inert coordenado ao onset de cada grupo"
      pattern: "(playing|complete|inert)"
    - from: "tests/cinematic-intro-visual.spec.ts"
      to: "src/components/invite/Hero.tsx"
      via: "page.addInitScript envolve WAAPI antes do goto, captura somente tracks marcadas e os casos normais validam inert/onset/fail-open sem CTA invisível focável"
      pattern: "(addInitScript|data-intro-track|data-intro-copy|inert)"
    - from: "10-01 checkpoint"
      to: "10-02-PLAN.md"
      via: "depends_on bloqueante; timeline e DESIGN.md só avançam após aprovação explícita"
      pattern: "depends_on: \\[10-01\\]"
  prohibitions:
    - id: P-10-VISUAL
      requirement_id: INTRO-01
      provenance: flagged-unverified
      category: values
      status: unresolved
      statement: "A abertura MUST NOT virar novamente uma demonstração técnica de céu vazio, queda vertical e fade genérico, nem receber o rótulo de cinematográfica sem julgamento humano dos keyframes."
    - id: P-10-ACCESS
      requirement_id: INTRO-02
      provenance: flagged-unverified
      category: safety
      status: unresolved
      statement: "A ornamentação MUST NOT funcionar como loading, modal ou barreira que atrasa foco, navegação, scroll, toque ou o estado final solicitado por reduced motion."
---

<objective>
Substituir integralmente a composição rejeitada por um protótipo editorial contínuo e produzir evidência visual determinística antes de investir na timeline completa.

Purpose: resolver cedo o maior risco da fase — qualidade subjetiva — usando frames comparáveis em desktop e mobile, em vez de aceitar novamente uma animação tecnicamente correta e visualmente seca.
Output: cena estratificada, instrumentação Playwright test-only, duas contact sheets em 0/40/70/88/100% e checkpoint humano bloqueante.

Decisões cobertas neste gate: D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08, D-09, D-10, D-12, D-13, D-14, D-15, D-16, D-18, D-19, D-26, D-27, D-28 e D-29.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-abertura-cinematogr-fica-do-p-r-do-sol/10-CONTEXT.md
@.planning/phases/10-abertura-cinematogr-fica-do-p-r-do-sol/10-RESEARCH.md
@.planning/phases/10-abertura-cinematogr-fica-do-p-r-do-sol/10-PATTERNS.md
@.planning/phases/10-abertura-cinematogr-fica-do-p-r-do-sol/10-VALIDATION.md
@src/lib/cinematicIntro.ts
@src/lib/cinematicIntro.test.ts
@src/routes/Home.tsx
@src/components/layout/Shell.tsx
@src/components/invite/Hero.tsx
@src/components/invite/SeaWaves.tsx
@src/index.css
@tests/cinematic-intro.spec.ts
@playwright.config.ts

The existing 10-01/02/03 SUMMARY files describe the rejected implementation and are not visual precedent. Do not use them to preserve sky-only, vertical fall, 2000ms, `descending -> revealing`, or the 260ms group fade.
</context>

## Artifacts this phase produces

| Symbol / file / artifact | Kind | Produced in |
|---|---|---|
| `IntroState = "playing" \| "complete"` | replaced lifecycle contract | Task 1 |
| `CINEMATIC_INTRO_DURATION_MS = 3000` | replaced timing contract | Task 1 |
| `window.__pwCinematicIntroTimeline` | namespace criada somente pelo `page.addInitScript()` do visual spec; ausente do bundle | Task 2 |
| `[data-intro-scene]` | semantic scene root | Task 2 |
| `[data-intro-sun]` | one canonical production selector | Task 2 |
| `[data-intro-layer]` | atmospheric layer contract | Task 2 |
| `[data-intro-track]` | semantic ownership marker for finite intro WAAPI effects | Task 2 |
| `[data-intro-copy="primary\|secondary"]` | content hierarchy contract | Task 2 |
| `tests/cinematic-intro-visual.spec.ts` | deterministic capture spec | Task 2 |
| `artifacts/intro-keyframes-desktop.png` | 1280x800 contact sheet | Task 2 |
| `artifacts/intro-keyframes-mobile.png` | 320x760 contact sheet | Task 2 |

## Probe Assumptions (unresolved — flagged, not backstopped)

| Requirement | Deterministic edge probe | Assumption carried forward |
|---|---|---|
| INTRO-01 | `unclassified` / `unresolved` | Geometry and identity can be automated, but editorial quality and perceptual continuity require the explicit blocking checkpoint in Task 3. |
| INTRO-02 | `unclassified` / `unresolved` | Interaction, reduced motion and performance contracts are planned, but the probe supplies no classified edge set; Plans 10-02/10-03 must enforce them directly. |

No prohibition descriptor is minted: P-10-VISUAL and P-10-ACCESS remain flagged-unverified judgment/safety assumptions, exactly as required by the spec-less fallback.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Retire the rejected phase model and make the approval gate fail-safe</name>
  <read_first>
    - src/lib/cinematicIntro.ts
    - src/lib/cinematicIntro.test.ts
    - src/routes/Home.tsx
    - src/components/layout/Shell.tsx
    - src/components/invite/Hero.tsx
    - .planning/phases/10-abertura-cinematogr-fica-do-p-r-do-sol/10-CONTEXT.md
  </read_first>
  <files>src/lib/cinematicIntro.ts, src/lib/cinematicIntro.test.ts, src/routes/Home.tsx, src/components/layout/Shell.tsx, src/components/invite/Hero.tsx</files>
  <behavior>
    - Eligibility remains mount-scoped for `/` and `/#inicio`, while direct known section fragments remain complete and scroll to their target (D-20, D-21, D-22).
    - The lifecycle vocabulary is `playing | complete`; the rejected `descending | revealing` phase and 260ms reveal timer no longer exist.
    - Removing the rejected blanket reveal never leaves the route blocked: skip link, topbar and any control already visibly presented stay operable. Once Task 2 stages copy, only the still-invisible primary/secondary group may be inert, and each group must unlock at its own visible onset.
    - Reduced motion and ineligible frequency/known-fragment entries are synchronously complete: both content groups are fully visible, non-inert and operable, no finite intro animation is created, and the app never creates a test namespace or seek API (D-18, D-20, D-21, D-22).
    - Any WAAPI/controller setup or playback failure commits `complete` immediately, exposing the same fully visible, non-inert final hero (D-28).
  </behavior>
  <action>
    Replace the old two-step visual phase contract in `cinematicIntro.ts`, its unit tests, `Home.tsx`, `Shell.tsx`, and `Hero.tsx`. Preserve the valid policies for eligible hashes, known direct fragments, mount-scoped reentry and skip-link ordering, but remove the 2000ms descent, separate 260ms reveal, vertical WAAPI effect, blanket `[data-intro-reveal]` behavior and any production dependency on `data-testid`.

    Establish the final `IntroState` and 3000ms nominal clock constants now so subsequent work does not inherit rejected names. Make the approval gate fail-safe: remove blanket page/chrome inert, keep the skip link first and outside every inert region, and keep the topbar plus any control that is actually visible operable. Model `complete` as the only bypass/error result: reduced motion, ineligible frequency/known-fragment entry and any setup/playback exception must synchronously expose the final hero with both semantic content groups visible, non-inert and operable. During ordinary `playing`, permit `inert` only on an individual copy group while that exact group is still visually absent; Task 2 owns the timed onset and release. No test mode, global bridge or query-controlled storyboard path may be compiled into the app. Do not update `DESIGN.md` in this plan; it is deliberately held until human approval.
  </action>
  <verify>
    <automated>npm test -- src/lib/cinematicIntro.test.ts &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Unit tests reject the old state names, 2000ms constant and 260ms reveal contract.
    - `/`, `/#inicio` and direct-fragment policy remain explicit and storage-free.
    - Unit coverage makes `complete` the immediate fully visible/non-inert result for reduced motion, ineligible frequency/known-fragment entry and explicit fail-open completion.
    - A production build has no test-only mode, seek API or global test namespace; no blanket hero/chrome inert remains, and only a not-yet-visible semantic copy group may be inert during `playing`.
  </acceptance_criteria>
  <done>The obsolete visual state machine is gone, valid entry policy is preserved, and the approval gate cannot strand the normal invitation.</done>
</task>

<task type="auto">
  <name>Task 2: Build the continuous editorial storyboard and deterministic contact sheets</name>
  <read_first>
    - src/components/invite/Hero.tsx
    - src/components/invite/SeaWaves.tsx
    - src/index.css
    - tests/cinematic-intro.spec.ts
    - playwright.config.ts
    - DESIGN.md
    - .planning/phases/10-abertura-cinematogr-fica-do-p-r-do-sol/10-RESEARCH.md
    - .planning/phases/10-abertura-cinematogr-fica-do-p-r-do-sol/10-PATTERNS.md
  </read_first>
  <files>src/components/invite/Hero.tsx, src/index.css, tests/cinematic-intro-visual.spec.ts, .planning/phases/10-abertura-cinematogr-fica-do-p-r-do-sol/artifacts/intro-keyframes-desktop.png, .planning/phases/10-abertura-cinematogr-fica-do-p-r-do-sol/artifacts/intro-keyframes-mobile.png</files>
  <action>
    Recompose the real `Hero` as one continuously mounted scene. Add semantic, pointer-transparent layers for final sky base, cool veil, warm horizon, far/near clouds, haze, the existing sea/waves, horizon depth, a simple masked reflection, subtle static texture and side silhouettes/palms. Use existing semantic color tokens/currentColor only. Keep exactly one `[data-intro-sun]` inside the real responsive target; never create an intro clone or alternate hero. The first sample must already contain a recognizable landscape per D-01, D-02, D-03, D-04 and D-05.

    Define desktop and mobile art-direction variables independently. Desktop and 320x760 must show different composition choices per D-16, while both preserve the final real target per D-19. Storyboard the canonical progress points as actual provisional finite WAAPI effects on an eligible normal production-preview entry—not behind any test condition—so the sol has diagonal X+Y movement (D-06), camera/parallax reads subtly (D-07, D-08), and halo plus reflection connect before landing (D-09, D-10). Keep skip link, topbar and any other control that is already visibly presented operable throughout this pre-approval playback. Stage `[data-intro-copy="primary"]` and `[data-intro-copy="secondary"]` separately near 76–100%: both begin visually absent and inert; remove `inert` from primary at the first keyframe where it actually becomes visible, then remove it from secondary/CTAs only at their later visible onset. The two releases must be driven from the same nominal timeline as their opacity/transform onset, not from a final 3000ms blanket timer. Any exception while creating, coordinating or advancing the provisional WAAPI effects must immediately commit `complete`, remove inert from both groups and expose the fully visible final hero. Reduced motion, ineligible frequency and known direct fragments must enter that same final visible/operable state without creating the provisional timeline. Large moving surfaces may sample only `transform` and `opacity`; masks, texture, gradients and blur remain static.

    Do not add any preview/seek API, `import.meta.env.DEV` bridge or test namespace to production source. Mark each finite coordinated WAAPI owner with a semantic `data-intro-*` attribute (prefer an explicit `data-intro-track` value alongside scene/layer/copy/sun semantics); production behavior must not read those markers as a test switch.

    Create `tests/cinematic-intro-visual.spec.ts` with a helper that calls `page.addInitScript()` before `page.goto('/')`. Inside that page-world init script, save and delegate to the original `Element.prototype.animate` for every call, then inspect the returned effect timing and pause/register the handle only when the animated element itself owns a unique `data-intro-track` value and its duration/iterations are finite. Store handles keyed by that semantic track value and a test-owned `seek(progress)` helper in `window.__pwCinematicIntroTimeline`, a namespace created exclusively by the injected script. Non-intro or infinite animations must be delegated unchanged, never paused or registered. After navigation, compare the registered keys with the `[data-intro-track]` set rendered by the real hero and wait until every coordinated track is present; clamp progress and assign `currentTime = progress * 3000` to every captured finite handle, then wait two `requestAnimationFrame` paints before capture so the whole timeline is sampled together.

    Add normal-playback accessibility cases against the same production preview: before each copy onset its group is visually absent and inert; at primary's first visible sample it is non-inert while the still-hidden secondary/CTA group remains inert; at secondary's later visible onset its links become non-inert and focusable. Assert that no CTA with computed opacity/visibility making it invisible is focusable, while the skip link and visibly presented topbar controls remain usable. Add reduced-motion and direct-fragment/frequency-bypass cases proving an immediate final visible, non-inert state, plus a forced WAAPI/controller failure case proving the same fail-open result without an uncaught page error.

    Also open the normal production preview without `addInitScript` and prove `window.__pwCinematicIntroTimeline` is absent. Use the injected helper only in the capture cases to seek 0, 0.40, 0.70, 0.88 and 1.00 at 1280x800 and 320x760, capture the real hero, then use a temporary Playwright page with a labeled CSS grid to compose exactly two PNG contact sheets without adding an image package. Generate the two committed artifacts. Do not create permanent pixel baselines yet and do not edit `DESIGN.md`.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; npx playwright test tests/cinematic-intro-visual.spec.ts --project=emulated-chromium-desktop</automated>
  </verify>
  <acceptance_criteria>
    - Both PNGs exist, are non-empty, label all five required progress samples, and are generated by a repeatable Playwright command.
    - The production preview without `page.addInitScript()` has no `window.__pwCinematicIntroTimeline`; no production source exports or consults a seek/preview bridge.
    - The injected wrapper delegates every `animate()` call and pauses/registers only finite animations whose owning element is marked with semantic `data-intro-*`; a non-intro animation remains unregistered and follows normal playback.
    - At 0% the horizon, sea and depth are visibly present; at 70–88% halo/reflection form the landing bridge; at 100% the canonical sun is aligned with its real target.
    - In successful normal playback, primary and secondary start inert while invisible; primary becomes non-inert at its visible onset before secondary, and CTA links become focusable only at secondary's visible onset. Skip/topbar controls that are already visible remain usable throughout, and no invisible CTA is focusable.
    - Reduced motion, ineligible frequency/known-fragment entry and forced WAAPI/controller failure all expose the immediate final hero with both copy groups visible, non-inert and operable; failure emits no uncaught page error.
    - Mobile is a separate vertical composition, not a clipped desktop canvas.
    - Production selectors are semantic; no CSS/controller behavior depends on `data-testid`.
    - No new package, video, canvas, remote image, animated filter, animated gradient or second sun is introduced.
  </acceptance_criteria>
  <done>The replacement direction exists as deterministic, reviewable desktop and mobile storyboards, without claiming visual approval.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Approve or reject the art direction before timeline/lifecycle execution</name>
  <read_first>
    - .planning/phases/10-abertura-cinematogr-fica-do-p-r-do-sol/artifacts/intro-keyframes-desktop.png
    - .planning/phases/10-abertura-cinematogr-fica-do-p-r-do-sol/artifacts/intro-keyframes-mobile.png
    - .planning/phases/10-abertura-cinematogr-fica-do-p-r-do-sol/10-CONTEXT.md
  </read_first>
  <files>.planning/phases/10-abertura-cinematogr-fica-do-p-r-do-sol/artifacts/intro-keyframes-desktop.png, .planning/phases/10-abertura-cinematogr-fica-do-p-r-do-sol/artifacts/intro-keyframes-mobile.png</files>
  <action>Pause execution and present the two generated contact sheets to the user. Do not infer approval from green tests, silence, `--auto`, or prior discussion choices. Record the user's explicit verdict against every criterion below; if any criterion is rejected, return to Task 2 and regenerate both artifacts before asking again.</action>
  <what-built>Uma paisagem editorial contínua em cinco keyframes determinísticos para desktop 1280x800 e mobile 320x760. Nenhuma timeline completa, resize/bfcache ou baseline permanente deve avançar antes deste gate.</what-built>
  <how-to-verify>
    1. Abra as duas contact sheets lado a lado.
    2. Julgue o frame 0: deve parecer uma ilustração editorial finalizada, com paisagem contínua e profundidade; rejeite se parecer gradiente/fundo de protótipo.
    3. Compare 0/40/70%: o sol deve descrever um arco diagonal natural e a mudança de enquadramento deve parecer recuo de câmera, não zoom de interface.
    4. Compare 70/88/100%: halo, horizonte e reflexo devem formar uma ponte contínua; rejeite qualquer encaixe seco, corte, swap ou fade-cortina.
    5. Julgue mobile separadamente: arco compacto, horizonte baixo, reflexo vertical e palmeiras laterais precisam parecer dirigidos para 320x760, não recortados do desktop.
    6. Confirme que 100% continua sendo reconhecivelmente o hero do convite e que título/data precedem convite/CTAs.
  </how-to-verify>
  <acceptance_criteria>
    - Aprovação explícita de qualidade editorial.
    - Aprovação explícita da continuidade da paisagem.
    - Aprovação explícita da ponte luz/halo/reflexo.
    - Aprovação explícita da direção mobile.
    - Aprovação explícita de que o recuo lê como câmera, não zoom.
  </acceptance_criteria>
  <verify>
    <human-check>O usuário aprovou explicitamente qualidade editorial, continuidade, ponte luz/reflexo, direção mobile e câmera-versus-zoom nas duas contact sheets.</human-check>
  </verify>
  <done>Os cinco critérios têm aprovação explícita registrada e 10-02 está liberado; caso contrário, o plano permanece neste checkpoint.</done>
  <resume-signal>Responda `approved` para liberar 10-02, ou descreva os pontos rejeitados; qualquer rejeição mantém 10-02 e 10-03 bloqueados.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| URL/system preference → route state | Hash e `prefers-reduced-motion` são entradas externas que decidem se qualquer estado visual pode tocar a interface. |
| Playwright init script → production preview page world | A instrumentação pode observar WAAPI no teste, mas sua namespace e seu seek não podem existir sem injeção externa nem entrar no bundle. |
| Decorative scene → semantic UI | Arte fica sobre o hero, porém não pode capturar pointer, foco ou esconder controles. |

## STRIDE Threat Register — ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-10-01-A | Tampering | wrapper WAAPI injetado por `page.addInitScript` | medium | mitigate | Delegar sempre ao `animate` original, registrar apenas owners semânticos `data-intro-*`, limitar seek a progresso clampado e provar em teste separado que `window.__pwCinematicIntroTimeline` não existe sem injeção. |
| T-10-01-B | Denial of Service | layers do hero / `inert` | high | mitigate | Arte `pointer-events:none`; skip/topbar visíveis ficam disponíveis; cada grupo de copy é inert somente enquanto invisível e desbloqueia no próprio onset; reduced motion, bypass e falha convergem ao final visível/não-inert; browser test proíbe CTA invisível focável. |
| T-10-01-C | Information Disclosure | screenshots de aprovação | low | accept | Os artifacts contêm apenas a arte pública do convite, sem PII, segredos ou dados operacionais. |
| T-10-SC | Tampering | npm package installs | high | mitigate | Este plano proíbe installs e reutiliza React/CSS/SVG/Playwright já auditados; qualquer desvio exige parar e executar o Package Legitimacy Gate. |

**Blocking rule:** nenhum achado high pode ser aceito. T-10-01-B e T-10-SC precisam estar mitigados antes do checkpoint humano.
</threat_model>

<verification>
1. `npm test -- src/lib/cinematicIntro.test.ts`
2. `npm run build`
3. `npx playwright test tests/cinematic-intro-visual.spec.ts --project=emulated-chromium-desktop`
4. No visual spec, verificar separadamente que a namespace injetada não existe no preview de produção aberto sem `page.addInitScript`.
5. Verificar que os dois PNGs existem e contêm 0/40/70/88/100%.
6. Checkpoint humano aprova/rejeita exatamente os cinco critérios perceptuais; aprovação automática é proibida.
</verification>

<success_criteria>
- A implementação rejeitada foi removida, não apenas polida.
- O frame inicial é uma paisagem editorial completa em desktop e mobile.
- A mesma cena mostra arco, câmera, luz/reflexo, hierarquia e hero final em cinco samples determinísticos.
- O user aprova explicitamente direção editorial, continuidade, ponte luminosa, mobile e leitura de câmera.
- `DESIGN.md`, timeline completa, lifecycle e baselines permanecem inalterados/bloqueados até a aprovação.
</success_criteria>

## Source-audit conclusion for this plan

- **GOAL:** o risco perceptual da cena contínua é coberto pelo storyboard e checkpoint.
- **REQ:** INTRO-01 é diretamente coberto; INTRO-02 é preservado pelo gate fail-safe e arte não-interativa.
- **RESEARCH:** early visual approval, semantic layers, one canonical sun, no new dependency e performance-safe properties estão cobertos.
- **CONTEXT:** D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08, D-09, D-10, D-12, D-13, D-14, D-15, D-16, D-18, D-19, D-26, D-27, D-28 e D-29 são executados ou julgados aqui; D-11, D-17 e D-20–D-25 seguem explicitamente para 10-02/10-03.
- **Probe fallback:** INTRO-01 e INTRO-02 continuam `unclassified/unresolved`; nenhuma cobertura foi inventada.

<output>
Create `.planning/phases/10-abertura-cinematogr-fica-do-p-r-do-sol/10-01-SUMMARY.md` after the checkpoint is approved. Record the explicit approval criteria and artifact paths; if rejected, revise this plan's artifacts and repeat the checkpoint instead of starting 10-02.
</output>
