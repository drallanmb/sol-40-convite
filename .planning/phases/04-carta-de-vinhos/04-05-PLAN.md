---
phase: 04-carta-de-vinhos
plan: 05
type: execute
wave: 4
depends_on: ["04-01", "04-02", "04-03", "04-04"]
files_modified:
  - .planning/phases/04-carta-de-vinhos/04-PALETTE-REFERENCES.md
  - convex/schema.ts
  - convex/wineModel.ts
  - convex/wineCatalog.ts
  - convex/wineInternal.ts
  - convex/wines.ts
  - convex/wines.test.ts
  - convex/_generated/*
  - src/routes/Presentes.tsx
  - src/components/gifts/{WineImage,WineBottleVisual,WineCard,GiftPreview,WineCatalog}.tsx
  - src/index.css
  - package.json
  - scripts/audit-wine-assets.mjs
  - public/wines/manifest.json
autonomous: true
requirements: [GIFT-01, GIFT-02, GIFT-03, GIFT-04]

must_haves:
  truths:
    - "D-10/D-13: todos os cards usam uma única silhueta autoral neutra, sem foto, logo, marca, arte copiada, texto de rótulo ou claim de fidelidade."
    - "D-11: os 37 registros comerciais permanecem intactos; `imageUrl`, manifest e arquivos de foto deixam de integrar o contrato final."
    - "D-14: cada productCode possui exatamente dois hex muted distintos, uma URL https oficial/reputável e data de consulta; somente os hex entram no DTO público."
    - "A aplicação não baixa, publica, hotlinka ou solicita em runtime nenhuma imagem de rótulo; URLs de provenance não são image sources."
    - "D-15: catálogo é 1 coluna <768, 2 em 768–1023, 3 em 1024–1279 e 4 somente >=1280; preview fixa continua 1/2/3."
    - "D-21/D-22: gifted conserva silhueta/paleta/posição, perde o WhatsApp e atualiza nas duas views sem reload."
    - "A reconciliação adiciona/atualiza paletas e remove `imageUrl` legado sem tocar em status, giftedBy ou giftedAt."
    - "O schema aceita `imageUrl` apenas como campo legado opcional durante limpeza; queries e tipos públicos nunca o retornam."
    - "Loading/error/empty/partial permanecem cobertos; não existe mais estado separado de mídia quebrada porque o visual é markup local determinístico."
    - "Silhueta e halo são decorativos `aria-hidden`; nome, produtor, preço e código fornecem identidade acessível sem descrição enganosa da garrafa."
  artifacts:
    - path: ".planning/phases/04-carta-de-vinhos/04-PALETTE-REFERENCES.md"
      provides: "tabela 37/37 de productCode, nome, dois hex, URL, tipo de fonte e data"
    - path: "convex/wineCatalog.ts"
      provides: "37 paletas e provenance canônicos; nenhum imageUrl"
    - path: "convex/wineModel.ts"
      provides: "validadores hex/https/date e DTO público com dois hex"
    - path: "convex/schema.ts"
      provides: "migração em dois pushes: campos novos opcionais para backfill, depois paleta/provenance obrigatórios e imageUrl legado opcional"
    - path: "convex/wineInternal.ts"
      provides: "reconciliação de paleta, limpeza legada e smoke reversível existente"
    - path: "convex/wines.ts"
      provides: "projeção pública dos dois hex sem provenance/imageUrl"
    - path: "convex/wines.test.ts"
      provides: "37 paletas, schema, privacy, reconciliation e regressão gifted"
    - path: "src/routes/Presentes.tsx"
      provides: "validação fail-closed do DTO por paleta, sem dependência de imageUrl"
    - path: "src/components/gifts/WineBottleVisual.tsx"
      provides: "silhueta SVG/CSS autoral única + halo bipartido"
    - path: "src/components/gifts/WineImage.tsx"
      provides: "arquivo obsoleto removido"
    - path: "src/components/gifts/WineCard.tsx"
      provides: "visual determinístico available/gifted"
    - path: "src/components/gifts/GiftPreview.tsx"
      provides: "mesma silhueta/paleta no trio 1/2/3"
    - path: "src/components/gifts/WineCatalog.tsx"
      provides: "grid e skeleton 1/2/3/4"
    - path: "scripts/audit-wine-assets.mjs"
      provides: "infraestrutura obsoleta removida"
    - path: "public/wines/manifest.json"
      provides: "manifest obsoleto removido"
  key_links:
    - from: "convex/wineCatalog.ts"
      to: "convex/wineInternal.ts"
      via: "ensureWineCatalog reconcilia paleta/provenance e limpa imageUrl sem espalhar sobre gift state"
      pattern: "palettePrimary|paletteSecondary"
    - from: "convex/wines.ts"
      to: "src/components/gifts/WineBottleVisual.tsx"
      via: "PublicWine entrega somente palettePrimary/paletteSecondary"
      pattern: "palette(Primary|Secondary)"
    - from: "src/components/gifts/WineCard.tsx"
      to: "src/components/gifts/WineBottleVisual.tsx"
      via: "card e preview compartilham a mesma geometria e paleta por vinho"
      pattern: "WineBottleVisual"
    - from: "convex/wineInternal.ts#setWineGiftStateForSmoke"
      to: "src/routes/Presentes.tsx"
      via: "snapshot → gifted internal → subscriptions home/catalog → restore exato"
      pattern: "setWineGiftStateForSmoke"
  prohibitions:
    - "Não reintroduzir reserva, expiração, checkout ou mutation pública de vinho."
    - "Não expor giftedBy, giftedAt, paletteReferenceUrl ou paletteReferencedAt no DTO público."
    - "Não copiar, baixar, hotlinkar, scrapear ou versionar foto, logo, arte, tipografia ou composição de rótulo."
    - "Não afirmar que a silhueta reproduz a garrafa ou rótulo real."
    - "Não manter imagem/placeholder/fallback de imagem como solução final ou runtime paralelo."
    - "Não sobrescrever código da Fase 5; reler cada arquivo live e limitar diff aos paths declarados da Fase 4."
---

<objective>
Substituir o gate obsoleto de 37 fotos por um sistema final autônomo: 37 paletas de dois tons com provenance, uma silhueta autoral neutra compartilhada, grid 1/2/3/4 e remoção segura da infraestrutura de imagens.

Purpose: concluir GIFT-01–04 com a direção visual explicitamente aprovada, sem dependência externa de arquivos ou direitos de fotografia.

Output: catálogo/DTO reconciliados com paletas, visual local determinístico, legado de assets removido e validação end-to-end verde.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-carta-de-vinhos/04-CONTEXT.md
@.planning/phases/04-carta-de-vinhos/04-UI-SPEC.md
@.planning/phases/04-carta-de-vinhos/04-VALIDATION.md
@.planning/phases/04-carta-de-vinhos/04-01-SUMMARY.md
@.planning/phases/04-carta-de-vinhos/04-02-SUMMARY.md
@.planning/phases/04-carta-de-vinhos/04-03-SUMMARY.md
@.planning/phases/04-carta-de-vinhos/04-04-SUMMARY.md
@/Users/allanmesquitabrito/.codex/generated_images/019f9644-e00b-7f73-85a6-5f70928442f4/call_M6hysFZ2GBdBeROsuWq63oMA.png
@convex/wineCatalog.ts
@convex/wineModel.ts
@convex/wineInternal.ts
@convex/wines.ts
@convex/wines.test.ts
@src/components/gifts/WineImage.tsx
@src/components/gifts/WineCard.tsx
@src/components/gifts/GiftPreview.tsx
@src/components/gifts/WineCatalog.tsx
@src/index.css
</context>

## Artifacts this phase produces

| Symbol / file | Kind |
|---|---|
| `palettePrimary`, `paletteSecondary`, `paletteReferenceUrl`, `paletteReferencedAt` | novos campos comerciais canônicos |
| validators `assertMutedPalette`, `assertHttpsReference` | contratos de dados |
| `04-PALETTE-REFERENCES.md` | provenance legível 37/37 |
| `WineBottleVisual` | visual autoral compartilhado |
| grid `1/2/3/4` e skeleton correspondente | layout revisado |
| reconciliação que limpa `imageUrl` legado | migração idempotente |
| remoção de WineImage, manifest, auditor e npm script | cleanup |
| relatório de seed/smoke/browser | evidência final |

## Revision coverage audit

| SOURCE | Item | Coverage | Status |
|---|---|---|---|
| GOAL | Escolher vinho → WhatsApp sem duplicação | Tasks 1–3 preservam domínio, action gating e smoke reativo | COVERED |
| REQ | GIFT-01/02 | Task 1 mantém schema/37 records e adiciona paletas/provenance | COVERED |
| REQ | GIFT-03/04 | Tasks 2–3 preservam catálogo/WhatsApp e gifted sem action | COVERED |
| CONTEXT | D-10/D-13 revisadas | Task 2 cria uma silhueta autoral neutra | COVERED |
| CONTEXT | D-11 revisada | Tasks 1–2 preservam conteúdo e removem imageUrl/manifest runtime | COVERED |
| CONTEXT | D-14 revisada | Task 1 mapeia dois hex + URL/data para 37 códigos | COVERED |
| CONTEXT | D-15 revisada | Tasks 2–3 implementam/verificam catálogo 1/2/3/4 e preview 1/2/3 | COVERED |
| UI-SPEC | Estados, acessibilidade e referência visual | Tasks 2–3 cobrem markup local, aria-hidden e matriz | COVERED |
| SUPERSEDED | 37 fotos/alpha/crop/manifest/rights checkpoint | Removido por decisão explícita do usuário | EXCLUDED |

<threat_model>
OWASP ASVS L1 e propriedade intelectual; qualquer high bloqueia.

| ID | Ameaça | Controle |
|---|---|---|
| T-04-24 | URL/hex malformado chega ao CSS/cliente | regex estrita `#RRGGBB`, cores distintas, URL `https://` validada e testes 37/37. |
| T-04-25 | Provenance vira hotlink ou arte copiada | URL não entra no DTO; nenhuma tag img/fetch/background-image remoto; inspeção de bundle/network. |
| T-04-26 | Silhueta imita marca/rótulo | uma geometria neutra, label abstrato sem texto/logo e `aria-hidden`; revisão contra referência aprovada. |
| T-04-27 | Migração de imageUrl reseta presente | ensure patcha campos comerciais/undefined explicitamente e testes preservam status/giftedBy/giftedAt. |
| T-04-28 | Cleanup remove arquivo da Fase 5 | listar targets, reler live, diff name-status escopado e proibir deletions fora dos quatro assets da Fase 4. |
| T-04-29 | Smoke deixa dado alterado | snapshot retornado, trap/finally, restore exato e count/status final testados. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Mapear 37 paletas com provenance e reconciliar o domínio</name>
  <read_first>
    - convex/wineCatalog.ts (37 registros live; preservar campos comerciais e ordem)
    - convex/wineModel.ts, convex/schema.ts, convex/wineInternal.ts, convex/wines.ts
    - convex/wines.test.ts (testes já verdes; ampliar sem reescrever)
    - .planning/phases/04-carta-de-vinhos/04-CONTEXT.md (D-10, D-11, D-13, D-14 revisadas)
    - .planning/phases/04-carta-de-vinhos/04-UI-SPEC.md (§ Label Palette & Bottle Silhouette Contract)
  </read_first>
  <files>.planning/phases/04-carta-de-vinhos/04-PALETTE-REFERENCES.md, convex/wineCatalog.ts, convex/wineModel.ts, convex/schema.ts, convex/wineInternal.ts, convex/wines.ts, convex/wines.test.ts, convex/_generated/*, src/routes/Presentes.tsx</files>
  <behavior>
    - Exatamente 37 códigos canônicos têm primary/secondary distintos em `#RRGGBB`, URL https e data ISO.
    - PublicWine retorna os dois hex, mas nunca provenance, imageUrl ou gift identity.
    - Ensure executado duas vezes atualiza/limpa somente na primeira e preserva estado gifted integralmente.
    - Documento legado com imageUrl valida durante migração e termina sem o campo após ensure.
  </behavior>
  <action>
Pesquisar manualmente na web cada um dos 37 nomes/códigos. Preferir página oficial do produtor; se indisponível, página oficial Mistral ou retailer/catálogo reputável que identifique inequivocamente produto/safra. Observar somente a família cromática do rótulo, sem baixar/copiar a imagem. Escolher dois hex distintos e muted adequados ao cellar; registrar em `04-PALETTE-REFERENCES.md`: productCode, nome, primary, secondary, URL, `official-producer|official-retailer|reputable-catalog`, data UTC e nota curta de inspiração cromática sem reproduzir layout/arte. Não usar image search result URL como provenance final.

Escrever testes antes da produção. Adicionar os quatro campos ao WineCatalogItem e 37 records; criar validadores estritos de hex, URL https e data. Persistir paleta/provenance na tabela.

Executar a migração do schema em dois estágios, sem janela em que documentos legados sejam inválidos:

1. **Schema de transição:** adicionar `palettePrimary`, `paletteSecondary`, `paletteReferenceUrl` e `paletteReferencedAt` como opcionais e manter `imageUrl` opcional. Publicar esse schema combinado com `npx convex dev --once`. Queries permanecem fail-closed para registros ainda sem paleta e nunca projetam provenance/imageUrl.
2. **Backfill/reconciliação:** executar `ensureWineCatalog`, preencher os quatro campos em todos os 37 documentos, limpar `imageUrl` com `undefined`, preservar integralmente gift state e verificar por função internal/teste que existem 37/37 documentos completos e nenhum legado remanescente.
3. **Schema final:** somente após a verificação 37/37, tornar os quatro campos de paleta/provenance obrigatórios, manter apenas `imageUrl` opcional como tolerância de migração legada e publicar novamente com `npx convex dev --once`.

Remover `imageUrl` de WineCatalogItem, publicWineValidator e PublicWine. A query pública projeta somente primary/secondary. Atualizar também `src/routes/Presentes.tsx`: `isDisplayWine` valida os dois hex exigidos e deixa de exigir `imageUrl`; provenance continua ausente do cliente.

RELER schema live antes do patch e preservar todas as tabelas da Fase 5. Ampliar commercialFieldsChanged/ensure para palette/provenance e patchar `imageUrl: undefined` quando legado existir, sem tocar status/giftedBy/giftedAt. Testar 37/37, código `0699230`, invalid hex/url/date, privacy keys, primeira/segunda reconciliação, limpeza legada e gifted preservation. Regenerar somente por CLI sobre schema combinado.
  </action>
  <verify>
    <automated>npx vitest run convex/wines.test.ts -t "catalog|palette|schema|reconciliation|public queries|featured" && npm run build && git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - Tabela markdown e WINE_CATALOG têm os mesmos 37 códigos/hex/URLs/datas.
    - Toda URL é página identificável, https e registrada; nenhuma imagem é salva.
    - Todos os pares são distintos, muted e válidos; nenhuma palette está ausente.
    - Public DTO contém dois hex e omite provenance/imageUrl/private gift fields.
    - O primeiro push aceita os documentos legados; ensure completa 37/37 e limpa imageUrl; somente então o segundo push endurece os quatro novos campos.
    - Ensure preserva gift state e segunda execução é unchanged.
    - `Presentes.tsx` aceita o novo DTO somente com duas cores válidas e não depende de imageUrl/provenance.
    - Fase 5 permanece intacta em schema e gerados.
  </acceptance_criteria>
  <done>As 37 paletas têm provenance auditável e chegam reativamente ao cliente sem carregar arte remota ou expor metadados internos.</done>
</task>

<task type="auto">
  <name>Task 2: Implementar silhueta, grid 1/2/3/4 e remover legado de imagens</name>
  <read_first>
    - referência visual aprovada PNG (inspecionar; não copiar pixels/asset para o app)
    - src/components/gifts/WineImage.tsx, WineCard.tsx, GiftPreview.tsx, WineCatalog.tsx (RELER live)
    - src/index.css e package.json (RELER live; preservar Fase 5)
    - scripts/audit-wine-assets.mjs e public/wines/manifest.json (targets obsoletos exclusivos)
    - .planning/phases/04-carta-de-vinhos/04-UI-SPEC.md (§ Wine card, Responsive, Accessibility)
  </read_first>
  <files>src/components/gifts/WineImage.tsx, src/components/gifts/WineBottleVisual.tsx, src/components/gifts/WineCard.tsx, src/components/gifts/GiftPreview.tsx, src/components/gifts/WineCatalog.tsx, src/index.css, package.json, scripts/audit-wine-assets.mjs, public/wines/manifest.json</files>
  <action>
Criar `WineBottleVisual` como componente SVG/CSS local: halo circular bipartido primary/secondary, garrafa escura neutra com highlights discretos e retângulo cream completamente vazio/abstrato no lugar do label. Uma única geometria para todos os produtos, grupo visual `aria-hidden="true"`, sem `<img>`, remote URL, producer text, logo ou fidelity copy. Usar CSS custom properties tipadas/inline apenas para os dois hex já validados.

Substituir WineImage em catalog card e preview. Gifted suaviza o stage completo sem perder status textual. Remover handlers/loading/fallback de imagem. Catalog e skeleton usam `grid-cols-1 md:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4`, com quarto skeleton visível somente em xl. Preview continua `grid-cols-1 md:grid-cols-2 lg:grid-cols-3` porque são três cards. Confirmar que 1280 é o primeiro breakpoint de quatro colunas.

Antes do cleanup, executar `rg 'imageUrl|WineImage|audit:wine-assets|manifest.json|/wines/' convex src package.json` e resolver referências Phase 4. Excluir somente `WineImage.tsx`, `scripts/audit-wine-assets.mjs` e `public/wines/manifest.json`; remover apenas o script npm `audit:wine-assets`. Não tocar package-lock se nenhuma dependência mudar. Inspecionar `git diff --name-status` e falhar se houver deletion fora desses três paths ou alteração Phase 5. Remover tokens/classes de halo por tone somente quando não houver consumidor; manter tokens globais compartilhados.
  </action>
  <verify>
    <automated>npm test && npm run build && git diff --check && test ! -e src/components/gifts/WineImage.tsx && test ! -e scripts/audit-wine-assets.mjs && test ! -e public/wines/manifest.json && ! rg -n "imageUrl|WineImage|audit:wine-assets|/wines/" convex/wines.ts src/routes/Presentes.tsx src/components/gifts src/content/gifts.ts package.json</automated>
  </verify>
  <acceptance_criteria>
    - Visual usa markup local e exatamente uma geometria sem marca para todos os 37.
    - Halos usam dois hex do DTO; home e catálogo não possuem mapa duplicado.
    - Catálogo mede 1/2/3/4 nos breakpoints; preview mede 1/2/3.
    - Nenhuma tag img, URL remota, fallback de imagem ou fidelity claim existe na superfície gifts.
    - Manifest, auditor, WineImage e npm script foram removidos; nenhuma deletion externa ocorreu.
    - Testes/build passam com código da Fase 5 preservado.
  </acceptance_criteria>
  <done>A direção visual aprovada está implementada como silhueta própria e o legado fotográfico foi removido com diff estritamente escopado.</done>
</task>

<task type="auto">
  <name>Task 3: Executar seed, smoke reversível e matriz final autônoma</name>
  <read_first>
    - convex/wineInternal.ts (`ensureWineCatalog`, `setWineGiftStateForSmoke`)
    - convex/wines.test.ts e 04-VALIDATION.md
    - src/components/gifts/WineBottleVisual.tsx, WineCatalog.tsx, GiftPreview.tsx
    - .planning/phases/04-carta-de-vinhos/04-UI-SPEC.md (§ Visual Acceptance Matrix)
  </read_first>
  <files>convex/_generated/*</files>
  <action>
Confirmar que a Task 1 registrou os dois pushes da migração: schema de transição → ensure/backfill 37/37 → schema final. Rodar novamente `npx convex dev --once` no schema final combinado. Executar `npx convex run wineInternal:ensureWineCatalog '{}' --deployment dev` duas vezes e registrar segunda `created:0`, `updated:0`, total 37.

Com home e `/presentes` montadas, capturar snapshot e aplicar gifted no dev com `setWineGiftStateForSmoke` para `39778`; observar ambas as subscriptions sem reload, posição/paleta estáveis e anchor removido. Restaurar exatamente o `previousState` em trap/finally, nunca presumir available; confirmar views restauradas e total 37.

Usar Browser para a matriz 320, 375, 768, 1024 e 1280, mais 320 a 200%, teclado e reduced motion. Confirmar 1/1/2/3/4 colunas no catálogo, preview 1/1/2/3/3, três bands, 37 cards, paleta bipartida, silhueta idêntica/sem marca, long text, loading/error/empty/partial, available/gifted, direct/refresh/back/forward/gifted/unknown deep link e CTA RSVP partial/mixed/all-no. Inspecionar Network/DOM: zero requests para URLs de provenance/imagens, zero `<img>` na superfície gifts, target/rel/href WhatsApp corretos. Comparar composição visual à referência aprovada sem exigir pixel match.

Rodar full suite/build e revisar diff final. Não criar checkpoint: a direção visual e a substituição das fotos já foram aprovadas explicitamente pelo usuário.
  </action>
  <verify>
    <automated>npx vitest run convex/wines.test.ts src/lib/wineWhatsApp.test.ts src/lib/wineDeepLink.test.ts && npm test && npm run build && npx convex dev --once && git diff --check</automated>
    <human-check>Executante registra evidência Browser da matriz 320/375/768/1024/1280/200%, two-view smoke/restore, ausência de requests remotos e comparação com a referência aprovada.</human-check>
  </verify>
  <acceptance_criteria>
    - Seed é idempotente e total permanece 37.
    - Smoke muda/restaura home e catálogo sem reload e sem deixar gift state alterado.
    - Catálogo apresenta 4 colunas somente >=1280; preview nunca cria slot vazio.
    - 37 cards usam a mesma silhueta e seus pares específicos; nenhum logo/arte/texto de label aparece.
    - Network/DOM não contém remote label assets; provenance permanece server-side/documental.
    - WhatsApp/deep links/RSVP e todos os estados anteriores continuam verdes.
    - Full suite, build, Convex smoke e diff check passam; nenhum high permanece.
  </acceptance_criteria>
  <done>A Carta está concluída end-to-end com visual autoral, 37 paletas rastreáveis, responsividade revisada e nenhuma dependência de fotografia.</done>
</task>

</tasks>

<verification>
- `npx vitest run convex/wines.test.ts src/lib/wineWhatsApp.test.ts src/lib/wineDeepLink.test.ts`
- `npm test && npm run build && npx convex dev --once && git diff --check`
- Seed duas vezes; smoke two-view com snapshot/restore.
- Browser 320/375/768/1024/1280/200%, network e accessibility.
- `rg` final escopado sem imageUrl/WineImage/audit/paths `/wines/` em `convex/wines.ts`, `src/routes/Presentes.tsx`, `src/components/gifts`, `src/content/gifts.ts` e `package.json`; ocorrências legítimas da Fase 5 (por exemplo `MemoryCard`) ficam fora da auditoria e não são alteradas.
</verification>

<success_criteria>
- D-10/D-11/D-13/D-14/D-15 revisadas estão implementadas.
- 37 registros preservam conteúdo/status e recebem duas cores/provenance.
- Uma silhueta autoral neutra substitui toda dependência de foto.
- Grid é 1/2/3/4; preview é 1/2/3.
- Infraestrutura antiga de imagens foi removida sem tocar Fase 5.
- GIFT-01–04, WhatsApp, deep link, RSVP e reatividade continuam verdes.
</success_criteria>
