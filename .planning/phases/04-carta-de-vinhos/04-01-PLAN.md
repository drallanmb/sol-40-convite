---
phase: 04-carta-de-vinhos
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - convex/schema.ts
  - convex/wineModel.ts
  - convex/wineCatalog.ts
  - convex/wineInternal.ts
  - convex/wines.ts
  - convex/wineTest.ts
  - convex/wines.test.ts
  - convex/_generated/*
autonomous: true
requirements: [GIFT-01, GIFT-02, GIFT-03, GIFT-04]

must_haves:
  truths:
    - "Tracer-first de produção: os 37 registros canônicos percorrem catálogo tipado → reconciliação interna → query pública reativa antes dos contratos client-side e da expansão visual (D-07, D-09, D-11, D-22)."
    - "A tabela `wines` separa conteúdo comercial de `status`, `giftedBy` e `giftedAt`; código Mistral permanece string, inclusive `0699230` (GIFT-01)."
    - "O catálogo contém exatamente 37 registros integrais da fonte antiga, com códigos/imagens únicos, 13/10/14 nas três faixas e trio fixo `39778`, `39158`, `39470` (D-03, D-07, D-11, GIFT-02)."
    - "`ensureWineCatalog` é internal-only e idempotente: falha fechada em duplicata, preserva estado de presente, corrige só conteúdo comercial e reporta inesperados sem apagar."
    - "`setWineGiftStateForSmoke` é uma mutation interna operacional estreita: aceita estado validado, retorna snapshot anterior exato e suporta restauração sem presumir que o vinho começou available."
    - "A query pública retorna projeção explícita sem `_id`, `giftedBy`, `giftedAt` ou `updatedAt`, em categoria → preço → código; destaques mantêm ordem/status (D-04, D-09, D-22)."
  artifacts:
    - path: "convex/wineModel.ts"
      provides: "validadores, tipos, limites, ordem e PublicWine"
    - path: "convex/wineCatalog.ts"
      provides: "WINE_CATALOG e FEATURED_WINE_CODES"
    - path: "convex/schema.ts"
      provides: "tabela wines e índices"
    - path: "convex/wineInternal.ts"
      provides: "ensureWineCatalog e setWineGiftStateForSmoke internal-only"
      exports: ["ensureWineCatalog", "setWineGiftStateForSmoke"]
    - path: "convex/wines.ts"
      provides: "listCatalog e listFeatured públicos"
      exports: ["listCatalog", "listFeatured"]
    - path: "convex/wineTest.ts"
      provides: "makeWineTest"
    - path: "convex/wines.test.ts"
      provides: "schema, catálogo, reconciliação, smoke seam, query e privacidade"
    - path: "convex/_generated/*"
      provides: "declarações regeneradas sobre schema combinado"
  key_links:
    - from: "convex/wineCatalog.ts"
      to: "convex/wineInternal.ts"
      via: "reconciliação comercial sem spread sobre estado mutável"
      pattern: "WINE_CATALOG"
    - from: "convex/wines.ts"
      to: "convex/schema.ts"
      via: "índices by_category_price_code e by_product_code"
      pattern: "by_(category_price_code|product_code)"
    - from: "convex/wineInternal.ts#setWineGiftStateForSmoke"
      to: "convex/wines.ts#listCatalog"
      via: "mutation interna altera estado validado; subscription pública observa projeção; snapshot retornado restaura exatamente"
      pattern: "setWineGiftStateForSmoke"
  prohibitions:
    - "Não reintroduzir reserva temporária, expiração, checkout ou confirmação automática."
    - "Não expor `giftedBy` ou identidade de comprador publicamente."
    - "Não criar mutation pública de seed/status/reserva/presente; a seam de smoke deve existir somente em `internal`."
    - "Não hotlinkar/scrapear/copiar imagens sem permissão."
    - "Não aceitar placeholder como conclusão final."
    - "Não sobrescrever alterações paralelas da Fase 5 em `convex/schema.ts` ou gerados; reler live e aplicar somente o delta Fase 4."
---

<objective>
Entregar o tracer de produção Convex da Carta antes de qualquer expansão horizontal: schema, 37 registros, reconciliação segura, seam interna reversível de smoke e queries públicas reativas.

Purpose: provar GIFT-01/02/04 e a fonte pública de GIFT-03 sobre dados reais, com privacidade e restauração operacional garantidas.

Output: domínio Convex completo/testado e API gerada sem mutation pública de vinho.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-carta-de-vinhos/04-CONTEXT.md
@.planning/phases/04-carta-de-vinhos/04-RESEARCH.md
@.planning/phases/04-carta-de-vinhos/04-PATTERNS.md
@.planning/phases/04-carta-de-vinhos/04-VALIDATION.md
@convex/schema.ts
@convex/rsvpModel.ts
@convex/rsvpInternal.ts
@convex/rsvpTest.ts
@convex/rsvps.test.ts
@/Users/allanmesquitabrito/Documents/Site Sol 40 anos/sol-40-integrado/lib/wines.ts
</context>

## Probe assumptions resolved explicitly

| Requirement | Suposição concreta |
|---|---|
| GIFT-01 | Tabela validada com conteúdo comercial, status `available|gifted`, identidade/timestamp opcionais e projeção pública sem privados. |
| GIFT-02 | Exatamente 37 registros da fonte canônica, 13/10/14, códigos/imagens únicos e reconciliação idempotente. |
| GIFT-03 | O backend suficiente nesta wave é DTO público ordenado e reativo; anchor/copy/encoding são contratados no 04-02. |
| GIFT-04 | Gifted permanece no DTO/ordem; escrita pública é proibida e a transição de smoke é internal-only/reversível. |

## Artifacts this phase produces

| Symbol / file | Kind |
|---|---|
| `WineCategory`, `WineTone`, `WineStatus`, `WineGiftState`, `PublicWine` | contratos |
| `WINE_CATALOG`, `FEATURED_WINE_CODES` | catálogo canônico |
| `wines`, `by_product_code`, `by_category_price_code` | tabela/índices |
| `ensureWineCatalog` | reconciliação interna |
| `setWineGiftStateForSmoke` | seam operacional interna reversível |
| `listCatalog`, `listFeatured` | queries públicas |
| `makeWineTest`, `convex/wines.test.ts` | harness/suíte |
| `convex/_generated/*` | API regenerada |

<threat_model>
OWASP ASVS L1; qualquer high bloqueia.

| ID | Ameaça | Controle |
|---|---|---|
| T-04-01 | Buyer identity vaza | DTO/returns explícitos e teste das chaves. |
| T-04-02 | Reconciliação reseta presente | teste intercala gifted e confirma preservação. |
| T-04-03 | Duplicata/inconsistência | collect+fail closed; inesperados reportados. |
| T-04-04 | Seam operacional vira endpoint público | `internalMutation`, teste `internal` vs `api`, argumentos validados. |
| T-04-05 | Smoke destrói estado anterior | mutation retorna snapshot completo e teste restaura available e gifted preexistente. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Definir modelo, catálogo canônico e schema aditivo</name>
  <read_first>
    - convex/schema.ts (RELER LIVE; preservar tabelas/índices da Fase 5)
    - convex/rsvpModel.ts e convex/rsvps.test.ts (validadores/harness locais)
    - /Users/allanmesquitabrito/Documents/Site Sol 40 anos/sol-40-integrado/lib/wines.ts
    - .planning/phases/04-carta-de-vinhos/04-VALIDATION.md (rows schema/catalog)
  </read_first>
  <files>convex/wineModel.ts, convex/wineCatalog.ts, convex/schema.ts, convex/wineTest.ts, convex/wines.test.ts</files>
  <behavior>
    - Exatamente 37 códigos/imagens únicos, distribuição 13/10/14, preços safe-integer positivos e trio fixo.
    - Código `0699230` permanece string.
    - Schema aceita somente categorias, tonalidades e status conhecidos.
  </behavior>
  <action>
Escrever primeiro testes `schema` e `catalog`. Criar validadores/literais `ate-200|200-350|350-500`, `rubi|dourado|rose|verde`, `available|gifted`, estado de presente reutilizável, limites e PublicWine. Portar integralmente os 37 registros sem revisar copy/preço/safra por memória; adicionar categoryOrder e tuple fixa.

RELER `convex/schema.ts` imediatamente antes do patch e acrescentar apenas `wines` com campos comerciais, status/identity/time e índices `by_product_code`, `by_category_price_code`. Preservar toda adição live da Fase 5. Criar harness com modules injetados; `import.meta.glob` fica somente no teste.
  </action>
  <verify>
    <automated>npx vitest run convex/wines.test.ts -t "schema|catalog" && npm run build && git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - Testes provam 37, unicidade, 13/10/14 e fidelidade integral.
    - Schema/validators rejeitam literais/formas desconhecidos.
    - Diff de schema é aditivo e conserva Fase 5.
    - Harness deployable não usa import.meta.glob.
  </acceptance_criteria>
  <done>Modelo, dados e armazenamento do tracer estão definidos e protegidos por testes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implementar reconciliação e seam interna reversível de smoke</name>
  <read_first>
    - convex/wineModel.ts, convex/wineCatalog.ts, convex/schema.ts, convex/wines.test.ts (Task 1)
    - convex/rsvpInternal.ts (padrão internal writer)
    - .planning/phases/04-carta-de-vinhos/04-RESEARCH.md (§ idempotent reconciliation e Real Convex smoke)
  </read_first>
  <files>convex/wineInternal.ts, convex/wines.test.ts</files>
  <behavior>
    - Primeira ensure cria 37; segunda cria zero e count permanece 37.
    - Patch comercial preserva status/giftedBy/giftedAt; duplicata falha; inesperado é reportado.
    - `setWineGiftStateForSmoke` retorna snapshot anterior, aplica estado validado e restaura exatamente tanto available quanto gifted preexistente.
    - Ambas funções existem somente em `internal`.
  </behavior>
  <action>
Implementar `ensureWineCatalog` como internalMutation, por índice e sem espalhar objeto comercial sobre estado. Implementar `setWineGiftStateForSmoke` também como internalMutation com args `{ productCode, state }`, onde `state` é union validada de `{status:'available'}` ou `{status:'gifted', giftedBy, giftedAt}`. A mutation coleta exatamente um vinho, retorna `previousState` completo antes do patch, aplica/limpa os opcionais coerentemente e retorna estado atual. Nome e docstring devem afirmar uso operacional/dev; não adicionar flag/query pública.

Testar snapshot→gifted→restore para item initially available e snapshot→alteração→restore para item already gifted. Testar código inexistente/duplicado e afirmar que exports estão em `internal`, não na API pública.
  </action>
  <verify>
    <automated>npx vitest run convex/wines.test.ts -t "reconciliation|smoke seam|internal only" && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Idempotência e preservação de estado passam.
    - Snapshot contém exatamente o necessário para restauração sem suposição.
    - Restauração remove opcionais quando snapshot era available e repõe valores quando gifted.
    - Nenhuma mutation de wines é exportada por `api`.
  </acceptance_criteria>
  <done>Seed e alteração operacional de status são internas, testadas e reversíveis.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Fechar o tracer com queries públicas e geração combinada</name>
  <read_first>
    - convex/wineCatalog.ts, convex/wineModel.ts, convex/wineInternal.ts, convex/wines.test.ts
    - convex/rsvps.ts (args/returns/projeção pública)
    - convex/schema.ts (RELER LIVE antes de gerar)
  </read_first>
  <files>convex/wines.ts, convex/wines.test.ts, convex/_generated/*</files>
  <behavior>
    - listCatalog retorna 37 DTOs na ordem faixa/preço/código sem campos privados.
    - listFeatured retorna os três códigos fixos na ordem fixa e conserva gifted.
    - API gerada contém readers públicos e writers somente no namespace internal.
  </behavior>
  <action>
Escrever testes `public queries`/`featured`, incluindo conjunto exato de chaves. Criar queries com args/returns explícitos, ranges pelo índice composto e projeção compartilhada segura. Remontar destaques pela tuple, não por insertion order.

RELER schema e status do worktree; executar geração Convex somente sobre a árvore combinada. Não editar gerados à mão nem restaurar snapshot anterior à Fase 5.
  </action>
  <verify>
    <automated>npx vitest run convex/wines.test.ts && npm test && npm run build && git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - Ordem, featured e privacy passam.
    - `api.wines` contém apenas queries; `setWineGiftStateForSmoke` e ensure aparecem somente em internal.
    - Gerados preservam todas as funções live da Fase 5.
    - Full suite/build passam.
  </acceptance_criteria>
  <done>O tracer Convex de produção está completo, reativo e seguro antes da expansão client-side.</done>
</task>

</tasks>

<verification>
- `npx vitest run convex/wines.test.ts`
- `npm test && npm run build && git diff --check`
- Inspecionar API gerada: readers públicos, writers somente internal.
- ASVS L1 sem high.
</verification>

<success_criteria>
- GIFT-01/02 têm schema, 37 registros e reconciliação idempotente.
- GIFT-03 recebe DTO público ordenado.
- GIFT-04 pode ser testado live por seam internal reversível sem write público.
- Tracer precede contratos/UI.
- Fase 5 permanece intacta.
</success_criteria>

