---
phase: 04-carta-de-vinhos
plan: 03
type: execute
wave: 2
depends_on: ["04-01", "04-02"]
files_modified:
  - src/App.tsx
  - src/content/gifts.ts
  - src/components/gifts/WineImage.tsx
  - src/components/gifts/WineCard.tsx
  - src/components/gifts/WineCatalog.tsx
  - src/routes/Presentes.tsx
  - src/index.css
autonomous: true
requirements: [GIFT-03, GIFT-04]

must_haves:
  truths:
    - "D-01: `/presentes` é uma rota pública dedicada dentro do Shell, com abertura compacta e não um hero alto (D-12)."
    - "D-07/D-08/D-09: as três faixas ficam abertas, atalhos levam aos IDs exatos, e a ordem recebida por preço/código não muda por status."
    - "D-10/D-14/D-15: card exibe imagem real/fallback, status, preço, nome, produtor, descrição, código e ação em grade 1/2/3 com halo pela tonalidade."
    - "D-17/D-18/D-19/D-20: disponível possui um anchor externo direto para Vanessa com copy exata; clicar não executa mutation nem confirmação intermediária."
    - "D-21/D-22: gifted permanece na posição, com texto literal e action ausente; atualização reativa muda o card sem reload e preserva foco/scroll."
    - "UI empty/catalog: catálogo zero mostra heading/body exatos e remove atalhos, contagens, cards e links."
    - "UI empty/category band: faixa vazia permanece na ordem e mostra a copy exata de faixa vazia."
    - "UI empty/bottle media: falha de imagem preserva stage e conteúdo com `Foto temporariamente indisponível`."
    - "UI loading/catalog: intro, nota e navegação ficam visíveis; três headings e uma linha responsiva de skeletons sem shimmer usam `aria-busy` e anúncio único."
    - "UI loading/bottle media: dimensões intrínsecas reservam 720×960 e evitam layout shift."
    - "UI loading/band navigation: atalhos permanecem visíveis quando há dados pendentes e somem apenas no empty total."
    - "UI error/catalog: somente a região de dados vira alert inline com copy e retry exatos; fragmento da URL permanece."
    - "UI error/bottle media: erro de mídia não remove nome, status, preço, código ou ação."
    - "UI populated/catalog e bottle media: 37 itens, três faixas, fotos/halos e conteúdo completo obedecem o contrato."
    - "UI partial/catalog e bands: cards válidos continuam, um aviso não bloqueante aparece no topo e dado comercial ausente nunca é inventado."
    - "UI overflow/catalog e shortcuts: 320px não tem overflow de página; chips rolam/snapam com pista visual abaixo de 640px."
    - "UI overflow/wine text: nome, produtor, descrição e código quebram naturalmente sem truncar."
    - "UI zero-one-many/catalog e bands: zero tem estado próprio, um card ocupa uma coluna normal, 37 não usam virtualização, contagem singular/plural é estável."
    - "UI long-text/wine names, producers, descriptions, labels e CTA: `overflow-wrap:anywhere`, altura mínima e footer flexível impedem clipping."
  artifacts:
    - path: "src/routes/Presentes.tsx"
      provides: "rota, intro compacta, nota, atalhos, query reativa e efeito de deep link"
    - path: "src/components/gifts/WineCatalog.tsx"
      provides: "estados completos e três bands abertas"
    - path: "src/components/gifts/WineCard.tsx"
      provides: "card available/gifted sem mutation"
    - path: "src/components/gifts/WineImage.tsx"
      provides: "asset, halo, loading e fallback resiliente"
    - path: "src/content/gifts.ts"
      provides: "copy literal, metadados das faixas e nav da rota"
    - path: "src/index.css"
      provides: "tokens cellar/status/halos e motion rules"
    - path: "src/App.tsx"
      provides: "Route /presentes"
  key_links:
    - from: "src/routes/Presentes.tsx"
      to: "convex/wines.ts"
      via: "useQuery(api.wines.listCatalog) mantém status reativo"
      pattern: "api\\.wines\\.listCatalog"
    - from: "src/routes/Presentes.tsx"
      to: "src/lib/wineDeepLink.ts"
      via: "hash validado após dados, getElementById, scrollIntoView e focus"
      pattern: "productCodeFromWineHash"
    - from: "src/components/gifts/WineCard.tsx"
      to: "src/lib/wineWhatsApp.ts"
      via: "available cria anchor; gifted não cria action"
      pattern: "buildWineWhatsAppUrl"
    - from: "src/components/gifts/WineImage.tsx"
      to: "public/wines/manifest.json"
      via: "imageUrl local canônico, dimensões e fallback textual"
      pattern: "imageUrl"
  prohibitions:
    - "Não reintroduzir reserva/expiração/checkout/confirmation modal ou mutation ao clicar."
    - "Não expor `giftedBy` publicamente nem pedir identidade do convidado."
    - "Não criar mutation pública de wines."
    - "Não hotlinkar/scrapear/copiar imagens sem permissão."
    - "Não aceitar placeholder como estado final; `Imagem em preparação` é desenvolvimento somente."
    - "Não sobrescrever mudanças da Fase 5 em `src/App.tsx` ou `src/index.css`; reler live e aplicar delta mínimo."
---

<objective>
Construir a experiência completa `/presentes` sobre o tracer já aprovado: identidade de adega, estados de dados e mídia, três faixas abertas, cards available/gifted, deep link acessível e handoff direto para WhatsApp.

Purpose: tornar GIFT-03 e GIFT-04 utilizáveis sem duplicação visual, sem inventar reserva e sem depender de um framework DOM/E2E novo.

Output: rota pública responsiva e acessível, componentes de catálogo e imagem, conteúdo centralizado e tokens nomeados.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-carta-de-vinhos/04-CONTEXT.md
@.planning/phases/04-carta-de-vinhos/04-RESEARCH.md
@.planning/phases/04-carta-de-vinhos/04-PATTERNS.md
@.planning/phases/04-carta-de-vinhos/04-VALIDATION.md
@.planning/phases/04-carta-de-vinhos/04-UI-SPEC.md
@.planning/phases/04-carta-de-vinhos/04-01-SUMMARY.md
@.planning/phases/04-carta-de-vinhos/04-02-SUMMARY.md
@src/App.tsx
@src/components/layout/Shell.tsx
@src/components/ui/Button.tsx
@src/index.css
</context>

## Probe assumptions resolved explicitly

- GIFT-03 unresolved fica concretizado por anchor semântico, `target="_blank"`, `rel="noopener noreferrer"`, href do helper testado, 48px e suffix assistivo.
- GIFT-04 unresolved fica concretizado por `status` da query reativa: gifted mantém card/conteúdo/posição e remove o anchor do DOM, com anúncio polite quando a mudança atinge o card focado.

## Artifacts this phase produces

| Symbol / file | Kind |
|---|---|
| `GIFTS_COPY`, `GIFT_BANDS`, `GIFTS_NAV_LINKS` | conteúdo centralizado |
| `WineImage` | mídia resiliente com halo |
| `WineCard` | article available/gifted |
| `WineCatalog` | bands, grid e estados |
| `Presentes` | rota e deep link assíncrono |
| `/presentes` | nova rota pública |
| tokens `cellar`, `cellar-soft`, `cellar-line`, `cellar-muted`, `gifted`, `halo-*` | extensão do design system |

<threat_model>
ASVS L1; qualquer high bloqueia.

| ID | Ameaça | Controle |
|---|---|---|
| T-04-10 | Reverse-tabnabbing | todo external anchor usa `noopener noreferrer`. |
| T-04-11 | Ação gifted ainda acionável | anchor é removido do DOM, não apenas desabilitado visualmente. |
| T-04-12 | Hash malformado alcança seletor DOM | helper validado + `getElementById`; unknown é no-op. |
| T-04-13 | Dados privados entram no componente | props usam apenas PublicWine e não exibem campos ausentes no DTO. |
| T-04-14 | Falha de imagem elimina caminho de compra | fallback substitui só mídia; texto e action permanecem. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Centralizar conteúdo/tokens e registrar a rota compacta</name>
  <read_first>
    - src/App.tsx (RELER LIVE e preservar qualquer rota paralela)
    - src/index.css (RELER LIVE e preservar tokens/regras da Fase 5)
    - src/components/layout/Shell.tsx (navLinks e footer existentes)
    - src/content/event.ts (padrão de copy; não modificar neste plano)
    - .planning/phases/04-carta-de-vinhos/04-UI-SPEC.md (§ Design System, Copywriting, Information Architecture)
  </read_first>
  <files>src/App.tsx, src/content/gifts.ts, src/routes/Presentes.tsx, src/index.css</files>
  <action>
Criar `gifts.ts` com todas as strings literais da UI-SPEC, três IDs/labels/eyebrows/headings das faixas e nav da rota `Convite`, `Confirmar presença`, `Programação`, `Local` usando targets absolutos corretos. Não duplicar número/mensagem de WhatsApp.

RELER App e CSS live. Adicionar `/presentes` antes do wildcard sem reordenar/remover rotas concorrentes. Adicionar uma primeira composição de `Presentes` dentro do Shell com intro compacta, sol decorativo aria-hidden, nota operacional D-20, nav de atalhos sem accordion e região de catálogo preparada para Task 3. Aplicar tokens exatos da UI-SPEC uma vez no `@theme`; preservar todas as adições da Fase 5.

Respeitar somente 400/700, quatro tamanhos 13/16/28/48 nesta fase, gutters 24/32/64, cap 1320px, editorial 62ch, topbar 72px e intro 360/420 mínima. Coral não pode ser texto normal sobre cellar.
  </action>
  <verify>
    <automated>npm run build && npx vitest run src/content/event.test.ts && git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - `/presentes` renderiza dentro do Shell e wildcard continua intacto.
    - Intro usa copy literal, superfície cellar sólida e sol aria-hidden sem hero alto.
    - Os três atalhos têm nav/label/IDs exatos e alvos >=44px.
    - Todos os tokens exigidos existem uma vez; nenhum hex cellar/halo é espalhado em JSX.
    - Rotas e CSS live da Fase 5 continuam no diff final.
  </acceptance_criteria>
  <done>A página dedicada existe com IA, copy, tokens e navegação de faixa corretos, pronta para receber o catálogo.</done>
</task>

<task type="auto">
  <name>Task 2: Implementar mídia e card available/gifted</name>
  <read_first>
    - src/lib/wineWhatsApp.ts (helper testado do plano 01)
    - convex/wineModel.ts (PublicWine)
    - src/components/ui/Button.tsx (usar buttonClassName apenas se semântica/contraste servirem)
    - .planning/phases/04-carta-de-vinhos/04-UI-SPEC.md (§ Wine card, Available and gifted states, Bottle Asset Contract)
  </read_first>
  <files>src/components/gifts/WineImage.tsx, src/components/gifts/WineCard.tsx</files>
  <action>
Criar `WineImage` com stage 264px, halo 180/204 pela tonalidade, imagem front-facing local `width=720 height=960`, object-contain, alt `Garrafa do vinho {nome}`, decoding async e eager controlável para primeira linha; abaixo dela lazy. Em erro, remover a imagem falha e mostrar o texto runtime exato sem inventar garrafa. Enquanto o manifest estiver pending, permitir `Imagem em preparação` explicitamente marcado como desenvolvimento.

Criar `WineCard` como `<article id={wineDomId(code)} tabIndex={-1} aria-labelledby>`, ordem interna exata, min-height 540/580, preço tabular, código discreto e commercial text nunca truncado. Available mostra `Disponível` e um único `<a>` full-width com helper WhatsApp, target/rel/suffix sr-only. Gifted mantém conteúdo e dimensões, suaviza só mídia/descrição, mostra selo AA e não renderiza anchor ou botão. Implementar anúncio polite/foco no article quando um card focado muda available→gifted, sem alterar scroll.
  </action>
  <verify>
    <automated>npx vitest run src/lib/wineWhatsApp.test.ts src/lib/wineDeepLink.test.ts && npm run build && git diff --check</automated>
    <human-check>Inspecionar em browser um card disponível e um gifted: disponível tem exatamente um anchor; gifted tem zero e mantém todo conteúdo.</human-check>
  </verify>
  <acceptance_criteria>
    - Ordem e textos internos seguem UI-SPEC; nomes/produtores quebram com anywhere.
    - CTA mede >=48px e inclui target/rel/suffix.
    - Gifted possui selo literal, nenhum anchor e cues não dependentes só de cor/opacidade.
    - Falha/pending de mídia não esconde nenhuma informação ou ação.
    - Não existe handler de mutation/reserva/window.open.
  </acceptance_criteria>
  <done>O card apresenta produto e disponibilidade com semântica segura e mídia resiliente.</done>
</task>

<task type="auto">
  <name>Task 3: Completar catálogo, estados e deep link assíncrono</name>
  <read_first>
    - src/routes/Presentes.tsx e src/content/gifts.ts (Task 1)
    - src/components/gifts/WineCard.tsx (Task 2)
    - convex/wines.ts (listCatalog e ordem já testada)
    - src/lib/wineDeepLink.ts (parser seguro)
    - .planning/phases/04-carta-de-vinhos/04-UI-SPEC.md (§ Bands, Grid, Deep-Link, Loading/Error/Empty/Partial)
  </read_first>
  <files>src/components/gifts/WineCatalog.tsx, src/routes/Presentes.tsx</files>
  <action>
Criar `WineCatalog` com as três sections sempre montadas em ordem, aria-labelledby, eyebrow+h2+contagem estável singular/plural e grids 1/2/3 com gaps 16/16/24. Consumir a ordem da query sem ordenar por status. Implementar loading com headings e 1/2/3 skeletons estáticos por band, empty total, empty band, partial notice, error alert/retry e populated conforme copies exatas. Não introduzir accordion, tabs, filtro, paginação, virtualização, shimmer ou truncamento.

Em `Presentes`, usar `useQuery(api.wines.listCatalog)` como subscription reativa. Tratar o padrão `undefined` de loading e capturar erro/retry segundo o padrão existente sem alterar o hash. Depois que dados renderizarem, observar `location.hash`, parsear, localizar via `getElementById`, scroll center smooth/auto conforme reduced motion, focus preventScroll e marcar o selecionado persistentemente com outline 3px/offset 6 e label visível. Reexecutar em direct load, refresh e back/forward; gifted funciona; unknown/malformed não anuncia erro.

Garantir scroll-margin-top `calc(72px + 24px)`, shortcuts mobile scroll/snap e no page overflow a 320px. Reduced motion remove scroll smooth, hover translate, active scale e selected transition, mantendo cues.
  </action>
  <verify>
    <automated>npm test && npm run build && git diff --check</automated>
    <human-check>Executar matriz manual `/presentes` em 375/768/1280, teclado, 320px a 200%, reduced motion, direct/refresh/back/forward/gifted/unknown hash e loading/error/empty/image failure.</human-check>
  </verify>
  <acceptance_criteria>
    - 37 records aparecem em três faixas abertas, 1/2/3 colunas e sem overflow horizontal.
    - Todos os estados covered da UI-SPEC são observáveis com copy/semântica especificadas.
    - Contagens/ordem/altura não mudam quando status muda.
    - Deep link foca, centraliza e rotula o card depois da query, respeitando reduced motion.
    - Unknown hash é no-op seguro.
    - Nenhum pacote DOM/E2E foi adicionado.
  </acceptance_criteria>
  <done>`/presentes` está funcional, responsivo, reativo e acessível em todos os estados definidos.</done>
</task>

</tasks>

<verification>
- `npm test && npm run build && git diff --check`
- Matriz manual 375/768/1280, teclado, 320px/200%, reduced motion.
- Verificar available vs gifted, loading/error/empty/partial, image failure e hashes.
- ASVS L1 sem high.
</verification>

<success_criteria>
- GIFT-03 abre o WhatsApp correto somente em vinho disponível.
- GIFT-04 mantém gifted visível e sem ação, reagindo live.
- Todas as considerações covered de catálogo/bands/mídia/shortcuts/texto estão implementadas.
- D-01, D-05, D-07–D-15 e D-17–D-22 são observáveis.
- Nenhuma alteração paralela da Fase 5 foi perdida.
</success_criteria>
