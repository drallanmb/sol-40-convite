---
phase: 04-carta-de-vinhos
plan: 02
type: execute
wave: 1
depends_on: ["04-01"]
files_modified:
  - src/lib/wineWhatsApp.ts
  - src/lib/wineWhatsApp.test.ts
  - src/lib/wineDeepLink.ts
  - src/lib/wineDeepLink.test.ts
  - scripts/audit-wine-assets.mjs
  - public/wines/manifest.json
  - package.json
  - package-lock.json
autonomous: true
requirements: [GIFT-02, GIFT-03]

must_haves:
  truths:
    - "D-17/D-18/D-19: helper produz anchor destination `https://wa.me/5511993709046`, mensagem Unicode literal e BRL pt-BR codificados exatamente uma vez."
    - "D-20: helper não acessa DOM/Convex nem reserva/altera estado."
    - "D-05: deep link é `/presentes#vinho-{productCode}`, preserva zero inicial e rejeita hash hostil antes do DOM."
    - "D-13: preflight cobre 37 códigos/paths e default estrito permanece bloqueado até licença, identidade, alpha, dimensões e arquivos reais."
  artifacts:
    - path: "src/lib/wineWhatsApp.ts"
      provides: "formatBRL, buildWineWhatsAppMessage, buildWineWhatsAppUrl"
    - path: "src/lib/wineWhatsApp.test.ts"
      provides: "copy/destino/encoding/security"
    - path: "src/lib/wineDeepLink.ts"
      provides: "wineDomId e productCodeFromWineHash"
    - path: "src/lib/wineDeepLink.test.ts"
      provides: "round-trip e hashes hostis"
    - path: "scripts/audit-wine-assets.mjs"
      provides: "preflight e audit estrito"
    - path: "public/wines/manifest.json"
      provides: "37 slots pending sem fatos inventados"
    - path: "package.json"
      provides: "audit:wine-assets"
  key_links:
    - from: "convex/wineModel.ts#PublicWine"
      to: "src/lib/wineWhatsApp.ts"
      via: "type-only shape pública"
      pattern: "PublicWine"
    - from: "convex/wineCatalog.ts"
      to: "public/wines/manifest.json"
      via: "bijeção código/imageUrl auditada"
      pattern: "productCode"
  prohibitions:
    - "Não reintroduzir reserva/expiração/checkout."
    - "Não expor giftedBy."
    - "Não criar mutation pública."
    - "Não hotlinkar/scrapear/copiar assets sem permissão."
    - "Não aceitar placeholder como final."
    - "Não sobrescrever package files da Fase 5; reler live e aplicar delta mínimo."
---

<objective>
Após o tracer Convex, congelar os contratos puros do handoff/deep link e criar o preflight de mídia que permite desenvolvimento sem converter pendência externa em aceite.

Purpose: separar falhas client-side e de assets do contexto do domínio Convex.

Output: helpers testados, manifest estrutural 37/37 e auditor preflight/strict.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-carta-de-vinhos/04-CONTEXT.md
@.planning/phases/04-carta-de-vinhos/04-RESEARCH.md
@.planning/phases/04-carta-de-vinhos/04-UI-SPEC.md
@.planning/phases/04-carta-de-vinhos/04-VALIDATION.md
@.planning/phases/04-carta-de-vinhos/04-01-SUMMARY.md
@convex/wineCatalog.ts
@convex/wineModel.ts
@package.json
</context>

## Probe assumptions resolved explicitly

- GIFT-02: asset readiness é exatamente 37 manifest slots, mas somente audit estrito + checkpoint 04-05 satisfaz a mídia final.
- GIFT-03: anchor contract usa telefone constante, copy literal, BRL compartilhado, encoding único e fragmento validado; WebViews ficam na Fase 7.

## Artifacts this phase produces

| Symbol / file | Kind |
|---|---|
| `formatBRL`, `buildWineWhatsAppMessage`, `buildWineWhatsAppUrl` | helpers |
| `wineDomId`, `productCodeFromWineHash` | helpers |
| duas suítes unit/security | testes |
| `audit-wine-assets.mjs`, manifest 37 slots e script npm | gate de mídia |

<threat_model>
ASVS L1; high bloqueia.

| ID | Ameaça | Controle |
|---|---|---|
| T-04-06 | Query injection no wa.me | telefone constante, encoding da frase inteira, parse test. |
| T-04-07 | Hash hostil alcança seletor | digits-only, parse inteiro e downstream getElementById. |
| T-04-08 | Asset sem direito passa | strict exige approved/metadados/binário; preflight não afirma aceite. |
| T-04-09 | Merge package apaga Fase 5 | re-leitura live, npm/lockfile incremental. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar contratos puros de WhatsApp e deep link</name>
  <read_first>
    - .planning/phases/04-carta-de-vinhos/04-CONTEXT.md (D-05, D-17–D-20)
    - .planning/phases/04-carta-de-vinhos/04-UI-SPEC.md (§ Copywriting e Deep-Link)
    - src/lib/phone.ts e src/lib/phone.test.ts
    - convex/wineModel.ts
  </read_first>
  <files>src/lib/wineWhatsApp.ts, src/lib/wineWhatsApp.test.ts, src/lib/wineDeepLink.ts, src/lib/wineDeepLink.test.ts</files>
  <behavior>
    - Host/path/text são exatos; `&?#!` e acentos permanecem num único text.
    - `0699230` conserva zero; hashes vazios/markup/seletor/sufixo retornam null.
  </behavior>
  <action>
Escrever suítes primeiro. Criar formatter BRL único, telefone constante, mensagem literal D-19 e builder que codifica a frase completa uma vez. Aceitar apenas shape pública por type-only import ou interface equivalente.

Criar helper digits-only para DOM ID/hash, sem React/DOM/Convex runtime. Cobrir round-trip, leading zero e matriz hostil da RESEARCH.
  </action>
  <verify>
    <automated>npx vitest run src/lib/wineWhatsApp.test.ts src/lib/wineDeepLink.test.ts && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Testes comparam pathname e mensagem decodificada completos.
    - Delimitadores não criam segundo parâmetro.
    - Helpers não acessam window/document.
    - Código leading-zero round-trips.
  </acceptance_criteria>
  <done>WhatsApp e fragmento estão congelados por testes puros.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Criar manifest 37/37 e auditor preflight/strict</name>
  <read_first>
    - convex/wineCatalog.ts
    - package.json e package-lock.json (RELER LIVE; preservar Fase 5)
    - .planning/phases/04-carta-de-vinhos/04-RESEARCH.md (§ Asset audit)
    - .planning/phases/04-carta-de-vinhos/04-UI-SPEC.md (§ Bottle Asset Contract)
  </read_first>
  <files>scripts/audit-wine-assets.mjs, public/wines/manifest.json, package.json, package-lock.json</files>
  <behavior>
    - Preflight passa somente com bijeção 37 código/path e relata pending.
    - Strict falha em pending, ausência, duplicata, alpha/dimensão/budget/metadados.
  </behavior>
  <action>
Derivar manifest de 37 slots canônicos; manter fatos externos ausentes como `pending`, sem inventar source/contact/date e sem conversa privada. Criar auditor determinístico: `--preflight` valida estrutura/bijeção; default strict exige arquivo alpha 720×960 <=300KB, approved e provenance/transformações.

RELER package files; adicionar somente `audit:wine-assets`. Preferir Node nativo; se dependência for inevitável, pin exato e atualizar lock via npm preservando Embla/Fase 5.
  </action>
  <verify>
    <automated>npm run audit:wine-assets -- --preflight && npm test && npm run build && git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - Preflight confirma 37 únicos e saída deixa strict bloqueado.
    - Strict nunca aprova pending/placeholder.
    - package/lock preservam estado live.
    - Nenhum framework DOM/E2E é adicionado.
  </acceptance_criteria>
  <done>Pipeline de desenvolvimento pode avançar, mas mídia final continua corretamente bloqueada.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/lib/wineWhatsApp.test.ts src/lib/wineDeepLink.test.ts`
- `npm run audit:wine-assets -- --preflight`
- `npm test && npm run build && git diff --check`
- ASVS L1 sem high.
</verification>

<success_criteria>
- D-05 e D-17–D-20 possuem contratos puros exatos.
- D-13 possui preflight útil e strict impossível de burlar com pending.
- 04-01 permanece o tracer anterior a este plano.
- Fase 5 permanece intacta.
</success_criteria>

