---
phase: 04-carta-de-vinhos
plan: 04
type: execute
wave: 3
depends_on: ["04-01", "04-02", "04-03"]
files_modified:
  - src/content/event.ts
  - src/content/event.test.ts
  - src/components/gifts/GiftPreview.tsx
  - src/routes/Home.tsx
  - src/components/rsvp/FamilyForm.tsx
autonomous: true
requirements: [GIFT-03, GIFT-04]

must_haves:
  truths:
    - "D-02: header da home inclui `Presentes` → `/presentes`, e navs de RSVP/presentes usam fontes centralizadas."
    - "D-03: home exibe exatamente três intermediários fixos `39778`, `39158`, `39470` e CTA literal `Ver a carta completa`."
    - "D-04: os três cards consultam Convex, mantêm ordem/conteúdo quando gifted e nunca substituem o item."
    - "D-05: cada preview card é um único Link sem nested action para `/presentes#vinho-{código}`."
    - "D-06: callout de presentes aparece somente após primeiro `result.kind === saved`, para partial/mixed/all-no, persiste por sessão montada e nunca aparece em falha/initial/restored-clean."
    - "D-16: preview é bloco cellar completo imediatamente após Countdown e antes de Local; ordem Local→Guide→Programa→Traje e adições da Fase 5 são preservadas."
    - "UI loading/home preview: heading/support/CTA permanecem e exatamente três skeletons preservam altura em grid 1/2/3."
    - "UI error/home preview: somente grid vira alert/retry; CTA completo segue disponível."
    - "UI populated/home preview: trio/order/status/preço e deep links exatos aparecem."
    - "UI overflow/navigation: labels refluem, mobile nav continua acessível e não cria overflow."
    - "UI long-text/navigation: labels não truncam nem ficam inacessíveis por teclado."
    - "UI long-text/preview names, labels e CTA: wrapping natural não cria nested control nem clipping."
  artifacts:
    - path: "src/components/gifts/GiftPreview.tsx"
      provides: "dobra cellar reativa com trio fixo e deep links"
    - path: "src/routes/Home.tsx"
      provides: "GiftPreview no ponto acordado"
    - path: "src/content/event.ts"
      provides: "navs combinadas e copy do callout"
    - path: "src/content/event.test.ts"
      provides: "contratos exatos combinados das Fases 4 e 5"
    - path: "src/components/rsvp/FamilyForm.tsx"
      provides: "hasSavedSuccessfully e callout persistente"
  key_links:
    - from: "src/components/gifts/GiftPreview.tsx"
      to: "convex/wines.ts"
      via: "useQuery(api.wines.listFeatured)"
      pattern: "api\\.wines\\.listFeatured"
    - from: "src/components/gifts/GiftPreview.tsx"
      to: "src/lib/wineDeepLink.ts"
      via: "Link inteiro usa wineDomId para o fragmento"
      pattern: "wineDomId"
    - from: "src/routes/Home.tsx"
      to: "src/components/gifts/GiftPreview.tsx"
      via: "render imediatamente após Countdown"
      pattern: "<GiftPreview"
    - from: "src/components/rsvp/FamilyForm.tsx"
      to: "src/content/event.ts"
      via: "callout usa copy centralizada e Link /presentes"
      pattern: "hasSavedSuccessfully"
  prohibitions:
    - "Não reintroduzir reserva, expiração ou checkout."
    - "Não expor giftedBy publicamente."
    - "Não criar mutation pública de wines."
    - "Não hotlinkar/scrapear/copy-pastear assets."
    - "Não tratar placeholder como aceitação."
    - "Não sobrescrever mudanças paralelas da Fase 5 em event.ts, event.test.ts, Home.tsx ou outros compartilhados; reler live, preservar e testar o estado combinado."
---

<objective>
Conectar a Carta aos três pontos públicos decididos: header, dobra curada da home e sucesso real do RSVP, preservando integralmente a implementação paralela da Fase 5.

Purpose: convidados descobrem presentes no fluxo natural, com status ao vivo e sem confundir clique/salvamento com reserva.

Output: navegação combinada, GiftPreview reativo e CTA pós-save persistente.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-carta-de-vinhos/04-CONTEXT.md
@.planning/phases/04-carta-de-vinhos/04-UI-SPEC.md
@.planning/phases/04-carta-de-vinhos/04-PATTERNS.md
@.planning/phases/04-carta-de-vinhos/04-03-SUMMARY.md
@src/content/event.ts
@src/content/event.test.ts
@src/routes/Home.tsx
@src/components/rsvp/FamilyForm.tsx
</context>

## Probe assumptions resolved explicitly

- GIFT-03: o caminho adicional de descoberta é Link de rota/fragmento; o único handoff externo continua no card de `/presentes`.
- GIFT-04: preview consome a mesma query reativa e conserva o trio fixo; status gifted aparece sem trocar o produto.

## Artifacts this phase produces

| Symbol / file | Kind |
|---|---|
| `GiftPreview` | nova seção pública |
| `GIFTS_RSVP_CALLOUT` (ou campo equivalente centralizado) | copy de pós-save |
| nav entries combinadas | conteúdo compartilhado |
| `hasSavedSuccessfully` | estado local persistente da mounted family session |
| testes de nav/copy combinados | regressão das Fases 4+5 |

<threat_model>
ASVS L1; high bloqueia.

| ID | Ameaça | Controle |
|---|---|---|
| T-04-15 | CTA aparece após falso sucesso | flag muda exclusivamente no branch `saved`; matriz cobre failures/clean state. |
| T-04-16 | Preview duplica catálogo e deriva do backend | única fonte é `listFeatured`; códigos ficam canônicos no backend/testes. |
| T-04-17 | Nested links causam ativação ambígua | card preview inteiro é um Link e não contém WhatsApp/controle interno. |
| T-04-18 | Merge paralelo remove rota/seção/memórias | re-leitura live + teste de ordem combinada + diff focado. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Mesclar navegação e copy de presentes no contrato live</name>
  <read_first>
    - src/content/event.ts (RELER LIVE; identificar NAV_LINKS, RSVP_NAV_LINKS e quaisquer entradas/copy da Fase 5)
    - src/content/event.test.ts (RELER LIVE; expectativas devem representar o conjunto final, não snapshot isolado)
    - src/components/layout/Shell.tsx (um array alimenta desktop e mobile)
    - src/content/gifts.ts (copy própria de `/presentes`)
  </read_first>
  <files>src/content/event.ts, src/content/event.test.ts</files>
  <behavior>
    - Home nav contém Confirmar presença, Presentes e as entradas live existentes em ordem coerente.
    - RSVP nav contém Convite, Presentes, Programação, Local e entradas da Fase 5 que já existirem.
    - Callout centralizado tem heading/body/CTA literais da UI-SPEC e href `/presentes`.
  </behavior>
  <action>
Atualizar primeiro os testes exatos para o estado combinado encontrado no worktree. Adicionar `Presentes` a NAV_LINKS e RSVP_NAV_LINKS sem remover/reordenar arbitrariamente a entrada de Memórias ou outras mudanças da Fase 5. Centralizar o callout pós-RSVP com as três copies literais.

Não modificar Shell: seus Links já suportam rotas. Testar que links de home fragment usados fora da home permanecem absolutos e que nenhuma URL/copy é repetida em componente.
  </action>
  <verify>
    <automated>npx vitest run src/content/event.test.ts && npm run build && git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - Teste exato passa para NAV_LINKS, RSVP_NAV_LINKS e callout.
    - Entradas da Fase 5 vistas no início continuam presentes.
    - Label/href `Presentes` é literal/canônico.
    - Nenhuma mudança funcional em Shell.
  </acceptance_criteria>
  <done>A navegação e copy pública combinadas estão centralizadas e protegidas contra regressão concorrente.</done>
</task>

<task type="auto">
  <name>Task 2: Criar a dobra curada e reativa da home</name>
  <read_first>
    - src/routes/Home.tsx (RELER LIVE; preservar MemoriesSection/adicionais da Fase 5)
    - convex/wines.ts (listFeatured retorna ordem fixa)
    - src/lib/wineDeepLink.ts
    - .planning/phases/04-carta-de-vinhos/04-UI-SPEC.md (§ Home preview, Home loading/error, Responsive)
  </read_first>
  <files>src/components/gifts/GiftPreview.tsx, src/routes/Home.tsx</files>
  <action>
Criar seção cellar completa com kicker/heading/support exatos, `useQuery(api.wines.listFeatured)`, três slots loading, error alert/retry, empty/partial resiliente e CTA `Ver a carta completa`. Popular mostra exatamente os três retornados na ordem 39778/39158/39470 e nunca procura substitutos.

Cada preview card é um único `<Link>` para `/presentes#${wineDomId(code)}`, com accessible name incluindo nome, preço e status gifted quando aplicável. Não aninhar WhatsApp ou qualquer controle. Reutilizar a mídia/linguagem visual sem transformar o card em article com anchor interno.

RELER Home imediatamente antes do patch. Inserir GiftPreview depois de Countdown e antes de Local. Preservar Local→Guide→Programa→Traje e qualquer seção de Memórias inserida pela Fase 5 em sua posição live; atualizar somente o comentário de composição.
  </action>
  <verify>
    <automated>npm test && npm run build && git diff --check</automated>
    <human-check>Em 375/768/1280, conferir 1/2/3 grid, terceiro tablet sem stretch, exatamente três cards, deep links e gifted sem substituição.</human-check>
  </verify>
  <acceptance_criteria>
    - Preview está no ponto D-16 e não remove nenhuma seção live.
    - Populated possui exatamente os três códigos e um CTA geral.
    - Loading mantém três slots; error substitui só grid; CTA geral permanece.
    - Card é um único Link e accessible name inclui estado.
    - Alteração reativa de status não troca ordem/produto.
  </acceptance_criteria>
  <done>A home antecipa a adega com trio fixo, status real e deep link correto.</done>
</task>

<task type="auto">
  <name>Task 3: Exibir CTA persistente somente após save confirmado do RSVP</name>
  <read_first>
    - src/components/rsvp/FamilyForm.tsx (RELER LIVE; branch exato `case 'saved'`, feedback e estrutura do form)
    - src/content/event.ts (copy aprovada da Task 1)
    - src/lib/rsvpDraft.test.ts e convex/rsvps.test.ts (matriz de saves; não adicionar DOM framework)
    - .planning/phases/04-carta-de-vinhos/04-UI-SPEC.md (§ RSVP integration)
  </read_first>
  <files>src/components/rsvp/FamilyForm.tsx</files>
  <action>
Adicionar `hasSavedSuccessfully=false` local. Alterá-lo para true exclusivamente dentro de `case 'saved'`, depois que backend retornou sucesso. Não derivar de `dirty`, attendance counts, toast ou view restaurada. Não resetar em edição, feedback expirado ou falha posterior dentro da mesma mounted family session.

Renderizar abaixo do feedback/save action um callout cream/sand com heading/body/Link `/presentes` literais. Ele deve aparecer em partial, mixed, attending e all-no salvos; ficar ausente em initial, restored-clean, rate_limited, connection failure, invalid_update e session_expired. O callout não afirma compra/reserva.

Como não há DOM runner, manter mudanças localizadas e validar automaticamente build/testes de contratos existentes; registrar a matriz browser manual no verify sem instalar Testing Library/Playwright.
  </action>
  <verify>
    <automated>npm test && npm run build && git diff --check</automated>
    <human-check>Executar save partial, mixed, all-no e falhas: callout aparece apenas após `saved`, persiste após editar/falhar e some após remount de outra sessão.</human-check>
  </verify>
  <acceptance_criteria>
    - Única escrita `setHasSavedSuccessfully(true)` está no branch `saved`.
    - CTA não aparece no primeiro render nem por `dirty === false`.
    - Todos os attendance outcomes salvos mostram CTA.
    - CTA persiste após reset de feedback/edição na sessão montada.
    - Nenhum pacote de teste DOM foi adicionado.
  </acceptance_criteria>
  <done>O fluxo RSVP oferece presentes somente depois de confirmação real do backend, sem competir antes do save.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/content/event.test.ts && npm test && npm run build && git diff --check`
- Matriz browser de home 375/768/1280 e save outcomes.
- Diff audit dos arquivos compartilhados confirma preservação da Fase 5.
- ASVS L1 sem high.
</verification>

<success_criteria>
- D-02–D-06 e D-16 estão observáveis.
- Home e `/presentes` mostram o mesmo status reativo.
- RSVP CTA tem gate exclusivamente backend-confirmed.
- Navegação/texto longo/preview loading-error-populated cumprem as considerações covered.
- Alterações live da Fase 5 continuam intactas.
</success_criteria>
