---
phase: 02-convite-p-blico
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - vite.config.ts
  - src/content/event.ts
  - src/content/event.test.ts
  - src/lib/countdown.ts
  - src/lib/countdown.test.ts
  - src/hooks/useCountdown.ts
autonomous: true
requirements: [INVITE-01, INVITE-02, INVITE-03]

must_haves:
  truths:
    - "`npx vitest run` exits 0 from a clean checkout — the test framework that every other plan's automated verify depends on exists and runs (VALIDATION.md Wave 0 gap closed)."
    - "`getEventState(now)` returns phase `antes` strictly before `2026-10-17T00:00:00-03:00`, `hoje` between that instant and `2026-10-17T16:00:00-03:00`, `agora` from `2026-10-17T16:00:00-03:00` through `2026-10-18T05:00:00-03:00` inclusive, and `depois` after it — proven by unit tests that construct `now` from offset-qualified ISO strings, so the result is identical under any host timezone (INVITE-01)."
    - "`getEventState` takes the current instant as an injected `now: Date` parameter, so the whole state machine is testable at fixed dates without mocking global time."
    - "In the `depois` phase the parts grow without ceiling: a `now` far past `EVENT_END` yields a 4-digit `days` value that is a finite number, never `NaN`, never clamped to zero (D-10)."
    - "`pluralizeUnit(1, unit)` returns the singular form and `pluralizeUnit(0 | 2, unit)` returns the plural form for all four units (`days`/`hours`/`minutes`/`seconds`), so no renderer can print a mismatched pt-BR unit label."
    - "`src/content/event.ts` is the single source of every piece of invite copy, date, address, URL and distance for this phase — no section component hardcodes a literal string that belongs to the content module (D-03)."
    - "`PROGRAMA` holds exactly the 7 confirmed blocks in the locked order 16:00 / 17:00 / 17:45 / 19:00 / 20:30 / 00:30 / 03:00, with no hedging or tentativeness annotation (D-02)."
    - "The venue name and street name both use the corrected spelling `Matapuã`; the old project's misspelled street variant appears nowhere in `src/` (D-04)."
    - "Every external URL exported by the content module was carried from a verified source (old project's `EventSite.tsx` for the 3 original guide links and the 3 hotels; the research-verified Tripadvisor `.com` URL for Croa do Goré) — none was constructed by pattern-guessing (D-14)."
  artifacts:
    - package.json
    - vite.config.ts
    - src/content/event.ts
    - src/content/event.test.ts
    - src/lib/countdown.ts
    - src/lib/countdown.test.ts
    - src/hooks/useCountdown.ts
  key_links:
    - "`src/hooks/useCountdown.ts` -> `src/lib/countdown.ts#getEventState` — the hook owns only the 1s interval and its cleanup; all math lives in the pure function."
    - "`src/lib/countdown.ts` -> `src/content/event.ts#EVENT_DAY_START|EVENT_DATE|EVENT_END` — the three offset-qualified boundary strings are imported, never re-declared."
    - "`src/content/event.ts` -> every `src/components/invite/*` component in plans 02-03..02-06 — the content module is the upstream of the whole page."
  prohibitions:
    - id: P-04
      requirement_id: INVITE-03
      category: transparency
      statement: "MUST NOT present invented or unverified external facts as confirmed — no fabricated URL, no invented hotel or attraction name, and no distance, ranking or review count stated as fact that was not carried across from a verified source. If a value cannot be sourced, the field is omitted rather than guessed."
---

<objective>
Close the Wave 0 test-infra gap and build the two dependency-free foundations every other Phase 2 plan imports: the typed event-content module (D-03) and the timezone-safe countdown state machine (INVITE-01).

Purpose: every downstream plan's automated verify runs on `vitest`, and every downstream component reads its copy from `src/content/event.ts`. Nothing else in the phase can start until these exist.
Output: `vitest` installed and runnable, `src/content/event.ts` with the full locked copy set, `src/lib/countdown.ts` with `getEventState` + `pluralizeUnit` under test, and `src/hooks/useCountdown.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-convite-p-blico/02-CONTEXT.md
@.planning/phases/02-convite-p-blico/02-UI-SPEC.md
@.planning/phases/02-convite-p-blico/02-RESEARCH.md
@.planning/phases/02-convite-p-blico/02-PATTERNS.md
@.planning/phases/02-convite-p-blico/02-VALIDATION.md
</context>

## Artifacts this phase produces

Symbols and files created by **this plan** (new — not pre-existing codebase surface):

| Symbol / file | Kind |
|---------------|------|
| `src/content/event.ts` | new file |
| `EVENT_DATE`, `EVENT_END`, `EVENT_DAY_START` | exported const |
| `HERO`, `COUNTDOWN_COPY`, `PROGRAMA`, `DRESS`, `VENUE`, `GUIDE`, `HOTELS`, `NAV_LINKS`, `SECTION_IDS` | exported const |
| `ProgramaItem`, `DressRule`, `GalleryItem`, `GuidePlace`, `Hotel`, `NavLink` | exported type |
| `src/lib/countdown.ts` | new file (first file in `src/lib/`) |
| `getEventState`, `pluralizeUnit`, `toParts` | exported function |
| `EventPhase`, `Parts`, `CountdownUnit` | exported type |
| `src/hooks/useCountdown.ts` | new file (first file in `src/hooks/`) |
| `useCountdown` | exported hook |
| `src/content/event.test.ts`, `src/lib/countdown.test.ts` | new test files |
| `test` script in `package.json`, `test` block in `vite.config.ts` | new config surface |

## Probe Assumptions (unresolved — flagged, not dismissed)

No `02-SPEC.md` exists for this phase, so the deterministic edge probe ran against the requirement text directly. It returned **4 applicable / 0 resolved / 4 unresolved**, every row `unclassified`. Per protocol an unclassified row is never auto-backstopped and never silently dropped — it is carried here as an explicit flagged assumption. Rows owned by this plan:

| Requirement | Probe status | Flagged assumption carried into this plan |
|-------------|--------------|--------------------------------------------|
| INVITE-01 | `unclassified` / `unresolved` | The probe could not classify the countdown requirement into a shape category, so its edge set is derived from D-10 + RESEARCH Pitfalls 1–4 instead: the three phase boundaries, the no-ceiling `depois` growth, and pt-BR pluralization at 0/1/2. Assumed complete; not machine-confirmed. |
| INVITE-02 | `unclassified` / `unresolved` | Edge set for the programa/dress content is assumed to be "exactly 7 locked blocks, exactly 2 gallery figures, no partial record" per D-02 and the UI-SPEC `partial` row. Not machine-confirmed. |
| INVITE-03 | `unclassified` / `unresolved` | Edge set for local/guia/hotéis is assumed to be "3 or 4 guide cards, 3 hotels, all URLs verified-source" per D-13/D-14. Not machine-confirmed. |

INVITE-04's unresolved row is carried in `02-07-PLAN.md`.

<tasks>

<task type="auto">
  <name>Task 1: Install and wire vitest (Wave 0 infra gate)</name>
  <files>package.json, package-lock.json, vite.config.ts</files>
  <read_first>
    - package.json (current dependency set and script list — there is no `test` script today)
    - vite.config.ts (current plugin config — react + tailwindcss)
    - .planning/phases/02-convite-p-blico/02-VALIDATION.md (§ Wave 0 Requirements — the exact gap this task closes)
    - .planning/phases/02-convite-p-blico/02-RESEARCH.md (§ Standard Stack "Supporting (new this phase)" and § Package Legitimacy Audit — vitest 4.1.10 is verified and approved, peerDependency on vite ^8.0.0 satisfied by the installed vite 8.1.5)
  </read_first>
  <action>
Install `vitest` at exactly version `4.1.10` as a devDependency using `npm install --save-exact --save-dev vitest@4.1.10`. The `--save-exact` flag is required: this repo pins every dependency to an exact version (see the existing `dependencies` block, all bare version numbers with no caret). Do not install `@testing-library/react`, `jsdom`, `happy-dom`, `@vitest/ui` or `@vitest/coverage-*` — this phase's entire test surface is pure functions over `Date` and string inputs, so the default node environment is sufficient, and RESEARCH.md's Package Legitimacy Audit approves exactly one new package.

Configure the test runner by adding a `test` block to the existing `defineConfig` call in `vite.config.ts` rather than creating a separate `vitest.config.ts` — one config file is simpler and the vite config already exists. Inside `test`, set `environment` to `node`, `globals` to `false` (tests import `describe`/`it`/`expect` explicitly from `vitest`, which keeps type resolution working without adding `vitest/globals` to the tsconfig `types` array), and `include` to match `src/**/*.test.ts`. Because `vite.config.ts` is typed against vite's own `UserConfig`, add the vitest config reference so TypeScript accepts the `test` key — import `defineConfig` from `vitest/config` instead of from `vite` (vitest re-exports a superset of vite's `defineConfig` that carries the `test` key type).

Add a `"test"` script to `package.json` set to `vitest run` — the non-watch form. Never add a script that invokes bare `vitest`, which enters watch mode and hangs a non-interactive agent run.

Confirm `npm run build` still succeeds after the config change: `tsconfig.node.json` type-checks `vite.config.ts`, so a bad import path there breaks the production build, not just the tests.
  </action>
  <acceptance_criteria>
    - `package.json` `devDependencies` contains a `"vitest"` key whose value is the exact string `4.1.10` (no caret, no tilde).
    - `package.json` `scripts` contains a `"test"` key whose value is exactly `vitest run` — `node -e 'process.exit(require("./package.json").scripts.test === "vitest run" ? 0 : 1)'` exits 0. This is an equality check, not a substring grep: the watch-mode form is a strict prefix of the correct value, so a substring gate cannot distinguish them.
    - `vite.config.ts` imports `defineConfig` from `vitest/config`; `grep -c "from 'vitest/config'" vite.config.ts` returns `1`.
    - `vite.config.ts` contains a `test:` config key with `environment: 'node'`.
    - `npx vitest run` exits 0 (an empty or passing suite both exit 0; a config error exits non-zero).
    - `npm run build` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run; npm run build</automated>
  </verify>
  <done>vitest 4.1.10 is an exact-pinned devDependency, `npm test` and `npx vitest run` both run the suite once and exit, and the production build still type-checks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author src/content/event.ts — the single source of invite content</name>
  <files>src/content/event.ts, src/content/event.test.ts</files>
  <read_first>
    - .planning/phases/02-convite-p-blico/02-UI-SPEC.md (§ Copywriting Contract — the authoritative, final copy for every string in this task; it supersedes the old project wherever they differ)
    - .planning/phases/02-convite-p-blico/02-CONTEXT.md (D-02 programa, D-03 content module, D-04 Matapuã spelling, D-13 guide, D-14 hotels)
    - /Users/allanmesquitabrito/Documents/Site Sol 40 anos/sol-40-integrado/app/convite/EventSite.tsx (lines 41-46 for hotel records; lines 495-503 for the 3 original Tripadvisor URLs, the map embed src and the route URL; lines 473-493 for programa and dress copy — this file is the source of truth for URLs, which must be copied character-for-character, never retyped from memory)
    - /Users/allanmesquitabrito/Documents/Site Sol 40 anos/sol-40-integrado/lib/event.ts (EVENT_DATE / EVENT_END offset-qualified string format)
    - src/index.css (the `@theme` token names available to downstream components — informs nothing about content, but confirms no color/typography value belongs in this module)
  </read_first>
  <behavior>
    - `PROGRAMA` has length 7 and its `time` values, read in array order, are "16:00", "17:00", "17:45", "19:00", "20:30", "00:30", "03:00".
    - `GUIDE` has length 4 and every entry's `url` starts with "https://".
    - `HOTELS` has length 3 and every entry's `url` starts with "https://".
    - `NAV_LINKS` has length 3 and its `href` values are "#aracaju", "#programacao", "#traje" in that order.
    - `DRESS.gallery` has length 2 and every figure carries a non-empty `alt`, a positive `width` and a positive `height`.
    - `EVENT_DATE`, `EVENT_END` and `EVENT_DAY_START` each end with the literal offset suffix "-03:00".
    - `new Date(EVENT_DAY_START) < new Date(EVENT_DATE) < new Date(EVENT_END)` holds as instants.
    - No exported string contains the old project's misspelled street variant.
  </behavior>
  <action>
Create `src/content/event.ts` as plain TypeScript literal exports — no runtime validation library, no zod, no new dependency. Follow this repo's convention of exporting typed `const`s (the structural precedent is the `@theme` block in `src/index.css`: constants centralized in one place rather than scattered across components).

Export the three offset-qualified boundary strings: `EVENT_DATE` = "2026-10-17T16:00:00-03:00", `EVENT_END` = "2026-10-18T05:00:00-03:00", and `EVENT_DAY_START` = "2026-10-17T00:00:00-03:00". `EVENT_DAY_START` is new this phase — D-10 counts down to midnight of event day, not to the 16:00 arrival time, so it is a distinct constant from `EVENT_DATE`.

Export `SECTION_IDS` mapping the four anchor targets used across the page: `hero` = "inicio", `local` = "aracaju", `programa` = "programacao", `traje` = "traje". Every downstream component derives its `id` attribute and every nav `href` derives its fragment from this object, so a rename can never desynchronize an anchor from its target.

Export `NAV_LINKS` as exactly three records of `{ label, href }`: "Local" -> "#aracaju", "Programação" -> "#programacao", "Traje" -> "#traje", in that order (UI-SPEC "Topbar nav" row: page order per D-05, and nothing pointing at RSVP, presentes or memórias).

Export `HERO` carrying the hero text layers: `eyebrow` "17 OUT · 16H · ARACAJU", `title` "Sol", `titleSub` "40 anos", `taglineLead` "Viva o seu melhor " and `taglineEm` "pôr do sol." (split so the component can wrap the emphasis in an `em` element rather than parsing markup out of a string), `metaLeft` "MATAPUÃ EVENTOS", `metaRight` "ARACAJU · SERGIPE", `ctaLabel` "Ver programação ↓" and `ctaHref` "#programacao". The hero CTA is deliberately the programação scroll link this phase, not an RSVP link — RSVP has no destination until Phase 3.

Export `COUNTDOWN_COPY` keyed by the four phase names `antes` / `hoje` / `agora` / `depois`, each carrying the kicker, heading and rail label for that state (UI-SPEC "Countdown" rows). For `antes`: kicker "ATÉ A GENTE SE ENCONTRAR", headingLead "O tempo já está ", headingEm "dourando.", railLabel "Faltam", and `showTiles` true. For `hoje`: kicker "O DIA CHEGOU", heading "É HOJE", railLabel "É HOJE", `showTiles` false. For `agora`: kicker "É AGORA", heading "TÁ ROLANDO", sub "A festa é agora.", railLabel "TÁ ROLANDO", `showTiles` false. For `depois`: kicker "DEPOIS DO PÔR DO SOL", heading "JÁ QUE VOCÊ NÃO FOI, PERDEU!", railLabel "Já foi", `showTiles` true. The `depois` heading is literal owner-dictated copy — ship it exactly as written, all caps, exclamation included; do not soften, rephrase or add an explanatory sentence around it.

Export `PROGRAMA: ProgramaItem[]` where `ProgramaItem` is `{ time: string; title: string; description: string }`, with exactly the seven confirmed blocks in order: 16:00 "Chegadas & abraços" / "Drinks frescos, música e o sol descendo devagar."; 17:00 "Banda Nona" / "Curta um pagode ao pôr do sol."; 17:45 "O brinde da Sol" / "Um instante para celebrar as primeiras quarenta voltas."; 19:00 "Jantar sob as luzes" / "Sabores sergipanos e mesa boa para ficar."; 20:30 "Dança com Alma Gêmea" / "Continue a festa no embalo da banda."; 00:30 "A festa não para: Latino!" / "Fôlego novo madrugada adentro."; 03:00 "Tudo que é bom tem que acabar 🥺" / "Até o próximo pôr do sol.". Also export `PROGRAMA_HEADING` "Uma tarde que vira noite, sem pressa de acabar." and `PROGRAMA_KICKER` "17 OUT". The schedule is confirmed — add no annotation suggesting it may still change (D-02).

Export `DRESS` carrying: `kicker` "TRAJE OBRIGATÓRIO"; `headingLead` "Todo mundo de " and `headingEm` "branco ou off-white."; `intro` "A festa começa com luz natural e segue pelo gramado. A ideia é chegar leve, confortável e inteiro na paleta da noite."; `rules` as two `DressRule` records `{ audience, text }` — "Homens" / "Camisa de linho ou algodão com bermuda ou calça de sarja branca." and "Mulheres" / "Vestidos, shorts, saias ou blusas em branco e off-white."; `callout` `{ kicker: "NO GRAMADO", lead: "Escolha conforto de verdade.", text: "Sandália baixa, tênis ou sapato confortável funcionam melhor. Evite salto fino." }`; and `gallery` as two `GalleryItem` records `{ src, alt, caption, width, height }`. Gallery item one: src "/dress-code-men.jpg", alt "Homem com camisa e bermuda brancas, usando sandália baixa em um gramado", caption "Leve, fresco e confortável". Gallery item two: src "/dress-code-women.jpg", alt "Duas mulheres com vestido branco e conjunto de shorts e blusa branca, usando calçados baixos no gramado", caption "Branco ou off-white, do seu jeito". The `width`/`height` values must be the real pixel dimensions of the compressed files produced by plan 02-02. Use men 1120x1400 and women 895x1400: these are not estimates — plan 02-02's compression recipe was executed against the real source files during planning and produced exactly those dimensions (sources are 1122x1402 and 1003x1568, resized to a 1400px long edge). If the files already exist, confirm with `sips -g pixelWidth -g pixelHeight public/dress-code-men.jpg` and correct these literals if 02-02's actual output differs. These numbers exist so the browser reserves the grid cell before decode; a wrong ratio reintroduces the layout shift they are there to prevent.

Export `VENUE` `{ kicker: "O ENCONTRO", name: "Matapuã Eventos", addressLine1: "Estrada Matapuã, 1213 · Mosqueiro", addressLine2: "Aracaju · Sergipe", mapEmbedSrc, routeUrl, mapCtaLabel: "Ver mapa", routeCtaLabel: "Abrir rota ↗" }`. Copy `mapEmbedSrc` and `routeUrl` character-for-character from the old project's map markup — the embed uses the no-API-key `output=embed` form with the percent-encoded venue query, and the route URL is the Google Maps search-API form with the same encoded query. Note the deliberate correction: the old project's rendered address used a misspelled street name while its title used the correct one; both fields here use the corrected spelling with the tilde-a in both the venue name and the street name (D-04).

Export `GUIDE: GuidePlace[]` where `GuidePlace` is `{ kicker: string; name: string; description: string; url: string }`, shipping **four** cards. The first three are the originals, with their `url` values copied verbatim from the old project (all three are `tripadvisor.com.br` Attraction_Review paths): "VER" / "Museu da Gente Sergipana" / "Cultura sergipana num casarão à beira do rio · aprox. 28 km da festa. Horários e avaliações no Tripadvisor ↗"; "PROVAR" / "Passarela do Caranguejo" / "Corredor gastronômico na Orla de Atalaia · aprox. 17 km da festa. Avaliações no Tripadvisor ↗"; "RESPIRAR" / "Orla de Atalaia" / "Mar, ciclovia, lagos e pôr do sol · aprox. 18 km da festa. Dicas no Tripadvisor ↗". The fourth card closes D-13's "ampliar com os mais bem avaliados": "NAVEGAR" / "Croa do Goré" / "Passeio de barco pelo rio Vaza Barris, entre mangue e ilhas · pra quem tiver um dia a mais. Avaliações no Tripadvisor ↗", with the url `https://www.tripadvisor.com/Attraction_Review-g303638-d4007022-Reviews-Croa_do_Gore-Aracaju_State_of_Sergipe.html`. That is deliberately the `.com` host while the other three use `.com.br`: research verified the `.com` variant resolves and did **not** verify a `.com.br` equivalent, and shipping a verified URL that breaks host convention is correct where shipping a convention-matching URL nobody confirmed is not. Add a short source comment on the fourth entry recording that the host divergence is intentional. Also export `GUIDE_KICKER` "PRA QUEM VEM DE FORA", `GUIDE_HEADING_LEAD` "Um fim de semana " and `GUIDE_HEADING_EM` "com gosto de Aracaju.".

Export `HOTELS: Hotel[]` where `Hotel` is `{ name: string; distance: string; url: string }`, shipping exactly the three the owners named, with names, distance strings and URLs copied verbatim from the old project's hotel array: "Aruanã Eco Praia Hotel" / "aprox. 12 km do local · opção mais próxima" / `https://aruanahotel.com.br/`; "Quality Hotel Aracaju" / "aprox. 21 km do local · Coroa do Meio" / `https://letsatlantica.com.br/hotel/quality-hotel-aracaju`; "Celi Connect" / "aprox. 18 km do local" / `https://celihotel.com.br/celi-connect/`. The old array also carried a fourth hotel the owners did not name — do not include it. Also export `HOTELS_HEADING` "ONDE FICAR · DISTÂNCIAS ATÉ A FESTA".

Write `src/content/event.test.ts` asserting the behaviors listed above. These are shape and count assertions over literals, not snapshots — their job is to make a silent content regression (a dropped programa block, a truncated guide array, a nav link quietly pointed at a section that does not exist) fail the build rather than reach the page.
  </action>
  <acceptance_criteria>
    - `npx vitest run src/content/event.test.ts` exits 0.
    - `grep -c 'export const PROGRAMA' src/content/event.ts` returns `1`.
    - `grep -o '"1[67]:00"\|"17:45"\|"19:00"\|"20:30"\|"00:30"\|"03:00"' src/content/event.ts | wc -l` returns at least `7`.
    - `grep -c 'https://' src/content/event.ts` returns at least `9` (4 guide + 3 hotel + map embed + route).
    - The old project's misspelled street name does not survive the port: `! grep -rq 'Matapoã' src/` succeeds. Assert it in exactly that form — do not build a `grep -rc ... | wc -l | grep -qx 0` pipeline, because macOS `wc -l` pads its output with leading spaces and the anchored `grep -qx 0` can then never match, making the gate report failure even when no file contains the misspelling.
    - `grep -ci 'provis' src/content/event.ts` returns `0` (the confirmed schedule carries no tentativeness annotation).
    - `grep -c '\-03:00' src/content/event.ts` returns at least `3`.
    - `grep -c 'tripadvisor' src/content/event.ts` returns `4`.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/content/event.test.ts && npm run build && ! grep -rq 'Matapoã' src/ && echo SPELLING_OK</automated>
  </verify>
  <done>`src/content/event.ts` exports every date, copy string, URL and image dimension the invite page needs, its shape is under test, the corrected Matapuã spelling holds throughout `src/`, and the build type-checks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Countdown state machine, pt-BR pluralizer and the ticking hook</name>
  <files>src/lib/countdown.ts, src/lib/countdown.test.ts, src/hooks/useCountdown.ts</files>
  <read_first>
    - src/content/event.ts (created in Task 2 — the three boundary constants this module imports)
    - .planning/phases/02-convite-p-blico/02-RESEARCH.md (§ Architecture Patterns Pattern 1 and Pattern 2 — the exact function and hook shapes to implement; § Common Pitfalls 1-4)
    - .planning/phases/02-convite-p-blico/02-CONTEXT.md (D-10 — the four states and their exact boundaries)
    - .planning/phases/02-convite-p-blico/02-VALIDATION.md (§ Per-Task Verification Map — the four named test filters `offset`, `phase`, `depois`, `pluralize` that downstream verification runs against this file)
    - /Users/allanmesquitabrito/Documents/Site Sol 40 anos/sol-40-integrado/app/convite/EventSite.tsx (lines 119-134 — the old `useCountdown`, which already cleans up its interval; carry that shape forward, do not regress it)
  </read_first>
  <behavior>
    - Test named "offset": two `Date` objects built from the same offset-qualified string are equal instants, and `new Date(EVENT_DATE).getTime()` equals the UTC instant `2026-10-17T19:00:00Z` — proving the target does not drift with the host timezone.
    - Test named "phase": `getEventState` returns `antes` one millisecond before `EVENT_DAY_START`, `hoje` at `EVENT_DAY_START` and one millisecond before `EVENT_DATE`, `agora` at `EVENT_DATE` and at `EVENT_END`, and `depois` one millisecond after `EVENT_END`.
    - Test named "depois": for a `now` roughly 27 years past `EVENT_END`, `parts.days` is greater than 9000, is a finite integer, and every part is a non-negative number (never `NaN`).
    - Test named "pluralize": `pluralizeUnit(1, u)` is singular and `pluralizeUnit(0, u)` and `pluralizeUnit(2, u)` are plural, for each of `days`, `hours`, `minutes`, `seconds` — i.e. "1 dia" but "0 dias" and "2 dias".
    - `toParts` of a zero or negative distance yields all-zero parts, never a negative number.
  </behavior>
  <action>
Create `src/lib/countdown.ts` — the first file in a new `src/lib/` directory.

Export the type `EventPhase` as the union of the four literal state names `"antes" | "hoje" | "agora" | "depois"`, the type `Parts` as `{ days: number; hours: number; minutes: number; seconds: number }`, and `CountdownUnit` as `keyof Parts`.

Export `getEventState(now: Date): { phase: EventPhase; parts: Parts }`. The current instant arrives as the `now` parameter and the function reads the clock from nowhere else — that injection is what makes the whole state machine testable at fixed dates without stubbing global time, and it is the single most important structural constraint in this file. Inside, parse the three boundary strings imported from `src/content/event.ts` into millisecond instants and compare against `now.getTime()`: before `EVENT_DAY_START` the phase is `antes` with parts counting down the remaining distance to `EVENT_DAY_START`; from `EVENT_DAY_START` up to but not including `EVENT_DATE` the phase is `hoje` with zeroed parts; from `EVENT_DATE` through `EVENT_END` inclusive the phase is `agora` with zeroed parts; strictly after `EVENT_END` the phase is `depois` with parts counting **up** from `EVENT_END`. The `depois` branch has no ceiling by design (D-10) — do not clamp, cap or modulo the day count.

Export `toParts(distanceMs: number): Parts`, flooring the distance into days / hours-mod-24 / minutes-mod-60 / seconds-mod-60, and flooring the input at zero first so a negative distance yields all zeros rather than negative digits. This is the single function that converts milliseconds into display parts; both the full tiles and the compact rail read its output.

Export `pluralizeUnit(count: number, unit: CountdownUnit): string` backed by a module-level record mapping each unit key to its singular/plural pt-BR pair — days to "dia"/"dias", hours to "hora"/"horas", minutes to "minuto"/"minutos", seconds to "segundo"/"segundos" — returning the singular only when `count` is exactly 1. Both countdown renderers in plan 02-04 must call this instead of hardcoding a label, because every countdown passes through 1 for every unit on its way down and two independent copies of the rule will drift.

Write `src/lib/countdown.test.ts` covering the behaviors above. The test names must literally contain the substrings `offset`, `phase`, `depois` and `pluralize`, because `02-VALIDATION.md` binds its per-requirement verification rows to `vitest run ... -t "<substring>"` filters on exactly those words. Build every `now` value in the tests from an offset-qualified ISO string literal so the assertions are timezone-independent, and never construct a `Date` from separate numeric year/month/day arguments, which resolves against the runner's local timezone and would make the suite pass or fail depending on the machine.

Create `src/hooks/useCountdown.ts` — the first file in a new `src/hooks/` directory. Export `useCountdown()`, which seeds React state via a lazy initializer calling `getEventState(new Date())` so the very first paint already shows correct digits (there is no skeleton or loading state for the countdown), then in a single `useEffect` with an empty dependency array schedules a 1000ms `window.setInterval` that recomputes the state, and returns `() => window.clearInterval(timer)` from that same effect. The cleanup must be returned from the same effect that created the interval: React 19 StrictMode double-invokes effects in development, and a missing cleanup stacks two intervals so the seconds visibly tick twice per second. The hook owns scheduling only — it performs no date math of its own.
  </action>
  <acceptance_criteria>
    - `npx vitest run src/lib/countdown.test.ts` exits 0.
    - `npx vitest run src/lib/countdown.test.ts -t "offset"` exits 0 and reports at least 1 passing test.
    - `npx vitest run src/lib/countdown.test.ts -t "phase"` exits 0 and reports at least 1 passing test.
    - `npx vitest run src/lib/countdown.test.ts -t "depois"` exits 0 and reports at least 1 passing test.
    - `npx vitest run src/lib/countdown.test.ts -t "pluralize"` exits 0 and reports at least 1 passing test.
    - `grep -c 'export function getEventState(now: Date' src/lib/countdown.ts` returns `1` — the injected-clock signature is present verbatim.
    - `grep -c 'export function pluralizeUnit' src/lib/countdown.ts` returns `1`.
    - `grep -c 'clearInterval' src/hooks/useCountdown.ts` returns at least `1`.
    - `grep -c 'setInterval' src/hooks/useCountdown.ts` returns exactly `1`.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/lib/countdown.test.ts && npx vitest run src/lib/countdown.test.ts -t "pluralize" && npm run build</automated>
  </verify>
  <done>`getEventState` resolves all four D-10 states at their exact boundaries from an injected clock, `depois` grows past 9000 days without overflow or NaN, `pluralizeUnit` is correct at 0/1/2 for all four units, and `useCountdown` ticks once per second with a guaranteed cleanup.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry -> local devDependencies | A new package (`vitest`) is fetched and executed as part of the build/test toolchain. |
| Static content module -> rendered DOM | Strings authored in `src/content/event.ts` are rendered by React components in later plans; the module is the only injection surface this phase introduces. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-SC | Tampering | `npm install vitest@4.1.10` | high | mitigate | RESEARCH.md § Package Legitimacy Audit verified `vitest` first-party against the npm registry (multi-year, tens-of-millions weekly downloads, source repo `github.com/vitest-dev/vitest`) with verdict `OK` / Approved — no `[ASSUMED]`, `[SUS]` or `[SLOP]` package is introduced, so no blocking-human legitimacy checkpoint is required. Install with `--save-exact` so the resolved version is pinned in `package.json` and `package-lock.json`, preventing a silent transitive upgrade on a later `npm install`. Dev-only: zero runtime/bundle exposure to guests. |
| T-02-01-A | Tampering / Injection | `src/content/event.ts` strings rendered into JSX | low | accept | Every value is a hardcoded TypeScript literal authored by the developer, not user input or fetched data, and JSX auto-escapes all interpolated text. Accepted for this phase. The register row is retained so a future phase that makes any part of this content editable (owner-editable event details) re-evaluates it rather than inheriting the acceptance silently. Constraint carried forward: no `dangerouslySetInnerHTML` anywhere in this phase. |
| T-02-01-B | Information Disclosure | External URLs authored into `GUIDE` / `HOTELS` / `VENUE` | low | mitigate | URLs are authored here but consumed in plan 02-06, which is where `target="_blank"` pairs with `rel="noreferrer"`. This plan's contribution is authoring only verified-source URLs (P-04), so no attacker-chosen destination can be introduced by a typo-squatted guess. |
</threat_model>

<verification>
- `npx vitest run` (full suite) exits 0 with at least the two test files present and passing.
- `npm run build` exits 0 — TypeScript compiles the new `src/content`, `src/lib` and `src/hooks` modules.
- `! grep -rq 'Matapoã' src/` succeeds — the misspelled street name appears in no file under `src/`.
- `git diff --stat` shows no changes outside the files listed in `files_modified`.
</verification>

<success_criteria>
- `vitest@4.1.10` is an exact-pinned devDependency, `npm test` runs the suite once (no watch mode), and `npx vitest run` exits 0.
- `src/content/event.ts` exports the full locked content set: 3 boundary dates, hero copy, 4 countdown-state copies, 7 programa blocks, dress copy plus 2 dimensioned gallery figures, venue with corrected spelling and both map URLs, 4 guide cards, 3 hotels, 3 nav links.
- `getEventState` returns the correct one of four phases at each of the three boundaries, computed from offset-qualified strings so the result is host-timezone independent (INVITE-01).
- `pluralizeUnit` is singular at exactly 1 and plural at 0 and 2 for all four units.
- `useCountdown` ticks once per second and clears its interval on unmount.
</success_criteria>

<output>
Create `.planning/phases/02-convite-p-blico/02-01-SUMMARY.md` when done.
</output>
