---
phase: 02-convite-p-blico
plan: 05
type: execute
wave: 2
depends_on: [02-01, 02-02]
files_modified:
  - src/components/invite/ProgramaSection.tsx
  - src/components/invite/DressCodeSection.tsx
autonomous: true
requirements: [INVITE-02]

must_haves:
  truths:
    - "The programa section renders all seven confirmed schedule blocks in order, each with its time, title and description, under the locked kicker and heading (INVITE-02, D-02)."
    - "The traje section renders the intro, both audience rule blocks, the grass callout and the two-photo reference gallery with their original captions (INVITE-02)."
    - "Neither section has an empty state: both read fixed-length literal arrays from `src/content/event.ts`, bundled at build time with no fetch and no user input, and the exported types are non-optional arrays — a zero-item render would be a build error, not a runtime state (UI Considerations, empty | E4, E5)."
    - "Neither section has a loading state for its text: the content is painted with the first HTML/JS chunk, so no per-section skeleton exists (UI Considerations, loading | E4)."
    - "Both dress-code photos are local `public/` assets carrying lazy loading and async decoding, and each image element declares explicit width and height so the grid cell is reserved before decode — lazy loading cannot shift the section (UI Considerations, loading | E5)."
    - "Neither section has an error state: the programa has no network dependency at all, and a failed photo decode falls back to its descriptive pt-BR alt text, with a missing file being a build failure rather than a runtime broken-image state (UI Considerations, error | E4, E5)."
    - "Typical volume is the only volume: seven programa blocks, two traje photos, both rule blocks and one callout, all fixed by D-02 and the copy contract (UI Considerations, populated | E4, E5)."
    - "Neither section can render partially: every record in the content module is a complete literal with all required fields, and the TypeScript types make a missing field a build error rather than a runtime half-record (UI Considerations, partial | E4, E5)."
    - "Both sections wrap rather than overflow at every width: the fluid clamp type scale plus a max-width content column mean no fixed-width container exists that content can exceed (UI Considerations, overflow | E4, E5 and long-text | E4, E5)."
    - "No singular/plural copy branch is reachable in either section — the counts are fixed and hardcoded (seven blocks, two photos) and the spacing is authored against the real count (UI Considerations, zero-one-many | E4, E5)."
  artifacts:
    - src/components/invite/ProgramaSection.tsx
    - src/components/invite/DressCodeSection.tsx
  key_links:
    - "`ProgramaSection.tsx` -> `src/content/event.ts#PROGRAMA`, `#PROGRAMA_KICKER`, `#PROGRAMA_HEADING`, `#SECTION_IDS.programa` — the section id is the scroll target of both the hero CTA and the topbar Programação link."
    - "`DressCodeSection.tsx` -> `src/content/event.ts#DRESS` and `#SECTION_IDS.traje` — the section id is the scroll target of the topbar Traje link."
    - "`DressCodeSection.tsx` -> `public/dress-code-men.jpg` and `public/dress-code-women.jpg` (ported in plan 02-02) — the declared width/height must match the real files or the reserved cell is wrong."
  prohibitions:
    - id: P-03
      requirement_id: INVITE-02
      category: values
      statement: "MUST NOT turn the dress-code section into body-policing or exclusionary instruction. The locked copy is an invitation to comfort, not a rule about bodies: no added text prescribing body types or sizes, no shaming of a clothing choice, and the two audience blocks stay reference suggestions rather than a mandatory assignment anyone must sort themselves into."
---

<objective>
Build the two content sections: the seven-block programa and the traje/dress-code block with its rules, grass callout and two-photo reference gallery.

Purpose: these are the two sections INVITE-02 names, and together they are what a guest actually plans their evening and their outfit around.
Output: `ProgramaSection.tsx` and `DressCodeSection.tsx` under `src/components/invite/`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-convite-p-blico/02-CONTEXT.md
@.planning/phases/02-convite-p-blico/02-UI-SPEC.md
@.planning/phases/02-convite-p-blico/02-PATTERNS.md
</context>

## Artifacts this phase produces

Symbols and files created by **this plan**:

| Symbol / file | Kind |
|---------------|------|
| `src/components/invite/ProgramaSection.tsx` | new file |
| `ProgramaSection` | exported component |
| `src/components/invite/DressCodeSection.tsx` | new file |
| `DressCodeSection` | exported component |

## Probe Assumptions (unresolved — flagged, not dismissed)

| Requirement | Probe status | Flagged assumption carried into this plan |
|-------------|--------------|--------------------------------------------|
| INVITE-02 | `unclassified` / `unresolved` | The probe could not classify the programa/traje requirement, so the edge set here is taken from UI-SPEC's E4 and E5 rows, all of which resolved `covered` because the content is a fixed-length bundled literal and the only media is two local assets with declared dimensions. The assumption is that "fixed count, complete record, local asset, alt-text fallback" exhausts the edge surface for these two sections. Not machine-confirmed.

<tasks>

<task type="auto">
  <name>Task 1: ProgramaSection — the seven-block schedule</name>
  <files>src/components/invite/ProgramaSection.tsx</files>
  <read_first>
    - src/content/event.ts (created in plan 02-01 — `PROGRAMA`, `PROGRAMA_KICKER`, `PROGRAMA_HEADING`, `SECTION_IDS`, and the `ProgramaItem` type)
    - src/routes/Home.tsx (lines 32-50 — this repo's established kicker + heading idiom and its array-to-grid `.map()` pattern, the only existing list-render precedent in the codebase)
    - src/index.css (the `@theme` tokens: `--text-heading`, `--text-caption`, `--tracking-label`, `--tracking-display`, the sunset palette)
    - .planning/phases/02-convite-p-blico/02-UI-SPEC.md (§ Copywriting Contract, the "Programa" rows; § Color — the programa is one of the light cream sections; § Spacing Scale — the clamp section-padding exception)
    - /Users/allanmesquitabrito/Documents/Site Sol 40 anos/sol-40-integrado/app/convite/EventSite.tsx (line 473 — the old programa markup: an ordered list of time-plus-detail rows, and the small sun-and-date art block beside it)
  </read_first>
  <action>
Create `src/components/invite/ProgramaSection.tsx` exporting a `ProgramaSection` component with no props.

Render a `section` element whose `id` comes from `SECTION_IDS.programa` — this is the scroll target for both the hero CTA and the topbar Programação link, so the id must be derived from the shared constant rather than typed in as a string. Give it `scroll-mt` sized to clear the sticky topbar (roughly the 72px header plus the 48px rail) so an in-page jump does not park the heading underneath the fixed chrome.

It is a light section: cream background, ink text, with the responsive clamp section padding declared in the UI-SPEC as the deliberate divergence from the fixed spacing tokens — roughly `clamp(80px, 10vw, 160px)` vertically and `clamp(24px, 7vw, 110px)` horizontally as Tailwind arbitrary values. Constrain the content to a centred max-width column.

Above the list, render the kicker from `PROGRAMA_KICKER` in the label role (caption size, bold, uppercase, `tracking-label`) and the heading from `PROGRAMA_HEADING` as an `h2` in the heading role (`font-serif`, `text-heading`, `tracking-display`). Port the old section's small decorative touch beside the heading — a soft sun disc with the day and month set across it — as CSS and text rather than an image, and mark it `aria-hidden="true"`; it repeats information the heading already carries.

Render the schedule as an ordered list, mapping over `PROGRAMA`. Each item renders its time inside a `time` element, then its title and its description. Use the item's time value as the React key. No time, title or description text may appear as a literal in this file: the schedule is confirmed and lives in the content module (D-03), and a second copy here is exactly how a later owner edit silently fails to reach the page. Do not add any annotation suggesting the schedule may still change — it is confirmed (D-02).

Layout the list mobile-first: on narrow screens each row stacks the time above its title and description; from the small breakpoint up, the time sits in a fixed-width left column with the detail beside it, so the times align into a readable spine. Separate rows with a hairline border in the `line` token rather than heavy card chrome — the old section was a quiet list, not a grid of cards. Let long titles wrap; do not truncate or ellipsize, since one of the seven titles carries an emoji and another is a full sentence.
  </action>
  <acceptance_criteria>
    - `src/components/invite/ProgramaSection.tsx` exists and exports `ProgramaSection`.
    - It maps the content module array: `grep -c 'PROGRAMA' src/components/invite/ProgramaSection.tsx` returns at least `3` (import, kicker/heading, map) and `grep -c '\.map(' src/components/invite/ProgramaSection.tsx` returns at least `1`.
    - No schedule value is duplicated as a literal: `grep -cE '[0-9]{2}:[0-9]{2}' src/components/invite/ProgramaSection.tsx` returns `0`.
    - The section id comes from the shared constant: `grep -c 'SECTION_IDS.programa' src/components/invite/ProgramaSection.tsx` returns `1` and `grep -c 'id="programacao"' src/components/invite/ProgramaSection.tsx` returns `0`.
    - `grep -ci 'provis' src/components/invite/ProgramaSection.tsx` returns `0`.
    - `grep -c '<time' src/components/invite/ProgramaSection.tsx` returns at least `1`.
    - `grep -c 'scroll-mt' src/components/invite/ProgramaSection.tsx` returns at least `1`.
    - `npm run build` exits 0 and `npx vitest run` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm run build && npx vitest run && test "$(grep -cE '[0-9]{2}:[0-9]{2}' src/components/invite/ProgramaSection.tsx | tr -d ' ')" = "0" && grep -q 'SECTION_IDS.programa' src/components/invite/ProgramaSection.tsx && echo PROGRAMA_OK</automated>
    <human-check>At 360px and 1440px confirm all seven blocks render in order, the times align into a column from the small breakpoint up, the emoji title and the longest description wrap without horizontal scroll, and clicking the hero CTA lands the heading below the sticky topbar rather than under it.</human-check>
  </verify>
  <done>The programa section renders all seven confirmed blocks from the content module in order, with no schedule value duplicated in the component and no tentativeness annotation.</done>
</task>

<task type="auto">
  <name>Task 2: DressCodeSection — rules, grass callout and the two-photo gallery</name>
  <files>src/components/invite/DressCodeSection.tsx</files>
  <read_first>
    - src/content/event.ts (created in plan 02-01 — the `DRESS` object: kicker, split heading, intro, two rule records, callout and two dimensioned gallery items)
    - public/dress-code-men.jpg and public/dress-code-women.jpg (ported in plan 02-02 — confirm the real pixel dimensions with `sips -g pixelWidth -g pixelHeight` and confirm they match the width/height literals in the content module; if they differ, correct the content module rather than the markup)
    - src/components/ui/Card.tsx (the cream card surface with offset shadow — the base for the rule blocks and the callout, and this project's established surface convention)
    - src/routes/Home.tsx (lines 61-74 — the two-column card grid pattern to follow for the rules and the gallery)
    - .planning/phases/02-convite-p-blico/02-UI-SPEC.md (§ Copywriting Contract, the four "Traje" rows; § Typography — the audience labels render at serif regular in the subheading role, not at a third font weight; § Color — the traje section is one of the dark plum sections and the callout is the one place the coral accent is used as a background)
    - /Users/allanmesquitabrito/Documents/Site Sol 40 anos/sol-40-integrado/app/convite/EventSite.tsx (lines 478-493 — the old traje markup: copy block on one side, figure gallery on the other, with the alt text this phase reuses verbatim)
  </read_first>
  <action>
Create `src/components/invite/DressCodeSection.tsx` exporting a `DressCodeSection` component with no props.

Render a `section` whose `id` comes from `SECTION_IDS.traje` — the topbar Traje link targets it — with the same `scroll-mt` offset for the sticky chrome as the programa section. It is one of the dark full-bleed sections: plum background, cream text, same responsive clamp section padding.

Lay it out as two columns from the medium breakpoint up and stacked on mobile: the copy block on one side, the photo gallery on the other.

The copy block renders, in order: the kicker from `DRESS.kicker` in the label role; an `h2` combining `DRESS.headingLead` with `DRESS.headingEm` wrapped in an `em` carrying `not-italic` plus the coral accent, following this project's established emphasis idiom; the `DRESS.intro` paragraph in the body role; the two rule blocks mapped from `DRESS.rules`; and the callout from `DRESS.callout`.

For the two rule blocks: render each record's audience label and its text. Set the audience label in `font-serif` at regular weight in the subheading size with a warm accent colour rather than reaching for a bolder font weight — the typography contract allows exactly two weights across the whole page, and size plus colour already carry the emphasis these labels need. Present the two blocks as parallel references side by side, not as a form of assignment: they are there so a guest can find an outfit idea quickly, and no copy beyond the locked strings may be added around them.

The callout is the one element in this section that uses the coral accent as a background. Render its kicker, its lead line and its body text inside a distinct panel, marked up as an `aside` since it is supplementary guidance rather than part of the rule sequence.

The gallery renders `DRESS.gallery` as two `figure` elements, each containing an `img` and a `figcaption`. Every image element must carry: the `src` from the record, the `alt` from the record (the descriptive pt-BR text ported verbatim from the old site — it is the fallback when a decode fails, so it must not be emptied or replaced with a filename), `loading="lazy"`, `decoding="async"`, and explicit `width` and `height` attributes taken from the record's stored dimensions. The width and height are load-bearing: they are what lets the browser reserve the grid cell before the image decodes, which is the whole reason this section does not shift as a guest scrolls into it. Pair them with a CSS `aspect-ratio` on the figure so the reservation survives the responsive resize, and let the image scale within its cell with an object-fit cover. The caption renders the record's caption text.

Give the gallery container an accessible label describing what it is, matching the old markup's labelled region.
  </action>
  <acceptance_criteria>
    - `src/components/invite/DressCodeSection.tsx` exists and exports `DressCodeSection`.
    - It reads everything from the content module: `grep -c 'DRESS' src/components/invite/DressCodeSection.tsx` returns at least `4`, and `grep -c 'SECTION_IDS.traje' src/components/invite/DressCodeSection.tsx` returns `1`.
    - Exactly two images render, each fully attributed: `grep -c '<img' src/components/invite/DressCodeSection.tsx` returns `1` (rendered inside a map over the two gallery records), and that element carries `loading="lazy"`, `decoding="async"`, a `width` attribute and a `height` attribute.
    - `grep -c 'loading="lazy"' src/components/invite/DressCodeSection.tsx` returns `1`.
    - `grep -c 'decoding="async"' src/components/invite/DressCodeSection.tsx` returns `1`.
    - `grep -cE 'width=\{|height=\{' src/components/invite/DressCodeSection.tsx` returns `2`.
    - `grep -c 'alt=' src/components/invite/DressCodeSection.tsx` returns `1` and its value resolves from the gallery record, not a literal or an empty string.
    - `grep -c 'aspect-\[\|aspect-ratio' src/components/invite/DressCodeSection.tsx` returns at least `1`.
    - No image path is hardcoded — the `src` resolves from the gallery record: `grep -cE 'src=\{' src/components/invite/DressCodeSection.tsx` returns `1` and `grep -cE 'src="/' src/components/invite/DressCodeSection.tsx` returns `0`.
    - No third font weight is introduced: `grep -cE 'font-(medium|semibold|extrabold|black)' src/components/invite/DressCodeSection.tsx` returns `0`.
    - The declared dimensions match the real files — `sips -g pixelWidth -g pixelHeight public/dress-code-men.jpg` and the women file agree with the `width`/`height` literals in `src/content/event.ts`.
    - `npm run build` exits 0 and `npx vitest run` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm run build && npx vitest run && grep -q 'loading="lazy"' src/components/invite/DressCodeSection.tsx && grep -q 'decoding="async"' src/components/invite/DressCodeSection.tsx && test "$(grep -cE 'width=\{|height=\{' src/components/invite/DressCodeSection.tsx | tr -d ' ')" = "2" && test "$(grep -cE 'src="/' src/components/invite/DressCodeSection.tsx | tr -d ' ')" = "0" && test "$(grep -cE 'font-(medium|semibold|extrabold|black)' src/components/invite/DressCodeSection.tsx | tr -d ' ')" = "0" && echo DRESS_OK</automated>
    <human-check>Load with the network throttled to Slow 3G and scroll into the traje section: confirm the two photo cells are already reserved at the correct aspect before the images decode, so nothing below them shifts (this is the declared no-CLS behaviour for E5). At 360px confirm the copy column and the gallery stack cleanly and the callout is legible against its accent background. Block the two images in devtools and confirm the descriptive alt text appears in their place.</human-check>
  </verify>
  <done>The traje section renders the locked copy, both audience reference blocks, the grass callout and the two-photo gallery, with every image carrying lazy loading, async decoding and explicit dimensions matching the real ported files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Bundled content module -> rendered DOM | All programa and traje copy is authored TypeScript literals; no user input, no fetch. |
| `public/` static assets -> rendered `img` elements | Same-origin images served by Vercel's static handling; no third-party image host. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-05-A | Information Disclosure | Dress-code photographs of real people | medium | mitigate | The two reference photos depict identifiable people and are served publicly. Mitigated by (a) re-encoding through `sips` in plan 02-02, which drops the source EXIF block including any GPS or device metadata, and (b) reusing them at exactly the scope the owners already published them at in the previous site — no new subject and no new distribution surface is introduced. |
| T-02-05-B | Tampering / Injection | Content strings rendered into JSX | low | accept | Every string is a hardcoded literal from `src/content/event.ts` and JSX auto-escapes interpolated text. No `dangerouslySetInnerHTML` is used anywhere in this phase, which is what keeps the split-heading rendering (lead plus emphasised fragment) safe as separate nodes rather than parsed markup. Accepted. |
| T-02-05-C | Denial of Service (user-facing) | Image weight on a mobile connection | low | mitigate | Both photos are lazy-loaded and below the fold, and plan 02-02 caps every asset under 250 KB. The explicit width/height plus aspect-ratio reservation means a slow decode costs waiting, never a layout jump that loses the guest's scroll position. |
</threat_model>

<verification>
- `npm run build` exits 0 and `npx vitest run` exits 0.
- Neither component duplicates a content-module string as a literal (checked by the schedule-time and image-path greps).
- The declared image dimensions match the real files in `public/`.
- Human check (end-of-phase): Slow 3G scroll-in with no layout shift; 360px stacking; alt-text fallback with images blocked; hero CTA anchor lands correctly.

</verification>

<success_criteria>
- All seven confirmed programa blocks render in order from the content module, with no schedule value or tentativeness annotation in the component.
- The traje section renders kicker, split heading, intro, both rule blocks, the grass callout and both gallery figures with their original captions and alt text.
- Both images carry lazy loading, async decoding and explicit width/height matching the real ported files, with an aspect-ratio reservation on the cell.
- Both section ids are derived from the shared `SECTION_IDS` constant, so the topbar and hero anchors cannot desynchronize from their targets.
- No third font weight is introduced anywhere in the traje section.
</success_criteria>

<output>
Create `.planning/phases/02-convite-p-blico/02-05-SUMMARY.md` when done.
</output>
