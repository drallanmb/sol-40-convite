---
phase: 01-funda-o-design-system-deploy
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/index.css
  - src/components/ui/Button.tsx
  - src/components/ui/Field.tsx
  - src/components/ui/Card.tsx
  - src/components/ui/Toast.tsx
  - src/components/layout/Shell.tsx
  - src/routes/Home.tsx
autonomous: true
requirements:
  - DESIGN-01
  - DESIGN-02
user_setup: []

must_haves:
  truths:
    - "As cores da paleta pôr do sol estão disponíveis como utilitárias Tailwind (bg-coral, text-plum, etc.)"
    - "Alegreya (display/serif) e Gabarito (corpo/sans) carregam self-hosted e são aplicáveis via tema"
    - "Botão, campo, card e toast renderizam com a identidade visual (mobile-first)"
    - "Existe um shell/layout base (topbar + main + footer) responsivo"
  artifacts:
    - path: "src/index.css"
      provides: "Tokens de cor + fontes + escala tipográfica no @theme do Tailwind v4"
      contains: "@theme"
    - path: "src/components/ui/Button.tsx"
      provides: "Primitivo Botão (variantes primary/quiet)"
      min_lines: 10
    - path: "src/components/ui/Field.tsx"
      provides: "Primitivo Campo (label + input/textarea)"
      min_lines: 10
    - path: "src/components/ui/Card.tsx"
      provides: "Primitivo Card (superfície com sombra offset)"
      min_lines: 8
    - path: "src/components/ui/Toast.tsx"
      provides: "Primitivo Toast (feedback flutuante)"
      min_lines: 8
    - path: "src/components/layout/Shell.tsx"
      provides: "Shell base (topbar + main + footer), mobile-first"
      min_lines: 15
  key_links:
    - from: "src/components/ui/Button.tsx"
      to: "src/index.css"
      via: "usa utilitárias Tailwind geradas pelos tokens do @theme (ex.: bg-orange, text-cream, font-sans)"
      pattern: "(bg-|text-|font-)"
    - from: "src/routes/Home.tsx"
      to: "src/components/layout/Shell.tsx"
      via: "Home renderiza o Shell + um preview dos primitivos para verificação visual"
      pattern: "Shell"
---

<objective>
Portar a identidade visual "pôr do sol" do projeto antigo para o tema do Tailwind v4 (tokens de cor + fontes + escala tipográfica via `@theme`) e construir os primitivos de UI (botão, campo, card, toast) e o shell/layout base (topbar + main + footer), mobile-first, renderizados num preview na home para verificação visual.

Purpose: DESIGN-01 e DESIGN-02. Toda tela das fases 2–6 vai consumir estes tokens e primitivos. Portar as cores EXATAS do `globals.css` antigo garante consistência com o material de referência do dono.
Output: `src/index.css` com `@theme`, quatro primitivos em `src/components/ui/`, `src/components/layout/Shell.tsx`, e a home renderizando o preview.

Fonte de estilo (consultar, não copiar cru): `/Users/allanmesquitabrito/Documents/Site Sol 40 anos/sol-40-integrado/app/globals.css` — o bloco `:root` (linhas ~57-103) tem os tokens exatos; classes como `.button-primary`, `.field`, `.toast`, `.rsvp-form`/`.memory-card` (cards) servem de referência de estilo para os primitivos.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tokens de cor + fontes + escala tipográfica no @theme</name>
  <files>src/index.css</files>
  <read_first>src/index.css, /Users/allanmesquitabrito/Documents/Site Sol 40 anos/sol-40-integrado/app/globals.css</read_first>
  <action>
    Reescreva `src/index.css` começando com `@import "tailwindcss";` seguido de um bloco `@theme` que define os tokens EXATOS do `:root` do globals.css antigo — NÃO reinvente cores. Cores como `--color-*` (para gerar utilitárias): cream `#fff3df`, sand `#f6dfc3`, peach `#f3a271`, coral `#ee6a50`, orange `#d94f29`, plum `#35192a`, wine `#6d253a`, ink `#2b1822`, sea `#1f4650`. Defina também os tokens de RSVP (sem verde): `--color-rsvp-sim` = sea (`#1f4650`), `--color-rsvp-pendente` = `#8a4a15`, `--color-rsvp-nao` = wine (`#6d253a`). A cor de linha/grade: `--color-line` = `rgba(53,25,42,.18)`. Fontes como `--font-serif` = `"Alegreya Variable", Georgia, serif` e `--font-sans` = `"Gabarito Variable", Arial, sans-serif`. Escala tipográfica no namespace `--text-*` com os `clamp()` exatos: caption `.8125rem`, small `.9375rem`, body `clamp(1rem,.96rem + .18vw,1.125rem)`, lead `clamp(1.25rem,1.12rem + .35vw,1.5rem)`, subheading `clamp(1.5rem,1.35rem + .4vw,1.75rem)`, heading `clamp(3rem,5.25vw,4.875rem)`, display `clamp(5rem,9vw,7.25rem)`. Tracking `--tracking-label: .1em` e `--tracking-display: -.025em`; durações `--duration-fast: 180ms` / `--duration-medium: 260ms`; easing `--ease-out: cubic-bezier(.22,1,.36,1)`; espaçamentos `--space-*` (2xs `.25rem` … xl `2rem`) e z-index (sticky/status/toast/skip). Carregue as fontes self-hosted com `@font-face` apontando para os `.woff2` variáveis de `@fontsource-variable/alegreya` e `@fontsource-variable/gabarito` (family "Alegreya Variable" e "Gabarito Variable", `font-weight: 400 900`, `font-display: swap`) — espelhando o padrão do globals.css antigo (self-host, sem CDN do Google). Fora do `@theme`, aplique os estilos base: `body` com `background: var(--color-cream)`, `color: var(--color-ink)`, `font-family` sans e `line-height: 1.62`; `:focus-visible` com outline coral; `::selection` coral/cream. Mobile-first.
  </action>
  <verify>
    <automated>npm run build && grep -q "@theme" src/index.css && grep -q "#fff3df" src/index.css && grep -q "#ee6a50" src/index.css && grep -q "#d94f29" src/index.css && grep -q "Alegreya Variable" src/index.css && grep -q "Gabarito Variable" src/index.css && grep -q "cubic-bezier(.22,1,.36,1)" src/index.css</automated>
  </verify>
  <acceptance_criteria>
    - src/index.css contém "@import "tailwindcss"" e um bloco "@theme"
    - src/index.css contém os hex exatos: #fff3df, #f6dfc3, #f3a271, #ee6a50, #d94f29, #35192a, #6d253a, #2b1822, #1f4650
    - src/index.css contém o token rsvp-pendente "#8a4a15" (paleta RSVP sem verde)
    - src/index.css contém "Alegreya Variable" e "Gabarito Variable" com @font-face
    - src/index.css contém "cubic-bezier(.22,1,.36,1)" (easing) e ao menos um clamp() da escala tipográfica
    - `npm run build` passa
  </acceptance_criteria>
  <done>Tokens de cor, fontes e escala tipográfica disponíveis como tema Tailwind; fontes self-hosted carregam; build passa.</done>
</task>

<task type="auto">
  <name>Task 2: Primitivos de UI — Button, Field, Card, Toast</name>
  <files>src/components/ui/Button.tsx, src/components/ui/Field.tsx, src/components/ui/Card.tsx, src/components/ui/Toast.tsx</files>
  <read_first>/Users/allanmesquitabrito/Documents/Site Sol 40 anos/sol-40-integrado/app/globals.css</read_first>
  <action>
    Crie quatro primitivos React tipados usando as utilitárias Tailwind geradas pelos tokens do Task 1 (mobile-first, alvos de toque ≥44px, foco visível). `Button.tsx`: variantes `primary` (fundo orange/plum, texto cream, uppercase, tracking-label, cantos arredondados 100px) e `quiet` (borda, transparente), com estado `disabled` e `aria-busy` — referência `.button-primary`/`.button-quiet` do globals.css. `Field.tsx`: label + input/textarea com borda inferior (foco vira coral), suportando `label`, `hint/small`, e passando props nativas — referência `.field`. `Card.tsx`: superfície `bg-[#fffaf1]`/cream com borda `line` e sombra offset (ex.: `box-shadow` sand deslocado) — referência `.rsvp-form`/`.memory-card`. `Toast.tsx`: container flutuante fixo (plum/cream, borda, sombra) com `role="status"` para feedback — referência `.toast`. Renderize texto via JSX (auto-escapado pelo React) — NÃO use `dangerouslySetInnerHTML`. Cada componente exporta um tipo de props e é reutilizável pelas fases seguintes. Não instale bibliotecas novas (sem shadcn, sem headlessui) — só React + Tailwind.
  </action>
  <verify>
    <automated>npm run build && test -f src/components/ui/Button.tsx && test -f src/components/ui/Field.tsx && test -f src/components/ui/Card.tsx && test -f src/components/ui/Toast.tsx && test "$(grep -rIl dangerouslySetInnerHTML src/components/ui/ 2>/dev/null | wc -l | tr -d ' ')" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - Existem Button.tsx, Field.tsx, Card.tsx, Toast.tsx em src/components/ui/
    - Cada arquivo usa utilitárias Tailwind dos tokens (grep: bg-/text-/font-/border-)
    - Nenhum primitivo usa dangerouslySetInnerHTML (grep em src/components/ui/ retorna 0)
    - Button expõe variantes e estado disabled/aria-busy
    - `npm run build` passa (sem erros de tipo)
  </acceptance_criteria>
  <done>Quatro primitivos tipados e reutilizáveis renderizam com a identidade visual; build passa.</done>
</task>

<task type="auto">
  <name>Task 3: Shell/layout base + preview na home</name>
  <files>src/components/layout/Shell.tsx, src/routes/Home.tsx</files>
  <read_first>src/routes/Home.tsx, /Users/allanmesquitabrito/Documents/Site Sol 40 anos/sol-40-integrado/app/globals.css</read_first>
  <action>
    Crie `src/components/layout/Shell.tsx`: um layout base mobile-first com topbar (wordmark "Sol faz 40" em serif + slot de navegação), uma área `<main>` que recebe `children`, e um footer — usando os tokens (cream de fundo, plum/ink no texto, serif nos títulos), referência `.topbar`/`.site-footer` do globals.css (adaptado, sem copiar cru; sem o comportamento de scroll/countdown, que é da Phase 2). Deve empilhar no mobile e espaçar no desktop (breakpoints Tailwind). Atualize `src/routes/Home.tsx` para renderizar o `Shell` envolvendo um pequeno "preview do design system": uma amostra de swatches das cores da paleta, os títulos em Alegreya, e uma instância de cada primitivo (Button primary+quiet, Field, Card, e um Toast disparável) — para que a identidade visual seja visível e verificável de ponta a ponta. Mantenha o preview enxuto (é andaime de verificação, não uma página final).
  </action>
  <verify>
    <automated>npm run build && grep -q "Shell" src/routes/Home.tsx && test -f src/components/layout/Shell.tsx && grep -Eq "(Button|Card|Field|Toast)" src/routes/Home.tsx</automated>
    <human-check>Rode `npm run dev` e abra `/`: confirme visualmente que (1) o fundo é o cream #fff3df e os títulos usam Alegreya (serif); (2) os swatches mostram coral/orange/plum/wine/sea corretos; (3) Button, Field, Card e Toast renderizam com a identidade pôr do sol; (4) o layout empilha bem no viewport mobile (~375px). Verificação diferida para o fim da fase (human_verify_mode=end-of-phase).</human-check>
  </verify>
  <acceptance_criteria>
    - src/components/layout/Shell.tsx existe com topbar, main (children) e footer
    - src/routes/Home.tsx importa e renderiza Shell
    - src/routes/Home.tsx renderiza ao menos um de cada primitivo (Button/Field/Card/Toast)
    - `npm run build` passa
  </acceptance_criteria>
  <done>Shell base responsivo e preview do design system na home; identidade visual renderiza; build passa.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| conteúdo renderizado → DOM | primitivos que exibem texto podem ser vetor de XSS se não escaparem |
| app → assets de fonte | fontes carregadas localmente vs CDN de terceiro |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering | XSS via primitivos (Toast/Field renderizando conteúdo) | medium | mitigate | Primitivos renderizam texto via JSX (auto-escape do React); proibido `dangerouslySetInnerHTML` (grep gate == 0 em src/components/ui/). |
| T-03-02 | Information disclosure | dependência de CDN externo de fontes (Google Fonts) | low | mitigate | Fontes self-hosted via `@fontsource-variable/*` com `@font-face` local — sem chamada a CDN de terceiro (também melhora performance em rede móvel). |
| T-03-03 | Tampering | instalação de UI kit de terceiro não auditado | low | accept | Nenhum pacote novo é instalado; primitivos usam só React + Tailwind já pinados em 01-01. |
</threat_model>

<verification>
Antes de declarar o plano completo:
- [ ] `npm run build` conclui sem erros de tipo
- [ ] grep confirma os hex exatos da paleta e as duas famílias de fonte em src/index.css
- [ ] Os quatro primitivos existem e não usam dangerouslySetInnerHTML
- [ ] Home renderiza o Shell + preview dos primitivos
- [ ] (human-check, fim de fase) `/` mostra a identidade pôr do sol correta e empilha bem no mobile
</verification>

<success_criteria>
- Todas as tasks concluídas
- Tokens de cor + fontes portados EXATAMENTE do globals.css antigo para o @theme
- Botão, campo, card e toast renderizam com a identidade visual
- Shell base responsivo (mobile-first)
- Build passa
</success_criteria>

<output>
Ao concluir, crie `.planning/phases/01-funda-o-design-system-deploy/01-03-SUMMARY.md`
</output>
