---
phase: 01-funda-o-design-system-deploy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - tsconfig.json
  - tsconfig.app.json
  - tsconfig.node.json
  - vite.config.ts
  - index.html
  - src/main.tsx
  - src/App.tsx
  - src/routes/Home.tsx
  - src/routes/Admin.tsx
  - src/index.css
  - convex/schema.ts
  - convex/tsconfig.json
  - .gitignore
  - .env.example
autonomous: false
requirements:
  - SETUP-01
user_setup:
  - service: convex
    why: "Criar o dev deployment do Convex e popular VITE_CONVEX_URL — o login do Convex abre o navegador e não pode ser automatizado."
    dashboard_config:
      - task: "Criar/entrar na conta Convex e rodar o dev deployment"
        location: "Terminal do projeto → `npx convex dev` (abre o navegador para login e cria o deployment de desenvolvimento)"
        details: "Ao rodar, o Convex grava VITE_CONVEX_URL em .env.local automaticamente. Deixe rodar em outro terminal enquanto desenvolve."

must_haves:
  truths:
    - "O projeto compila e sobe em dev com Vite (React + TS)"
    - "Existe rota `/` (home) e rota `/admin` placeholder, ambas via React Router v7 em library mode"
    - "O ConvexReactClient está montado no root e lê VITE_CONVEX_URL do ambiente"
    - "As versões críticas estão pinadas: react-router 7.x (não 8), typescript 6.x (não 7)"
  artifacts:
    - path: "package.json"
      provides: "Dependências com versões pinadas (Vite/React/TS/Tailwind v4/Convex/React Router v7)"
      contains: "react-router"
    - path: "vite.config.ts"
      provides: "Plugins Vite: @vitejs/plugin-react + @tailwindcss/vite"
      contains: "tailwindcss"
    - path: "src/main.tsx"
      provides: "Montagem do ConvexReactClient + BrowserRouter no root"
      contains: "ConvexReactClient"
    - path: "src/routes/Admin.tsx"
      provides: "Rota placeholder /admin (sem auth — auth é Phase 6)"
      min_lines: 3
    - path: "convex/schema.ts"
      provides: "defineSchema stub (fundação do backend Convex)"
      contains: "defineSchema"
  key_links:
    - from: "src/main.tsx"
      to: "src/App.tsx"
      via: "BrowserRouter envolvendo <App/>; App declara as rotas / e /admin"
      pattern: "BrowserRouter"
    - from: "src/main.tsx"
      to: "convex/schema.ts"
      via: "ConvexReactClient(import.meta.env.VITE_CONVEX_URL) conecta o frontend ao backend Convex"
      pattern: "import\\.meta\\.env\\.VITE_CONVEX_URL"
---

<objective>
Scaffoldar a stack base do projeto — Vite (SPA) + React + TypeScript + Tailwind v4 + Convex + React Router v7 — com todas as versões pinadas nas linhas maduras (React Router v7, TypeScript 6), e o "olá mundo" navegável com rota `/` e placeholder `/admin`, já conectado a um dev deployment do Convex.

Purpose: É a fundação de TODO o projeto (SETUP-01). Sem ela, nada mais pode ser construído. Pinar versões aqui evita bugs de major recém-lançado (React Router 8 / TypeScript 7) num projeto com data fixa (17/10/2026).
Output: Repositório com a stack instalada, `vite.config.ts` com o plugin do Tailwind, cliente Convex montado no root, rotas `/` e `/admin`, `convex/schema.ts` stub, `.env.example` e `.gitignore`.

Recomendação de execução: use o agente `convex:convex-expert` para todo código dentro de `convex/` (schema stub, wiring do cliente) e para validar a conexão frontend↔Convex. NÃO construir auth do dono agora — isso é da Phase 6; aqui `/admin` é só um placeholder de rota.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
@$HOME/.claude/gsd-core/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/STACK.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>Lista de pacotes a instalar (verificação de legitimidade de supply chain — não existe `## Package Legitimacy Audit` no RESEARCH, então a política de fallback trata todos como a-verificar). Todos são pacotes oficiais first-party, já com versões confirmadas contra o registro npm em 2026-07-23 na STACK.md.</what-built>
  <how-to-verify>
    Confirme que cada pacote é o oficial em npmjs.com antes de autorizar a instalação:
    1. vite → https://npmjs.com/package/vite (8.1.5)
    2. @vitejs/plugin-react → https://npmjs.com/package/@vitejs/plugin-react
    3. react + react-dom → https://npmjs.com/package/react (19.2.8)
    4. react-router → https://npmjs.com/package/react-router (fixar 7.18.1, NÃO 8.x)
    5. typescript → https://npmjs.com/package/typescript (fixar 6.0.3, NÃO 7.x)
    6. convex → https://npmjs.com/package/convex (1.42.3)
    7. tailwindcss + @tailwindcss/vite → https://npmjs.com/package/tailwindcss (4.3.3)
    8. @fontsource-variable/alegreya → https://npmjs.com/package/@fontsource-variable/alegreya (5.3.0)
    9. @fontsource-variable/gabarito → https://npmjs.com/package/@fontsource-variable/gabarito (5.3.0)
  </how-to-verify>
  <resume-signal>Digite "approved" para autorizar a instalação, ou aponte qualquer pacote suspeito.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Scaffold Vite + instalar dependências pinadas</name>
  <files>package.json, package-lock.json, tsconfig.json, tsconfig.app.json, tsconfig.node.json, vite.config.ts, index.html, .gitignore</files>
  <read_first>.planning/research/STACK.md</read_first>
  <action>
    Gere o scaffold base de forma não-interativa com `npm create vite@latest . -- --template react-ts` (template React + TypeScript, SPA — sem Next, sem SSR). Depois instale as dependências com versões EXATAS (sem `^`/`~`) conforme STACK.md: runtime → `convex@1.42.3`, `react-router@7.18.1`, `tailwindcss@4.3.3`, `@tailwindcss/vite@4.3.3`, `@fontsource-variable/alegreya@5.3.0`, `@fontsource-variable/gabarito@5.3.0`; dev → `typescript@6.0.3`. Fixe react/react-dom em 19.2.x. Pinos CRÍTICOS e inegociáveis: `react-router` na linha 7 (NÃO 8.x — major de 5 semanas exige Node ≥22.22 e ESM-only) e `typescript` na linha 6 (NÃO 7.x/"tsgo" — typescript-eslint ainda sem suporte). Configure `vite.config.ts` para usar os plugins `@vitejs/plugin-react` e `@tailwindcss/vite` (o plugin do Tailwind v4 substitui o postcss.config). Garanta que `package.json` tenha os scripts `dev`, `build` (compilação TS + `vite build`) e `preview`. Adicione ao `.gitignore` (se ainda não houver): `node_modules`, `dist`, `.env.local`, `.env.*.local`, `.convex`. Faça commit do `package-lock.json` para travar a árvore inteira de dependências. Instale APENAS o listado — não adicione bibliotecas extras.
  </action>
  <verify>
    <automated>test -f vite.config.ts && node -e "const p=require('./package.json'); const rr=(p.dependencies['react-router']||'').replace(/[^0-9.]/g,''); const ts=(p.devDependencies.typescript||'').replace(/[^0-9.]/g,''); if(!rr.startsWith('7.')) throw new Error('react-router deve ser 7.x, got '+rr); if(!ts.startsWith('6.')) throw new Error('typescript deve ser 6.x, got '+ts); console.log('pins OK', rr, ts)" && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - package.json contém "react-router" na linha 7 (grep: `"react-router": "7`)
    - package.json contém "typescript" na linha 6 (grep: `"typescript": "6`)
    - package.json contém "convex", "tailwindcss" e "@tailwindcss/vite"
    - vite.config.ts contém "tailwindcss" e o plugin react
    - package-lock.json existe e está versionado
    - `npm run build` conclui sem erros
  </acceptance_criteria>
  <done>Stack instalada com versões pinadas; `npm run build` passa; lockfile commitado.</done>
</task>

<task type="auto">
  <name>Task 2: Wire router + cliente Convex + rotas / e /admin</name>
  <files>src/main.tsx, src/App.tsx, src/routes/Home.tsx, src/routes/Admin.tsx, src/index.css, convex/schema.ts, convex/tsconfig.json, .env.example</files>
  <read_first>src/main.tsx, src/App.tsx, .planning/research/ARCHITECTURE.md</read_first>
  <action>
    Monte o root em `src/main.tsx`: crie um `ConvexReactClient` lendo `import.meta.env.VITE_CONVEX_URL` e envolva a árvore com `ConvexProvider` (do pacote `convex/react`) e com `BrowserRouter` (React Router v7 em library mode). Use APENAS `ConvexProvider` — NÃO monte `ConvexAuthProvider`/qualquer provider de auth; a auth do dono é da Phase 6 e reintroduzi-la aqui seria escopo errado. Em `src/App.tsx` declare as rotas com `<Routes>`: `/` → `Home`, `/admin` → `Admin`. Crie `src/routes/Home.tsx` com um "olá mundo" simples ("Sol faz 40 — em construção") e `src/routes/Admin.tsx` como placeholder mínimo ("Admin — área dos donos (em breve)", sem qualquer proteção ou dado). Mantenha `src/index.css` mínimo por enquanto — apenas `@import "tailwindcss";` (os tokens do design system entram no plano 01-03). Em `convex/schema.ts` crie um `defineSchema({})` stub (comentado que as tabelas rsvps/wines/posts/settings virão nas fases 3–6) para estabelecer a fundação do backend, com `convex/tsconfig.json` gerado. Crie `.env.example` documentando `VITE_CONVEX_URL` (URL pública do deployment Convex, injetada pelo build — valor de exemplo apenas). Recomende no SUMMARY que o schema/cliente foram validados com `convex:convex-expert`.
  </action>
  <verify>
    <automated>npm run build && grep -q "ConvexReactClient" src/main.tsx && grep -q "VITE_CONVEX_URL" src/main.tsx && grep -q "defineSchema" convex/schema.ts && grep -Eq "/admin" src/App.tsx</automated>
  </verify>
  <acceptance_criteria>
    - src/main.tsx contém "ConvexReactClient" e "ConvexProvider" (e NÃO "ConvexAuthProvider")
    - src/main.tsx contém "import.meta.env.VITE_CONVEX_URL"
    - src/main.tsx contém "BrowserRouter"
    - src/App.tsx declara as rotas "/" e "/admin"
    - src/routes/Admin.tsx existe como placeholder sem lógica de auth
    - convex/schema.ts contém "defineSchema"
    - .env.example contém "VITE_CONVEX_URL"
    - `npm run build` passa
  </acceptance_criteria>
  <done>Home e Admin navegáveis via React Router; cliente Convex conectado ao dev deployment; schema stub no lugar; build passa.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| máquina do dev → registro npm | pacotes de terceiros entram na árvore de build (risco de supply chain) |
| frontend (browser) → deployment Convex | cliente conecta via `VITE_CONVEX_URL` (URL pública por design) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-SC | Tampering | instalação npm (dependências) | high | mitigate | Checkpoint blocking-human verifica cada pacote em npmjs.com antes de instalar; versões EXATAS pinadas (sem `^`/`~`); `package-lock.json` commitado para travar a árvore transitiva. |
| T-01-01 | Tampering | major recém-lançado (React Router 8 / TypeScript 7) | medium | mitigate | Pins forçam react-router 7.x e typescript 6.x; verify falha o build se a major errada entrar. |
| T-01-02 | Information disclosure | `VITE_CONVEX_URL` no bundle do cliente | low | accept | A URL do deployment Convex é pública por design (o cliente reativo precisa dela); nenhum segredo é exposto — segredos ficam em env do servidor (plano 01-02). |
| T-01-03 | Spoofing | rota `/admin` acessível sem auth | low | accept | Placeholder sem dados nesta fase; o gate real (`requireOwner`) é da Phase 6, conforme roadmap — nenhum dado sensível existe ainda para proteger. |
</threat_model>

<verification>
Antes de declarar o plano completo:
- [ ] `npm run build` conclui sem erros nem warnings de tipo
- [ ] `npm run dev` sobe e `/` e `/admin` renderizam (navegação client-side funciona)
- [ ] Pins confirmados: react-router 7.x, typescript 6.x
- [ ] `.env.local` NÃO está versionado (aparece no `.gitignore`)
</verification>

<success_criteria>
- Todas as tasks concluídas
- Stack Vite+React+TS+Tailwind v4+Convex+React Router v7 instalada com versões pinadas
- Cliente Convex montado no root lendo VITE_CONVEX_URL
- Rotas `/` e `/admin` navegáveis
- Build passa; lockfile commitado
</success_criteria>

<output>
Ao concluir, crie `.planning/phases/01-funda-o-design-system-deploy/01-01-SUMMARY.md`
</output>
