---
phase: 03-rsvp
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - vite.config.ts
  - convex/convex.config.ts
  - convex/schema.ts
  - convex/rsvpModel.ts
  - convex/rsvpInternal.ts
  - convex/rsvpTest.ts
  - convex/rsvps.test.ts
  - convex/_generated/*
  - src/lib/phone.ts
  - src/lib/phone.test.ts
autonomous: true
requirements: [RSVP-01, RSVP-02, RSVP-05]

must_haves:
  truths:
    - "`normalizePhone` is a pure, server-authoritative Brazilian-phone parser whose table-driven tests cover formatting, explicit/spaced country code, domestic trunk zero, fixed lines, DDD 55, country + DDD 55, legacy ninth-digit candidates, invalid lengths, invalid DDDs, letters/control characters, and normalized equivalence (RSVP-01)."
    - "A valid national number beginning with DDD 55 is never mistaken for country code 55; country code is removed only when an explicit `+55` or excess total length proves it is a country prefix."
    - "Ambiguous ten-digit mobile-looking input exposes an exact candidate and the current ninth-digit candidate in deterministic order; a later lookup can fail closed if those candidates point at different invitations."
    - "`rsvps`, `rsvpGuests`, and `rsvpSessions` have explicit Convex validators and the indexed paths needed by the public flow; attendance is always the literal union `pending | yes | no`, never inferred from a missing field (RSVP-02)."
    - "Logical uniqueness is enforced inside one internal Convex mutation before insert: equivalent phone forms cannot create a second RSVP, and idempotent fixture setup cannot duplicate guests."
    - "Development fixtures are internal-only, idempotent, and cover normal, zero-person, one-person, and many-person/long-name invitations. Their valid demo phones are derived from a server-only random seed, returned only to the authenticated Convex CLI, and never committed or exposed by a public function."
    - "The first Convex integration harness runs through Vitest, registers the official rate-limiter test adapter, and tests schema/fixture/uniqueness behavior before public handlers are implemented."
    - "`@convex-dev/rate-limiter@0.3.2` and `convex-test@0.0.54` are exact-pinned and the Convex component is registered; no locally invented limiter replaces the official component (RSVP-05 foundation)."
    - "The existing untracked `convex/_generated/` directory is regenerated only by Convex tooling and is never deleted, copied over from a template, or hand-edited."
  artifacts:
    - src/lib/phone.ts
    - src/lib/phone.test.ts
    - convex/schema.ts
    - convex/rsvpModel.ts
    - convex/rsvpInternal.ts
    - convex/rsvpTest.ts
    - convex/rsvps.test.ts
    - convex/convex.config.ts
  key_links:
    - "`src/lib/phone.ts#normalizePhone` -> `convex/rsvpInternal.ts` and the public unlock handler in plan 03-02 — client masking is never authoritative."
    - "`convex/rsvpModel.ts` -> `convex/schema.ts`, `convex/rsvpInternal.ts`, and plan 03-02 handlers — attendance/session/contact bounds are declared once."
    - "`convex/rsvpTest.ts` -> `convex/rsvps.test.ts` — every Convex test instance uses the real phase schema plus `@convex-dev/rate-limiter/test`."
    - "`convex/convex.config.ts` -> generated `components.rateLimiter` types — generation is performed by `npx convex dev --once`, never by editing `convex/_generated`."
  prohibitions:
    - id: P-03-01
      requirement_id: RSVP-04
      provenance: flagged-unverified
      statement: "Acesso por telefone MUST NOT virar conta/login persistente nem autenticar admin."
      allowed_copy: "Only the approved negative phrases `Não precisa criar conta` and `sem criar conta` may mention account creation in the later UI."
      verification: "Schema has no guest account/password/role field; internal fixture exposes no admin/session-owner path; source tests assert no account/password/admin endpoint or form is created. Later copy tests require the two approved negative phrases and reject affirmative cadastro/login/senha/admin language."
    - id: P-03-02
      requirement_id: RSVP-04
      provenance: flagged-unverified
      statement: "Telefone desconhecido MUST NOT auto-cadastrar/criar família."
      verification: "Only an `internalMutation` can create a fixture/invitation; no public create/seed function exists, and an unknown lookup test in plan 03-02 asserts RSVP/guest counts remain unchanged."
    - id: P-03-05
      requirement_id: RSVP-04
      provenance: flagged-unverified
      statement: "Funções públicas MUST NOT expor telefone/contato/token/IDs brutos nem permitir enumeração de famílias."
      verification: "This plan creates no public reader. The schema uses an opaque guest `publicRef` distinct from Convex `_id`; plan 03-02 validates every public return shape and pre-unlock outcome."
---

<objective>
Build the Wave 0 foundation for RSVP: exact-pinned Convex test/rate-limit infrastructure, precise Brazilian phone normalization, the three-table schema, and an internal idempotent invitation seam under integration test.

Purpose: Plans 03-02 and 03-03 must not build security-sensitive public behavior or UI against an untested parser, an implicit schema, or a missing component harness. This plan makes those dependencies executable first.

Output: a passing phone unit suite, a passing Convex schema/fixture suite, registered rate-limiter component, generated API/component types, and a real Convex development smoke.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-rsvp/03-CONTEXT.md
@.planning/phases/03-rsvp/03-RESEARCH.md
@.planning/phases/03-rsvp/03-PATTERNS.md
@.planning/phases/03-rsvp/03-VALIDATION.md
@.planning/phases/03-rsvp/03-UI-SPEC.md
@.planning/phases/01-funda-o-design-system-deploy/01-01-SUMMARY.md
@package.json
@vite.config.ts
@convex/schema.ts
</context>

## Wave and dependencies

- **Wave:** 0 — test and data foundation.
- **Depends on:** Phase 1 only: Vite/Vitest config, direct Convex client, empty schema stub, exact-pin package policy.
- **Unblocks:** 03-02 public capability API, then 03-03 `/confirmar`.
- **No Phase 2 domain dependency:** only its existing UI integration points are read later; this plan does not pull invitation-page context into backend design.

## Artifacts this phase produces

| Symbol / file | Kind |
|---------------|------|
| `normalizePhone`, `NormalizedPhone`, phone candidate/key helpers | new pure module |
| `src/lib/phone.test.ts` | new table-driven unit suite |
| `attendanceValidator`, contact/session/family bounds | new shared Convex model constants |
| `rsvps`, `rsvpGuests`, `rsvpSessions` | new schema tables |
| `by_phone`, `by_rsvp`, `by_rsvp_sort`, `by_rsvp_public_ref`, `by_token_hash`, `by_expires_at` | new indexes |
| reusable internal insert helper plus `ensureDemoFixtures` | new guarded internal-only fixture/import seam for normal/zero/one/many-long shapes |
| `convex/convex.config.ts` | new rate-limiter component registration |
| `makeRsvpTest` | new Convex test harness factory |
| `convex/rsvps.test.ts` schema/fixture sections | new integration suite, extended in 03-02 |
| Convex API/component declarations | regenerated artifacts, never hand-authored |

## Probe assumptions (unresolved — flagged, not dismissed)

No `03-SPEC.md` exists, so the fallback edge probe remains active. No row is auto-dismissed.

| Requirement | Probe result | Explicit fallback coverage in this plan |
|-------------|--------------|-----------------------------------------|
| RSVP-01 | `unclassified unresolved` | The edge set is the complete phone matrix from RESEARCH: explicit/spaced `55`, trunk `0`, fixed/mobile/legacy, DDD 55, invalid length/DDD/text/control input, candidate equivalence, and deterministic ambiguity. This is assumed from RESEARCH/CONTEXT, not claimed as SPEC-confirmed. |
| RSVP-02 | `unclassified unresolved` | The edge set is valid/invalid attendance shapes, zero/one/many guests, logical phone uniqueness, deterministic sort, opaque guest references, and idempotent internal fixture creation. This is assumed from RESEARCH/CONTEXT. |
| RSVP-05 | `boundary unresolved`; `precision unresolved` | Component setup is completed here. Plan 03-02 must test every configured limiter at `N-1`, `N`, and `N+1`, expiration at `expiresAt-1ms`, `expiresAt`, and `expiresAt+1ms`, integer-second `retryAfter` ceiling, and equivalent normalized keys. Nothing in this row is treated as resolved by this foundation alone. |

## Scope budget

This plan names 12 paths, but only seven are authored behavior/test files. `package.json` + lockfile are one atomic dependency operation; `vite.config.ts` + `convex/convex.config.ts` are the Wave 0 runner/component wiring; `convex/_generated/*` is tool-owned output, not a hand-authored task. The three tasks stay isolated at 5 config/generated paths, 2 phone paths, and 6 schema/harness paths respectively. Splitting them would leave either an un-runnable Convex harness or a schema without its only safe fixture proof.

<tasks>

<task type="auto">
  <name>Task 1: Install and register the Convex test/rate-limit Wave 0 infrastructure</name>
  <read_first>
    - package.json (existing exact pins and `test: vitest run`; preserve every current dependency and script)
    - package-lock.json (committed lockfile; update through npm only)
    - vite.config.ts (currently includes only `src/**/*.test.ts`)
    - convex/tsconfig.json (Convex has its own type-check boundary; frontend build alone does not validate it)
    - .planning/phases/03-rsvp/03-VALIDATION.md (§ Test Infrastructure and § Wave 0 Requirements)
    - .planning/phases/03-rsvp/03-RESEARCH.md (§ Rate Limiting > Package and setup; § Testing Strategy)
    - convex/_generated/ (existing untracked generated directory; inspect, preserve, never hand-edit)
  </read_first>
  <files>package.json, package-lock.json, vite.config.ts, convex/convex.config.ts, convex/_generated/*</files>
  <action>
Install `@convex-dev/rate-limiter@0.3.2` as an exact application dependency and `convex-test@0.0.54` as an exact dev dependency using npm with `--save-exact`. Do not add a custom in-memory limiter. Inspect `convex-test@0.0.54`'s published peer requirements; install `@edge-runtime/vm` as an exact dev dependency only if that pinned setup requires it, using the peer version reported for this exact package rather than guessing a version. Preserve the repository's existing packages and scripts.

Extend the existing Vitest `include` to run both `src/**/*.test.ts` and `convex/**/*.test.ts`, retaining `environment: 'node'`, `globals: false`, `passWithNoTests: true`, and non-watch `vitest run`.

Create `convex/convex.config.ts` with `defineApp()` and `app.use(rateLimiter)` from the official component. Do not invent another component name and do not expose component internals to the browser.

Before generation, record `git status --short convex/_generated`. Run Convex generation/dev tooling in place; do not remove the directory and do not copy generated files from any other checkout. Never patch files under `convex/_generated` by hand. If the connected dev deployment is not yet available at this first task boundary, continue with the local test/config checks and perform the mandatory `npx convex dev --once` smoke in Task 3 before the plan is considered complete.
  </action>
  <acceptance_criteria>
    - `package.json` pins `@convex-dev/rate-limiter` to exactly `0.3.2` and `convex-test` to exactly `0.0.54`, with no caret or tilde.
    - If `@edge-runtime/vm` is present, its package.json value is exact and its necessity is documented from the pinned package's peer contract; it is not added speculatively.
    - `vite.config.ts` includes both `src/**/*.test.ts` and `convex/**/*.test.ts`, and `npm test` remains a one-shot command.
    - `convex/convex.config.ts` registers exactly the official rate-limiter component.
    - Existing files under `convex/_generated/` were not deleted or manually edited; all changes are attributable to Convex generation.
    - `npm test` and `npm run build` exit 0.
  </acceptance_criteria>
  <verify>
    <automated>node -e 'const p=require("./package.json"); process.exit(p.dependencies["@convex-dev/rate-limiter"]==="0.3.2" && p.devDependencies["convex-test"]==="0.0.54" && p.scripts.test==="vitest run" ? 0 : 1)' && grep -q "src/\\*\\*/\\*.test.ts" vite.config.ts && grep -q "convex/\\*\\*/\\*.test.ts" vite.config.ts && npm test && npm run build</automated>
  </verify>
  <done>The exact official packages are installed, Vitest sees frontend and Convex tests, the component is registered, and existing tests/build remain green without touching generated code manually.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement precise Brazilian phone normalization and candidate semantics</name>
  <read_first>
    - .planning/REQUIREMENTS.md (§ RSVP, RSVP-01)
    - .planning/phases/03-rsvp/03-CONTEXT.md (D-03 through D-06 and canonical prior phone references)
    - .planning/phases/03-rsvp/03-RESEARCH.md (§ Brazilian Phone Normalization, including parsing order, ambiguity, and required matrix)
    - /Users/allanmesquitabrito/Documents/Site Sol 40 anos/sol-40-integrado/lib/phone.mjs (prior behavior; preserve only the proven DDD-55 rule)
    - /Users/allanmesquitabrito/Documents/Site Sol 40 anos/sol-40-integrado/tests/phone.test.mjs (existing DDD 55 and international cases)
    - src/lib/countdown.ts and src/lib/countdown.test.ts (project convention for pure co-located logic/tests)
  </read_first>
  <files>src/lib/phone.ts, src/lib/phone.test.ts</files>
  <behavior>
    - `(79) 99999-9999`, `+55 (79) 99999-9999`, `55 79 99999-9999`, and `0 79 99999-9999` resolve to current canonical `79999999999` and the same limiter/lookup canonical key.
    - `(79) 3222-2222` remains fixed-line canonical `7932222222`.
    - `(55) 99999-9999` remains national DDD-55 mobile `55999999999`; `55 3222-2222` remains DDD-55 fixed `5532222222`; `+55 55 99999-9999` removes only the country prefix.
    - Ten-digit subscriber prefix `2`–`5` is fixed; each prefix `6`, `7`, `8`, and `9` returns deterministic `[exact, DDD + 9 + subscriber]` candidates while selecting the current form as the equivalence/limiter key; prefix `0` or `1` is invalid.
    - Invalid DDDs, subscriber prefixes/lengths, empty input, misplaced plus signs, letters, and control characters are invalid rather than silently reduced to digits.
  </behavior>
  <action>
Write the test matrix first, then implement `src/lib/phone.ts` as a pure TypeScript module with no DOM or Convex dependency. Export `normalizePhone` and a discriminated result that distinguishes `canonical`, `legacy-mobile`, and `invalid`, while providing deterministic lookup candidates and one normalized key for equivalent per-phone limiting.

Inspect the **raw value before trim** and reject every C0/C1 control character, including tab, CR, and LF. The only whitespace accepted is ordinary ASCII space (`U+0020`) and non-breaking space (`U+00A0`); trim only those two at the edges. Reject letters and any misplaced `+` before removing formatting. Then follow the RESEARCH parsing order: record explicit leading `+55`; remove only digits plus the approved spaces, parentheses, hyphen, and dot; remove one domestic trunk zero when the remaining length is plausible; remove country code `55` only for explicit `+55` or when excess total length proves a country prefix; never strip leading `55` from an already valid 10/11-digit national number; validate against the official Brazilian DDD set and subscriber structure.

Current 11-digit mobile subscribers must begin with `9`. For ten-digit national numbers, freeze the prefix rule with no discretionary “legacy range”: subscriber first digit `2`, `3`, `4`, or `5` is fixed and remains exact; first digit `6`, `7`, `8`, or `9` yields ordered candidates `[exact, DDD + '9' + subscriber]` without silently choosing a database family; first digit `0` or `1` is invalid. Keep candidate order stable and deduplicate it. Reject unsupported service/trunk shapes instead of broad `\D` stripping.

Test normalization precision, not only happy paths: equivalent formats have byte-identical canonical/limiter keys; explicit country + DDD 55 removes one `55`; subscriber prefixes `5`, `6`, `7`, `8`, and `9` each have a separate assertion proving the frozen boundary; `0` and `1` fail; too-short and too-long values fail; `00`, `10`, `20`, and `90` do not pass as DDDs; embedded/misplaced `+`, alphabetic text, tab, CR, LF, other control input, and empty strings fail; ASCII and non-breaking spaces succeed only as approved formatting. The client may reuse this module for obvious local guidance later, but server code remains authoritative.
  </action>
  <acceptance_criteria>
    - `src/lib/phone.test.ts` contains every required RESEARCH matrix row, separate subscriber-prefix `5/6/7/8/9` assertions, invalid `0/1`, explicit raw tab/CR/LF/control rejection, and allowed ASCII/NBSP formatting cases.
    - DDD 55 has separate mobile, fixed, and country+DDD55 regression cases.
    - Legacy input exposes both candidates deterministically and current/legacy equivalent inputs share the same normalized limiter key.
    - No branch strips `55` solely because a valid national number starts with those digits.
    - `normalizePhone` has no browser-global, React, database, or Convex import.
    - `npx vitest run src/lib/phone.test.ts` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/lib/phone.test.ts && ! grep -Eq "window|document|sessionStorage|from ['\\\"]convex" src/lib/phone.ts && npm run build</automated>
  </verify>
  <done>The isolated phone module canonicalizes Brazilian numbers precisely, preserves DDD 55, models ninth-digit ambiguity explicitly, rejects unsafe/malformed input, and passes the full table-driven suite.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Define RSVP schema, internal idempotent creation seam, and Convex integration harness</name>
  <read_first>
    - convex/schema.ts (empty Phase 1 stub; preserve its role but replace the empty schema)
    - convex/_generated/ (untracked generated output to preserve and regenerate only through tooling)
    - src/lib/phone.ts and src/lib/phone.test.ts (Task 2 canonical/candidate contract)
    - .planning/phases/03-rsvp/03-RESEARCH.md (§ Recommended Backend Architecture > Data model and Internal functions and fixtures)
    - .planning/phases/03-rsvp/03-VALIDATION.md (§ Wave 0 Requirements and RSVP-02 verification row)
    - .planning/phases/03-rsvp/03-PATTERNS.md (§ Convex boundary and client provider; § Test conventions)
  </read_first>
  <files>convex/schema.ts, convex/rsvpModel.ts, convex/rsvpInternal.ts, convex/rsvpTest.ts, convex/rsvps.test.ts, convex/_generated/*</files>
  <behavior>
    - Valid RSVP/family, guest, and session documents pass schema validators; invalid attendance literals and malformed required fields fail.
    - Running the guarded normal/zero/one/many-long internal fixture set twice produces the same four RSVPs and guest sets, not duplicate family/guest rows.
    - Equivalent phone forms are rejected as a second invitation by the indexed check inside the same mutation.
    - Guest rows carry an opaque `publicRef` distinct from Convex `_id`, unique within their RSVP, for the later public API.
  </behavior>
  <action>
Create `convex/rsvpModel.ts` for shared validators and bounded constants. Define attendance as exactly `pending | yes | no`, contact maximum 120 characters, a realistic maximum public patch/family size of 50, and session/token constants that plan 03-02 will reuse. Do not add user/password/role/account/admin fields.

Replace the empty schema with:

- `rsvps`: canonical `phone`, `displayName`, optional `contact`, `updatedAt`; index `by_phone`.
- `rsvpGuests`: `rsvpId`, opaque `publicRef`, `name`, `attendance`, `sortOrder`, optional `respondedAt`; indexes `by_rsvp`, `by_rsvp_sort`, and composite `by_rsvp_public_ref`.
- `rsvpSessions`: `tokenHash`, `rsvpId`, `expiresAt`, `createdAt`; indexes `by_token_hash` and `by_expires_at`.

Convex indexes are not unique constraints. In `convex/rsvpInternal.ts`, centralize an internal insert helper that normalizes the submitted phone, checks all equivalent candidates through `by_phone` inside the same mutation, rejects an existing logical invitation, creates opaque guest `publicRef` values, and then inserts. Expose only an `internalMutation` for deterministic development/test fixture setup. Re-running that fixture must identify the existing family and reconcile/return it without duplicating RSVP or guest rows. Do not export a public seed, self-registration, `createRsvp`, or list function.

Create `convex/rsvpTest.ts` as the one harness factory using `convexTest(schema, modules)` and register `@convex-dev/rate-limiter/test` on every instance. Start `convex/rsvps.test.ts` with real passing schema/fixture/uniqueness tests; this is not an empty placeholder for plan 03-02.

Define one guarded `internalMutation` named `ensureDemoFixtures` that creates four idempotent, non-public acceptance shapes through the same uniqueness helper:

- `normal`: three people with a mix of pending/yes/no;
- `zero`: an invitation with zero people for the invariant state;
- `one`: exactly one person;
- `many-long`: at least 12 people, including one deliberately long Portuguese display name and one long guest name.

Do not commit literal demo phones. Require `RSVP_ENABLE_DEMO_FIXTURES=development-only` and a server-only 32-byte-or-longer `RSVP_DEMO_SEED`; derive four stable valid canonical demo phones from a keyed SHA-256/HMAC-style derivation, and use non-personal names such as `Convite Demo Normal`. Re-running with the same seed returns the existing rows. The internal CLI result may return the labeled synthetic phone mapping needed for manual acceptance, but application logs and public functions must not emit it. The guard must fail closed when the flag/seed is absent, so production cannot seed accidentally.

Regenerate Convex types with tooling. Run `npx convex dev --once` at this schema/component boundary. If a connected deployment is unavailable, run the strongest local codegen/typecheck fallback possible, record the missing external smoke explicitly, and do not mark the phase itself complete until the real `dev --once` smoke has run. Never patch generated declarations by hand.

After the smoke, execute the fixtures concretely on the connected **development** deployment:

```bash
demo_seed="$(openssl rand -hex 32)"
npx convex env set RSVP_ENABLE_DEMO_FIXTURES development-only
npx convex env set RSVP_DEMO_SEED "$demo_seed"
unset demo_seed
npx convex run rsvpInternal:ensureDemoFixtures '{}'
```

Record the returned labeled synthetic phone mapping in the local execution summary/test notes only; never add it to committed source or public copy. Confirm a second identical `convex run` reports the same four logical fixtures and unchanged RSVP/guest counts.
  </action>
  <acceptance_criteria>
    - All three tables and all listed indexes exist with explicit validators.
    - Attendance is explicit on every guest and accepts only `pending`, `yes`, or `no`.
    - `rsvpGuests.publicRef` is distinct from `_id` and the public-ref lookup is always composite/scoped by `rsvpId`.
    - Fixture creation is `internalMutation`, idempotent, and uses `normalizePhone`; no public creation/list function exists.
    - Normal, zero, one, and many-long fixture shapes are all created by `ensureDemoFixtures`, are guarded by development-only env + server-only random seed, contain no real-person data or committed phone, and remain count-stable on a second CLI run.
    - Equivalent-format duplicate creation and repeated fixture setup are tested through `convex-test`.
    - The rate-limiter test adapter is registered by the shared harness.
    - `npx vitest run src/lib/phone.test.ts convex/rsvps.test.ts -t "schema|fixture|unique"` exits 0.
    - `npm test`, `npm run build`, and `npx convex dev --once` exit 0 before this plan is signed off; any unavailable connected smoke is recorded as an explicit outstanding gate rather than silently passed.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/lib/phone.test.ts convex/rsvps.test.ts -t "schema|fixture|unique" && npm test && npm run build && npx convex dev --once && git diff --check</automated>
  </verify>
  <done>The three-table schema, logical uniqueness invariant, opaque guest references, internal-only idempotent fixture seam, shared Convex test harness, and generated component/API types all pass local tests and the real Convex smoke.</done>
</task>

</tasks>

<threat_model>
## Security baseline

ASVS L1 is active. Any unresolved **high** threat below blocks completion of this plan.

## Assets

- Canonical phone-to-invitation association.
- Guest names, attendance state, and stable row integrity.
- Future session capability hashes and expiry metadata.
- Logical uniqueness of invitations and guests.
- Integrity of the test harness and official rate-limit component registration.

## Trust boundaries

| Boundary | Untrusted side | Trusted side |
|----------|----------------|--------------|
| Raw phone string -> `normalizePhone` | User-controlled formatting/text | Deterministic canonical/candidate result |
| Internal fixture/import args -> Convex mutation | Developer/test/import payload | Validated, unique RSVP/guest documents |
| npm/component packages -> application | External package contents | Exact-pinned lockfile and official component API |
| Convex generator -> `convex/_generated` | Generated artifacts | Application imports; no human edits |

## Threats, mitigation, and proof

| ID | Threat | Severity | Mitigation | Blocking proof |
|----|--------|----------|------------|----------------|
| T-03-01-A | Normalization confusion merges DDD 55 with country code or maps malformed input to an invitation. | high | Ordered parser, official DDD validation, explicit country evidence, legacy candidates instead of silent choice. | `src/lib/phone.test.ts` full DDD55/country/invalid/ambiguity matrix must pass. |
| T-03-01-B | Unknown input creates a new family, or a fixture seam becomes public/usable in production. | high | Creation exists only behind `internalMutation`; no public create/seed/list export; demo fixture additionally requires development-only flag plus server-only random seed and contains no committed raw phone. | Source-surface inspection, missing-env fail-closed test, idempotent four-shape tests, and CLI-only execution proof; plan 03-02 revalidates the eventual lookup boundary. |
| T-03-01-C | Equivalent phones or repeated fixture runs create duplicate families/guests. | high | Indexed candidate check and insert occur in one serializable mutation; fixture is idempotent; opaque guest refs are unique per invitation. | Convex tests assert one RSVP and stable guest count after duplicates/retries. |
| T-03-01-D | An invalid attendance/contact/session shape enters storage and later widens the public API. | high | Explicit schema validators and shared bounded model constants. | Valid/invalid schema integration tests and real `convex dev --once`. |
| T-03-01-E | Hand-edited/stale generated component types hide a missing limiter registration. | medium | Register official component, regenerate only with Convex tooling, inspect pre-existing untracked directory, real runtime smoke. | Generated output plus `npx convex dev --once`; frontend build alone is insufficient. |
| T-03-01-F | Dependency substitution introduces an unreviewed limiter/test package. | medium | Exact official versions from RESEARCH and npm lockfile; optional runtime peer installed only if required by the pinned contract. | Exact-version package assertion and clean npm install. |
</threat_model>

<verification>
- Fast task sampling never skips more than one task: Task 1 runs full existing tests/build, Task 2 runs phone tests/build, Task 3 runs both suites plus full tests/build/Convex smoke.
- `npx vitest run src/lib/phone.test.ts` proves RSVP-01 without a database.
- `npx vitest run convex/rsvps.test.ts -t "schema|fixture|unique"` proves the RSVP-02 foundation and P-03-02.
- `npm test && npm run build` passes at the wave boundary.
- `npx convex dev --once` validates schema, indexes, component registration, generated types, and Convex runtime compatibility.
- `git diff --check` reports no malformed planning or source diff.
</verification>

<success_criteria>
- Wave 0 is real and green: exact dependencies, component config, frontend + backend test discovery, and reusable Convex harness exist.
- Phone normalization is precise at all documented Brazilian edges and preserves DDD 55.
- `rsvps`, `rsvpGuests`, and `rsvpSessions` are validated and indexed for capability-scoped access.
- Internal creation enforces logical phone uniqueness and fixture idempotence; no public self-registration/list surface exists.
- The pre-existing untracked generated directory is preserved and updated only through Convex.
- All high threats in this foundation have green automated/source blocking proof; plan 03-02 revalidates the same prohibitions when the public boundary first exists.
</success_criteria>

<output>
Create `.planning/phases/03-rsvp/03-01-SUMMARY.md` when executed, recording exact dependency pins, phone normalization policy, schema/indexes, fixture identity, generated-file status, and the result of `npx convex dev --once`.
</output>
