---
phase: 03-rsvp
plan: 05
type: execute
wave: 4
depends_on: [03-04]
files_modified:
  - src/routes/Confirmar.tsx
  - src/components/rsvp/PhoneGate.tsx
  - src/components/rsvp/AttendanceGroup.tsx
  - src/components/rsvp/FamilyForm.tsx
  - src/components/rsvp/DiscardDialog.tsx
  - src/lib/rsvpDraft.ts
  - src/lib/rsvpDraft.test.ts
autonomous: true
requirements: [RSVP-03, RSVP-04, RSVP-05]

must_haves:
  truths:
    - "The UI-SPEC sign-off approved 2026-07-24 is implemented in full: visual system, copy, focus/accessibility, responsive layout, interaction state machine, explicit states, and all eight UI Considerations."
    - "`Usar outro telefone` clears immediately only when clean; dirty state opens the approved native modal with safe initial focus, Escape, focus return, and explicit destructive action."
    - "Restoring, empty gate, unlocking, local invalid, generic miss, lookup rate, lookup network, family loading, populated, zero/one/many, dirty partial, saving, complete attending, complete all-no, partial saved, save rate, save network, session expired, and deadline passed each have concrete rendering/recovery."
    - "Zero-person has exact invariant copy and no save; one-person uses normal full-width anatomy; many/long names expand the page naturally with no nested/horizontal scroll."
    - "Every person's native radio choices remain exact `Vai`, `Pendente`, `Não vai`; pending is neutral/selectable and all-no uses thankful copy. Color is supplemental to checked cue, text, border/fill, and textual counts."
    - "At 320–359px choices stack with 16px inset/24px card/8px shadow; 360–639px uses three equal choices; 640–1023px centers max 704px with 32px spacing; 1024px+ uses max 1120px, 0.8fr/1.2fr, min 560px card, and 64px gap."
    - "At 200% zoom/320px there is no horizontal scroll; all targets are 44px; long text wraps; no ellipsis/fixed submit/nested guest scroll; safe-area and reduced-motion behavior are correct."
    - "The literal deadline remains visible; development clock makes post-deadline helper executable while controls/saves stay enabled and backend date tests remain green."
    - "Development fixtures normal/zero/one/many-long, internal expired-session helper, rate-limit boundary behavior, and same-tab/incognito session behavior are exercised before sign-off."
    - "The visual save-throttle state is derived deterministically: a guarded internal action consumes exactly the 30 allowed saves for one fresh demo capability, the browser uses the exported storage contract `RSVP_CAPABILITY_STORAGE_KEY = 'sol40:rsvp-capability:v1'` and performs ordinal 31, and the UI proves `rate_limited`, rendered retry time, and unchanged dirty draft before teardown."
    - "Only `Não precisa criar conta` and `sem criar conta` may mention account creation; no affirmative cadastro/login/senha/admin copy/control/endpoint is present."
  artifacts:
    - src/components/rsvp/DiscardDialog.tsx
    - completed explicit-state rendering in Confirmar/PhoneGate/FamilyForm
    - completed responsive/accessibility/tone styling
    - full automated + manual validation record
  key_links:
    - "`DiscardDialog` -> dirty switch in FamilyForm/Confirmar -> session clear only after explicit destructive action."
    - "`ensureDemoFixtures` CLI output -> normal/zero/one/many-long browser matrix — no committed demo phone."
    - "`issueDemoSession state=expired` -> sessionStorage capability key -> exact expired restore path."
    - "`prepareSaveThrottleDemo` -> exactly 30 real no-op saves -> locally held token stored under `RSVP_CAPABILITY_STORAGE_KEY` (`sol40:rsvp-capability:v1`) -> browser ordinal 31 rate-limit UI -> `revokeDemoSession` teardown."
    - "`rsvpClock` dev key -> deadline-passed UI -> backend date-independent save tests."
  prohibitions:
    - id: P-03-01
      requirement_id: RSVP-04
      provenance: flagged-unverified
      statement: "Acesso por telefone MUST NOT virar conta/login persistente nem autenticar admin."
      allowed_copy: "Exactly `Não precisa criar conta` and `sem criar conta` are allowed negative assurances."
      verification: "Automated copy scan requires both and rejects affirmative account/login/password/admin UI; `/admin` remains unchanged."
    - id: P-03-02
      requirement_id: RSVP-04
      provenance: flagged-unverified
      statement: "Telefone desconhecido MUST NOT auto-cadastrar/criar família."
      verification: "Unknown manual/automated path remains generic gate; backend row counts unchanged."
    - id: P-03-03
      requirement_id: RSVP-03
      provenance: flagged-unverified
      statement: "Salvamento parcial MUST NOT sobrescrever pessoas omitidas nem criar linhas RSVP/hóspede duplicadas."
      verification: "Draft/backend suites plus partial/reopen manual flow assert omitted preservation and stable row counts."
    - id: P-03-04
      requirement_id: RSVP-03
      provenance: flagged-unverified
      statement: "30 de setembro MUST NOT bloquear salvamentos/edições posteriores."
      verification: "Dev clock browser state and backend before/on/after tests both save successfully."
    - id: P-03-05
      requirement_id: RSVP-04
      provenance: flagged-unverified
      statement: "Funções públicas MUST NOT expor telefone/contato/token/IDs brutos nem permitir enumeração de famílias."
      verification: "Browser DOM/URL/log inspection, generic miss, scoped contact only, and backend recursive forbidden-key/IDOR tests."
    - id: P-03-06
      requirement_id: RSVP-03
      provenance: flagged-unverified
      statement: "Interface MUST NOT constranger respostas `não vai` ou pendentes; tom neutro e acolhedor."
      verification: "Locked copy tests and manual tone review: pending no alert/error; all-no gratitude; partial success; no guilt language."
---

<objective>
Finish Phase 3 UI: native dirty-discard, exhaustive system/data states, approved responsive/accessibility/tone treatment, and reproducible browser validation using guarded development fixtures/clock/session helpers.

Purpose: 03-04 proves the normal path; this plan closes every UI-SPEC edge and Nyquist/manual gate before Phase 3 can be considered complete.

Output: fully conforming `/confirmar`, green automated/full runtime checks, and a recorded manual matrix across all fixture/state/viewport boundaries.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-rsvp/03-CONTEXT.md
@.planning/phases/03-rsvp/03-RESEARCH.md
@.planning/phases/03-rsvp/03-PATTERNS.md
@.planning/phases/03-rsvp/03-VALIDATION.md
@.planning/phases/03-rsvp/03-UI-SPEC.md
@.planning/phases/03-rsvp/03-04-SUMMARY.md
@src/routes/Confirmar.tsx
@src/components/rsvp/PhoneGate.tsx
@src/components/rsvp/AttendanceGroup.tsx
@src/components/rsvp/FamilyForm.tsx
@src/lib/rsvpDraft.ts
@src/lib/rsvpClock.ts
</context>

## Wave and dependencies

- **Wave:** 4 — edge/UI hardening and phase sign-off.
- **Depends on:** 03-04 complete normal route.
- **Completes:** RSVP-03, RSVP-04, RSVP-05 guest-facing acceptance.
- **Leaves for Phase 6:** admin auth/global CRUD/import/dashboard only.

## Artifacts this phase produces

| Symbol / file | Kind |
|---------------|------|
| `DiscardDialog` | new native modal |
| zero/one/many/long + all error/loading/saved states | completed route surfaces |
| approved 320/360/640/1024+ layout | completed responsive contract |
| focus/live-region/reduced-motion/tone treatment | completed accessibility contract |
| guarded fixture/session/clock manual matrix | final acceptance evidence |

## Probe assumptions (unresolved — flagged, not dismissed)

| Requirement | Probe result | Explicit fallback coverage |
|-------------|--------------|----------------------------|
| RSVP-03 | `unclassified unresolved` | all UI Considerations, explicit status values, zero/one/many/long, partial/all-no/complete, contact, responsive/a11y/tone. |
| RSVP-04 | `unclassified unresolved` | restoration/switch dialog/expiry/reopen/edit/same-tab/independent session and no stale scoped DOM. |
| RSVP-05 | `boundary unresolved`; `precision unresolved` | 03-02 N-1/N/N+1 + expiry/retry precision stay green; browser exercises throttled copy/re-enable/draft retention with no threshold disclosure. |

<tasks>

<task type="auto">
  <name>Task 1: Add native dirty-discard and exhaustive explicit state/data-shape rendering</name>
  <read_first>
    - src/routes/Confirmar.tsx and every existing RSVP component (03-04 normal path)
    - .planning/phases/03-rsvp/03-UI-SPEC.md (§ Interaction State Machine; § Explicit System States; § Using another phone)
    - src/lib/rsvpDraft.ts and tests (dirty/status/count model)
  </read_first>
  <files>src/routes/Confirmar.tsx, src/components/rsvp/PhoneGate.tsx, src/components/rsvp/FamilyForm.tsx, src/components/rsvp/DiscardDialog.tsx, src/lib/rsvpDraft.ts, src/lib/rsvpDraft.test.ts</files>
  <action>
Implement native `<dialog>` dirty switch exactly: approved heading/body/actions; safe initial focus; Escape/cancel; focus return; destructive action clears capability/snapshot/draft/status and opens empty gate. Clean switch remains immediate.

Complete every explicit state with exact copy/recovery: restoration, empty/unlocking/local-invalid/generic-miss/lookup-rate/network, family loading, zero/one/many, dirty partial, saving, three success branches, save rate/network, session expiry, deadline passed. Zero has no save; one avoids broken plurals; many/long expand page; rate/network retains input/draft; expiry removes scoped DOM. Extend pure tests for singular/plural pending copy and every locked success/tone branch without full markup snapshots.
  </action>
  <acceptance_criteria>
    - Dialog semantics/focus/copy/clear are exact and dirty data cannot be silently discarded.
    - Every explicit UI-SPEC state has visible treatment and recovery.
    - Zero/one/many/long behaviors are concrete; no generic blank/fallback.
    - Pending/all-no/partial copy remains neutral and approved.
    - Focused draft tests and build pass.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/lib/rsvpDraft.test.ts && npm run build && ! grep -R "dangerouslySetInnerHTML\\|console\\.log" src/routes/Confirmar.tsx src/components/rsvp</automated>
  </verify>
  <done>Dirty switching and all documented data/system states are explicit, recoverable, and tone-correct.</done>
</task>

<task type="auto">
  <name>Task 2: Apply approved responsive, visual, focus, and accessibility contract</name>
  <read_first>
    - .planning/phases/03-rsvp/03-UI-SPEC.md (Design System through Registry Safety; Responsive, Focus/Accessibility, UI Considerations, approved sign-off)
    - src/index.css (existing palette/focus/reduced-motion; add no duplicate palette)
    - src/components/ui/Card.tsx, Button.tsx, Field.tsx, Toast.tsx (reuse/override)
    - all RSVP route/components after Task 1
  </read_first>
  <files>src/routes/Confirmar.tsx, src/components/rsvp/PhoneGate.tsx, src/components/rsvp/AttendanceGroup.tsx, src/components/rsvp/FamilyForm.tsx, src/components/rsvp/DiscardDialog.tsx</files>
  <action>
Apply exact cream/card/sand/plum visual hierarchy, four typography roles/two weights, approved spacing, semantic RSVP colors, RSVP primary focus, and ordinary global focus. Implement viewport contracts at 320–359, 360–639, 640–1023, and 1024+ including card shadow/padding, choice stacking/equal row, max widths, desktop columns, name minimum, wrapping, safe area, and page-only scroll.

Verify native landmarks/form/fieldset/legend/radios/dialog, persistent labels, merged descriptions, alert vs polite status, no save focus jump, checked cue + text + color, 44px targets, 8px target separation where unsegmented, route/family focus, 200% zoom, reduced motion, no ellipsis/fixed bar/nested scroll. Use ordinary React text; scoped contact only after capability; no raw sensitive debug/URL/analytics.
  </action>
  <acceptance_criteria>
    - Every responsive/UI Consideration row maps to concrete classes/markup.
    - 320px/200% has no horizontal scroll and every control remains operable.
    - Native semantics, focus, live-region, checked cues, and targets meet UI-SPEC.
    - No new palette, UI library, icon package, animation, nested scroll, or fixed submit is added.
    - Only approved negative account phrases exist; no account/admin control/endpoint.
    - Full frontend tests/build pass.
  </acceptance_criteria>
  <verify>
    <automated>npm test && npm run build && grep -q "Não precisa criar conta" src/content/event.ts && grep -q "sem criar conta" src/content/event.ts && ! grep -R "dangerouslySetInnerHTML\\|localStorage" src/routes/Confirmar.tsx src/components/rsvp</automated>
    <human-check>Keyboard/screen-reader pass plus 320/360/640/1024+, 200% zoom, reduced-motion, long names, zero/one/many; confirm page-only scroll, wrapping, visible checked/focus, 44px targets and no coercive copy.</human-check>
  </verify>
  <done>The complete approved responsive/visual/accessibility contract is implemented across every RSVP surface.</done>
</task>

<task type="auto">
  <name>Task 3: Execute guarded fixtures, expired/deadline helpers, full Nyquist suite, and manual matrix</name>
  <read_first>
    - .planning/phases/03-rsvp/03-VALIDATION.md (all automated/manual rows)
    - .planning/phases/03-rsvp/03-UI-SPEC.md (Checker Sign-Off and explicit states)
    - .planning/phases/03-rsvp/03-01-SUMMARY.md (fixture seed/labels and CLI output location)
    - .planning/phases/03-rsvp/03-02-SUMMARY.md (public limits, expired-session helper)
    - .planning/phases/03-rsvp/03-04-SUMMARY.md (normal browser flow)
    - src/lib/rsvpClock.ts and src/lib/rsvpSession.ts (exact dev/storage keys)
  </read_first>
  <files>src/routes/Confirmar.tsx, src/components/rsvp/PhoneGate.tsx, src/components/rsvp/AttendanceGroup.tsx, src/components/rsvp/FamilyForm.tsx, src/components/rsvp/DiscardDialog.tsx, src/lib/rsvpDraft.test.ts</files>
  <action>
Run `npx convex dev --once`, then execute the guarded internal fixture command:

```bash
npx convex run rsvpInternal:ensureDemoFixtures '{}'
```

Use only the authenticated CLI result's labeled synthetic phones; do not copy them into source/planning. Run it twice and confirm the same normal/zero/one/many-long rows/counts.

Create an expired capability concretely:

```bash
npx convex run rsvpInternal:issueDemoSession '{"fixture":"normal","state":"expired"}'
```

Copy the returned synthetic `token` value into the existing capability sessionStorage key in browser devtools, reload `/confirmar`, verify exact expired notice/scoped DOM removal, then remove it. For post-deadline state set the documented dev clock key:

```js
sessionStorage.setItem('sol40:rsvp-dev-now:v1', '2026-10-01T00:00:00-03:00'); location.reload()
```

Verify helper text and still-enabled save, then remove the clock key. Exercise lookup throttling with repeated development fixture lookup.

Prepare the **save throttle visual state** deterministically in one terminal session. The browser setup/teardown commands below must use the exact exported `src/lib/rsvpSession.ts` contract `RSVP_CAPABILITY_STORAGE_KEY = 'sol40:rsvp-capability:v1'`; the literal is repeated here only because browser devtools cannot import the application source module. Generate the raw capability locally so it is never returned/logged by Convex, call the guarded internal action, and print a fully populated browser command:

```bash
rsvp_demo_token="$(node --input-type=module -e "import { randomBytes } from 'node:crypto'; process.stdout.write(randomBytes(32).toString('base64url'))")"
npx convex run rsvpInternal:prepareSaveThrottleDemo "{\"fixture\":\"normal\",\"token\":\"${rsvp_demo_token}\"}"
node --input-type=module -e 'const token = process.argv[1]; console.log(`sessionStorage.setItem(\"sol40:rsvp-capability:v1\", ${JSON.stringify(token)}); location.assign(\"/confirmar\")`)' "${rsvp_demo_token}"
```

The Convex command must return exactly `{ nMinusOne: 29, atLimit: 30, successfulCalls: 30, nextCallOrdinal: 31 }`, proving calls 29 (`N-1`) and 30 (`N`) succeeded; if not, stop rather than continuing with a non-deterministic state. Paste the printed JavaScript command verbatim into browser devtools. Once the normal family loads, change one attendance choice so the draft is visibly dirty, then click `Salvar respostas` immediately. That click is the session's **31st** (`N+1`) save call and must render the approved save-throttle message with a positive server-derived retry time while leaving the changed radio/contact and `Alterações ainda não salvas` state intact. Do not click a preparation save in the browser before this assertion.

After recording the throttled result, tear down both browser and server state:

```js
sessionStorage.removeItem('sol40:rsvp-capability:v1')
```

```bash
npx convex run rsvpInternal:revokeDemoSession "{\"token\":\"${rsvp_demo_token}\"}"
unset rsvp_demo_token
```

The prepare/revoke functions are internal-only and development-guarded; no public test endpoint, query parameter, committed token, family/contact output, or production bypass is permitted. Use browser offline mode separately for network/draft retention.

Complete and record the matrix: both entry points/direct refresh; valid/unknown/expired/throttled/network; same-tab reload vs independent/incognito; partial/reopen/edit/idempotent; clean/dirty dialog; normal/zero/one/many-long; deadline passed; keyboard/screen reader/focus/live; 320/360/640/1024+/200%/reduced motion; privacy/tone. Fix any defect within declared files and rerun narrow checks immediately.

Finish with full suite/build/real Convex smoke/diff check. Do not mark complete if any high threat or manual row fails.
  </action>
  <acceptance_criteria>
    - Fixture command succeeds twice idempotently and all four labeled shapes are exercised without committed raw phones.
    - Expired internal session and dev post-deadline clock paths are executed before manual sign-off.
    - Expired/throttle browser setup and teardown use the exact exported capability key contract `RSVP_CAPABILITY_STORAGE_KEY = 'sol40:rsvp-capability:v1'`; no alternate capability key is introduced.
    - `prepareSaveThrottleDemo` reports N-1=29 and N=30 saved with exactly 30 successful calls; the browser's immediate N+1=31 call displays rate-limited retry time and preserves the dirty draft; browser/server teardown commands succeed.
    - Backend N-1/N/N+1, expiry -1/at/+1, retry precision, token reuse, and invalid-token-global tests remain green.
    - Every manual matrix row is recorded pass; defects are fixed/retested.
    - `npm test`, `npm run build`, `npx convex dev --once`, and `git diff --check` exit 0.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/lib/rsvpDraft.test.ts src/lib/rsvpSession.test.ts src/lib/rsvpClock.test.ts convex/rsvps.test.ts && npm test && npm run build && npx convex dev --once && git diff --check</automated>
    <human-check>All fixture/session/deadline/rate/network/routing/edit/dialog/accessibility/responsive/privacy/tone rows described in this task are mandatory and recorded in 03-05-SUMMARY.md.</human-check>
  </verify>
  <done>All automated, real-runtime, guarded-fixture, and manual UI-SPEC/Nyquist evidence is green and recorded; Phase 3 is ready for verification.</done>
</task>

</tasks>

<threat_model>
## Security baseline

ASVS L1 is active; any failed high proof blocks Phase 3.

## Threat register

| ID | Threat | Severity | Mitigation | Blocking proof |
|----|--------|----------|------------|----------------|
| T-03-05-A | Expired/switched session leaves family/contact in DOM. | high | clear all scoped state before gate render. | expired helper browser check + backend expiry suite. |
| T-03-05-B | Dirty dialog loses answers without consent. | high | native modal, safe initial focus, explicit destructive action, focus return. | keyboard/manual dialog matrix. |
| T-03-05-C | Fixture seam seeds production or publishes demo access phones. | high | internal-only, development env guard, server seed-derived phones, CLI-only return, no committed literal. | missing-env tests, repeated CLI run, repo scan. |
| T-03-05-D | Post-deadline dev clock affects production/backend authorization. | high | DEV-only display clock; production ignore; backend no date branch. | clock production test + before/on/after backend tests. |
| T-03-05-E | Rate/network state loses draft, leaks thresholds, or cannot be reproduced from the verification instructions. | high | internal dev-only prepare action consumes exactly 30, browser owns call 31, retained state/integer retry/generic copy, explicit revoke teardown. | prepare counter assertion, visible 31st-call throttle/draft check, teardown, and backend boundary suite. |
| T-03-05-F | Responsive/accessibility failure blocks mobile/AT RSVP. | high | native semantics, exact breakpoints/targets/focus/live regions. | mandatory viewport/zoom/keyboard/screen-reader matrix. |
| T-03-05-G | UI coerces negative/pending answers or implies account/admin access. | high | exact approved tone and only two negative account assurances. | copy tests + manual tone/control review. |
</threat_model>

<verification>
- Task 1: focused state/tone tests + build.
- Task 2: full frontend tests/build + accessibility/responsive manual sample.
- Task 3: guarded fixture/session/clock execution, complete manual matrix, full tests/build/Convex smoke.
- Required backend filters include `token conflict`, `save global invalid token`, `expiry`, `rate`, `partial`, `privacy`.
- No final sign-off from build alone.
</verification>

<success_criteria>
- UI-SPEC approved sign-off is implemented across all states and viewports.
- Fixtures and helpers make zero/one/many-long, expired, throttled, network, and post-deadline states executable.
- Partial/reopen/edit stays idempotent and welcoming.
- No account/admin/self-registration, sensitive leak, deadline block, or raw-ID authorization exists.
- All high threats and automated/manual Nyquist gates pass.
</success_criteria>

<output>
Create `.planning/phases/03-rsvp/03-05-SUMMARY.md` when executed, including fixture idempotence (without raw phones), expired/deadline helper steps, all manual matrix rows, backend boundary filters, full commands, and residual phone-as-light-access risk.
</output>
