---
phase: 03-rsvp
plan: 04
type: execute
wave: 3
depends_on: [03-03]
files_modified:
  - src/App.tsx
  - src/routes/Confirmar.tsx
  - src/components/rsvp/PhoneGate.tsx
  - src/components/rsvp/AttendanceGroup.tsx
  - src/components/rsvp/FamilyForm.tsx
autonomous: true
requirements: [RSVP-03, RSVP-04, RSVP-05]

must_haves:
  truths:
    - "`/confirmar` is a declarative sibling route that uses shared Shell without countdown rail, one focused route `h1`, wordmark `/`, and absolute-home RSVP navigation; `/admin` and catch-all are unchanged."
    - "Phone gate and family form are separate stages. Restoration reads the versioned sessionStorage capability once before rendering a gate/family decision and never flashes a false not-found/gate state."
    - "A valid session opens one scoped family view; malformed/unknown/expired capability clears storage and every scoped datum before the exact expired notice returns focus to phone."
    - "Phone unlock retains typed input during retry, blocks duplicate submission, uses generic unknown/malformed copy, and stores/clears raw phone correctly. `token_conflict` generates a new capability and retries once only through the tested helper."
    - "The normal populated family path renders ordered people with native fieldset/legend/radios in exact `Vai`, `Pendente`, `Não vai` order, one optional shared contact, server counts, persistent clean/dirty/save status, and sparse save."
    - "Failed/rate-limited save retains draft/contact; successful save reconciles the returned/reactive server view and uses persistent approved message plus secondary Toast; session-expired save removes scoped DOM."
    - "Only the approved negative phrases `Não precisa criar conta` and `sem criar conta` mention accounts. No cadastro/login/senha/admin control, endpoint, or affirmative copy exists."
    - "The global/per-phone/save limiter UI consumes only server `retryAfterSeconds`, never bucket names/thresholds, and blocks duplicate interaction only for the active request/retry window."
  artifacts:
    - src/routes/Confirmar.tsx
    - src/components/rsvp/PhoneGate.tsx
    - src/components/rsvp/AttendanceGroup.tsx
    - src/components/rsvp/FamilyForm.tsx
  key_links:
    - "`src/App.tsx#/confirmar` -> `Confirmar` -> shared Shell/content foundation from 03-03."
    - "`Confirmar` session restore -> `api.rsvps.getCurrent` — token is the sole family authority."
    - "`PhoneGate` -> tested `unlockWithFreshCapability` -> `api.rsvps.unlockByPhone` — collision retries once with a distinct token."
    - "`FamilyForm` -> `rsvpDraft.buildSparseCommand` -> `api.rsvps.saveResponses` — no stale full snapshot."
  prohibitions:
    - id: P-03-01
      requirement_id: RSVP-04
      provenance: flagged-unverified
      statement: "Acesso por telefone MUST NOT virar conta/login persistente nem autenticar admin."
      allowed_copy: "Exactly `Não precisa criar conta` and `sem criar conta` are allowed negative assurances."
      verification: "Content tests require both; source scan rejects account/password/admin form/API and `/admin` remains unchanged."
    - id: P-03-02
      requirement_id: RSVP-04
      provenance: flagged-unverified
      statement: "Telefone desconhecido MUST NOT auto-cadastrar/criar família."
      verification: "Gate calls only `unlockByPhone`; generic miss stays in gate and no create/register input or mutation exists."
    - id: P-03-03
      requirement_id: RSVP-03
      provenance: flagged-unverified
      statement: "Salvamento parcial MUST NOT sobrescrever pessoas omitidas nem criar linhas RSVP/hóspede duplicadas."
      verification: "FamilyForm sends tested sparse command only; full backend idempotence suite runs in verify."
    - id: P-03-04
      requirement_id: RSVP-03
      provenance: flagged-unverified
      statement: "30 de setembro MUST NOT bloquear salvamentos/edições posteriores."
      verification: "Route renders deadline helper from display-only clock and never derives disabled/save authorization from date."
    - id: P-03-05
      requirement_id: RSVP-04
      provenance: flagged-unverified
      statement: "Funções públicas MUST NOT expor telefone/contato/token/IDs brutos nem permitir enumeração de famílias."
      verification: "UI consumes opaque guestRef and scoped view; no sensitive URL/log/debug text; own contact renders only after valid capability."
    - id: P-03-06
      requirement_id: RSVP-03
      provenance: flagged-unverified
      statement: "Interface MUST NOT constranger respostas `não vai` ou pendentes; tom neutro e acolhedor."
      verification: "Pending remains selectable/valid, all-no uses approved gratitude, and partial save succeeds."
---

<objective>
Implement the executable normal RSVP route: session restoration, phone unlock, one bounded token-collision retry, accessible family form, sparse save, and ordinary success/error/retry paths.

Purpose: This plan wires the stable backend and frontend contracts without absorbing the discard dialog, exhaustive data-shape edges, responsive polish, or final browser matrix owned by 03-05.

Output: `/confirmar` works end-to-end for a normal invitation and is ready for final state/visual hardening.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-rsvp/03-CONTEXT.md
@.planning/phases/03-rsvp/03-RESEARCH.md
@.planning/phases/03-rsvp/03-PATTERNS.md
@.planning/phases/03-rsvp/03-VALIDATION.md
@.planning/phases/03-rsvp/03-UI-SPEC.md
@.planning/phases/03-rsvp/03-03-SUMMARY.md
@src/App.tsx
@src/content/event.ts
@src/lib/rsvpDraft.ts
@src/lib/rsvpSession.ts
@src/lib/rsvpClock.ts
@convex/_generated/api.d.ts
</context>

## Wave and dependencies

- **Wave:** 3 — route and normal interaction path.
- **Depends on:** 03-03 content/primitives/draft/session/clock.
- **Unblocks:** 03-05 dialog, edge states, responsive/accessibility and final validation.

## Artifacts this phase produces

| Symbol / file | Kind |
|---------------|------|
| `/confirmar` router entry | new route |
| `Confirmar` | new state orchestrator |
| `PhoneGate` | new stage-one surface |
| `AttendanceGroup` | new native radio surface |
| `FamilyForm` | new stage-two normal-path surface |

## Probe assumptions (unresolved — flagged, not dismissed)

| Requirement | Probe result | Explicit fallback coverage |
|-------------|--------------|----------------------------|
| RSVP-03 | `unclassified unresolved` | normal family, yes/pending/no, partial subset, contact, clean/dirty/saving/success/retry paths. |
| RSVP-04 | `unclassified unresolved` | no-token/valid/malformed/unknown/expired restore, equivalent phone re-entry, one collision retry, and scoped DOM clear. |
| RSVP-05 | `boundary unresolved`; `precision unresolved` | Backend boundaries remain in 03-02; route uses integer retry only, request busy guards, one collision retry, and draft/input preservation. |

<tasks>

<task type="auto">
  <name>Task 1: Add `/confirmar`, session restoration, and the complete phone gate</name>
  <read_first>
    - src/App.tsx (add one sibling only)
    - src/content/event.ts (approved copy/nav/deadline)
    - src/lib/rsvpSession.ts and rsvpClock.ts (tested storage/retry/clock)
    - convex/_generated/api.d.ts and convex/rsvps.ts (actual unlock/get results)
    - .planning/phases/03-rsvp/03-UI-SPEC.md (§ `/confirmar` shell, Phone gate, Restoration, Unlocking, focus)
  </read_first>
  <files>src/App.tsx, src/routes/Confirmar.tsx, src/components/rsvp/PhoneGate.tsx</files>
  <action>
Add `/confirmar` as a sibling route without touching `/admin` or `*`. Compose the approved Shell/introduction/card; one route h1 gets focus after client navigation. Render literal deadline always and display-only late helper from `getRsvpNow`.

Read the versioned capability once before choosing a card. Missing token opens gate. Present token calls `getCurrent`; unresolved shows approved restoration card (`Reabrindo seu convite…`, decorative Sol, neutral skeletons, `aria-busy`, polite status). Valid view initializes route state. Null/malformed/expired clears token/scoped state and returns exact expired notice with phone focus. Provide a recoverable family-read network state without erasing a still-valid token or showing stale family data.

Implement PhoneGate exact copy/semantics: tel input, DDD hint, merged error description, generic miss, rate-limit `{tempo}`, connection retry, `Buscando…`, busy guard, privacy note. Generate a fresh capability and call the tested max-two-attempt helper. First `token_conflict` retries once with a distinct token; second conflict maps to generic connection failure, makes no third call, and stores nothing. Store token only with `unlocked`, clear raw phone after success, announce/focus family heading. Unknown never calls a create endpoint.
  </action>
  <acceptance_criteria>
    - `/confirmar` sibling exists and `/admin`/catch-all are byte-unchanged.
    - Restoration resolves before gate/family and clears all scoped state on invalid/expired.
    - Phone gate exact copy, labels, busy/error/retry/focus behavior are present.
    - One `token_conflict` retries with a new token; second stops after exactly two calls and no collided token is stored.
    - Both approved negative account phrases render; no affirmative account/login/password/admin flow exists.
    - Focused model/content/session/clock tests and build pass.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/content/event.test.ts src/lib/rsvpSession.test.ts src/lib/rsvpClock.test.ts && npm run build && ! grep -R "dangerouslySetInnerHTML\\|localStorage\\|console\\.log" src/routes/Confirmar.tsx src/components/rsvp/PhoneGate.tsx</automated>
  </verify>
  <done>Direct route, restoration, generic safe phone gate, and bounded collision retry work without account/self-registration behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Implement accessible normal family form and sparse save/reconcile</name>
  <read_first>
    - src/routes/Confirmar.tsx (Task 1 orchestration)
    - src/lib/rsvpDraft.ts and tests (only save-command source)
    - convex/_generated/api.d.ts and convex/rsvps.ts (save result/view)
    - src/components/ui/Card.tsx and Toast.tsx (reuse)
    - .planning/phases/03-rsvp/03-UI-SPEC.md (§ Family form, Per-person group, Shared contact, Editing, Saving)
  </read_first>
  <files>src/routes/Confirmar.tsx, src/components/rsvp/AttendanceGroup.tsx, src/components/rsvp/FamilyForm.tsx</files>
  <action>
Implement populated FamilyForm with greeting, server counts, form heading, ordered people, shared contact, persistent status, save action, privacy note, and a clean-only session switch action. While dirty, keep the switch action protected/not destructive; 03-05 replaces this interim protection with the required native dialog before Phase 3 sign-off.

AttendanceGroup uses visible fieldset/legend/native radios in exact Vai/Pendente/Não vai order, checked cue + text + semantic colors, full-label 44px targets, long-name wrapping, and group name `Presença de {name}`. Pending is explicit and never error/required.

Initialize tested snapshot/draft/dirty model. Send only sparse guest refs/contact command. Clean disables save/`Tudo salvo`; dirty enables/`Alterações ainda não salvas`; saving preserves draft and blocks duplicate submit. `saved` reconciles view, clears dirty, keeps focus, renders exact persistent success and secondary Toast. Rate/network keeps every choice/contact; session_expired clears token and all scoped DOM before gate. Use only server integer retry metadata, never thresholds/bucket names.
  </action>
  <acceptance_criteria>
    - Native accessible groups and exact choice order exist for every normal guest.
    - Shared contact is one optional invitation field with max 120 guidance and no email/phone mask.
    - Save payload comes exclusively from sparse model; pending and partial save are valid.
    - Success/rate/network/expiry behavior preserves or clears state exactly as specified.
    - Persistent status is primary; Toast is secondary.
    - Dirty switching cannot silently discard even before 03-05 dialog lands.
    - Frontend model tests, full backend suite, and build pass.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/lib/rsvpDraft.test.ts src/lib/rsvpSession.test.ts convex/rsvps.test.ts && npm test && npm run build && ! grep -R "dangerouslySetInnerHTML\\|localStorage\\|/admin" src/routes/Confirmar.tsx src/components/rsvp</automated>
    <human-check>Exercise one normal fixture: valid unlock, same-tab reload, change one person while another remains pending, contact set, save, retryable failure preserving draft, and re-entry/edit. Confirm no account/admin control and no raw sensitive value in URL/debug UI.</human-check>
  </verify>
  <done>The normal invitation can be opened and edited per person through accessible sparse/idempotent save with safe retry/session handling.</done>
</task>

</tasks>

<threat_model>
## Security baseline

ASVS L1 is active; all high threats block.

## Assets/boundaries

| Boundary | High risk | Mitigation/proof |
|----------|-----------|------------------|
| sessionStorage -> `getCurrent` | stale/forged capability leaves data visible | server validation; clear scoped state on null/expiry |
| `token_conflict` -> retry | multi-family capability or infinite retry | tested distinct fresh token, max two calls |
| controls -> save | IDOR/stale overwrite | opaque refs + sparse model + backend suite |
| server text -> DOM | XSS/token/contact leak | ordinary React text; no raw HTML/log/URL |

## Threat register

| ID | Threat | Severity | Mitigation | Blocking proof |
|----|--------|----------|------------|----------------|
| T-03-04-A | Collided token is stored/retried indefinitely. | high | only tested helper controls retry; store on unlocked only. | exact call/token/storage tests plus route review. |
| T-03-04-B | Expired/switch state leaves family/contact DOM. | high | clear capability/snapshot/draft/status before gate. | manual expiry plus source transition review. |
| T-03-04-C | Client submits stale full snapshot or foreign raw ID. | high | sparse opaque refs only; backend IDOR/idempotence tests. | draft + Convex suites. |
| T-03-04-D | Account/admin or public registration is implied/created. | high | only two negative phrases; unlock-only gate; `/admin` unchanged. | content/source tests. |
| T-03-04-E | Rate/network retry loses draft or spams mutations. | high | busy guard, integer retry, retained draft, server idempotence. | manual retry + backend boundary suite. |
</threat_model>

<verification>
- Task 1 samples content/session/clock + build before family work.
- Task 2 samples draft + full Convex suite + full test/build.
- Normal fixture browser check is required; exhaustive shapes/states wait for 03-05.
- `git diff --check` and source scans pass.
</verification>

<success_criteria>
- `/confirmar` normal path works end-to-end with session-only capability and one bounded collision retry.
- Phone remains light access, never account/admin/self-registration.
- Accessible per-person partial edits and contact save/reopen without duplicates.
- Retry/expiry preserve privacy and draft correctness.
- All high threats have green proof.
</success_criteria>

<output>
Create `.planning/phases/03-rsvp/03-04-SUMMARY.md` when executed, recording route states, collision attempts, normal fixture flow, sparse payload proof, and unchanged `/admin`.
</output>
