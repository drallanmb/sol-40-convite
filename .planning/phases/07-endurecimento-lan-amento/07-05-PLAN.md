---
phase: 07-endurecimento-lan-amento
plan: 05
type: execute
wave: 5
depends_on: [07-04]
files_modified:
  - .planning/phases/07-endurecimento-lan-amento/07-LAUNCH-CHECKLIST.md
  - .planning/phases/07-endurecimento-lan-amento/07-SMOKE.md
autonomous: false
requirements: [LAUNCH-03]

must_haves:
  truths:
    - "D-07: the public domain may be live, but invitation disclosure waits for the real-list review."
    - "D-15 through D-20: the owner may use CSV preview/partial import or manual family CRUD without automatic overwrite."
    - "D-24: a completed external production backup is revalidated before the first real import."
    - "This follow-up is independently resumable when the owner supplies the CSV; unavailable physical devices in Plan 07-06 cannot block it."
    - "Committed evidence contains aggregate counts, reviewer, timestamp and result only—never guest rows, names, phones, raw CSV or credentials."
  artifacts:
    - "07-LAUNCH-CHECKLIST Gate E aggregate sign-off"
    - "07-SMOKE real-list review evidence"
  key_links:
    - "completed backup checksum -> protected CSV preview/import -> ignored-row review -> owner sign-off -> invitation disclosure eligibility"
  prohibitions:
    - statement: "LAUNCH-03 MUST NOT send or authorize sending the invitation before backup and real-list review are signed."
      status: unverified
      flagged: true
    - statement: "LAUNCH-03 MUST NOT commit real family names, guest names, phones, rows, raw CSV, RSVP results, credentials or backup contents."
      status: unverified
      flagged: true
---

<objective>
Import and review the owner-supplied real guest list without coupling this checkpoint to the physical-device matrix.

Purpose: make invitation disclosure eligible only after recoverable, protected and privacy-preserving data review.

Output: backup revalidation, reviewed import/manual corrections, authorized sampling and aggregate-only Gate E sign-off.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/07-endurecimento-lan-amento/07-CONTEXT.md
@.planning/phases/07-endurecimento-lan-amento/07-VALIDATION.md
@.planning/phases/07-endurecimento-lan-amento/07-LAUNCH-CHECKLIST.md
@.planning/phases/07-endurecimento-lan-amento/07-SMOKE.md
@src/components/admin/AdminGuestImport.tsx
@src/components/admin/AdminGuests.tsx
</context>

## Decision coverage

| Decisions | Planned coverage |
|---|---|
| D-07 | Invitation disclosure remains blocked until this plan signs Gate E. |
| D-15–D-20 | Protected CSV preview/partial success and manual family CRUD support the unavailable-list workflow. |
| D-24 | The external backup checksum is revalidated immediately before import. |

## Planner assumptions and unresolved edge classification

The real CSV is not currently available. This plan is a separately resumable human-action follow-up after publication; it remains pending without blocking Plan 07-06 or the public domain. Supplying and confirming the intended CSV is the only prerequisite. The import is a costly data mutation and invitation distribution is a one-way social action; this plan performs neither messaging nor distribution.

<threat_model>
security_standard: OWASP ASVS
asvs_level: 1
block_on: high

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-07-DATA-03 | Information disclosure | real-list evidence | high | mitigate | Protected local preview, aggregate-only committed evidence, no PII in logs/screenshots/chat. |
| T-07-DATA-04 | Tampering | production guest list | high | mitigate | Revalidate backup, explicit owner confirmation, partial report review and no automatic overwrite. |

Any open or failed high-severity threat blocks Gate E.
</threat_model>

## Artifacts this phase produces

| Path / symbol | Result |
|---|---|
| `07-LAUNCH-CHECKLIST.md` Gate E | aggregate list/review/disclosure decision |
| `07-SMOKE.md` real-list row | timestamped aggregate verification without PII |

<tasks>

<task type="checkpoint:human-action">
  <name>Task 1: Import and review the real list before invitation disclosure</name>
  <reversibility>one-way</reversibility>
  <read_first>
    - .planning/phases/07-endurecimento-lan-amento/07-LAUNCH-CHECKLIST.md Gate E and backup evidence
    - .planning/phases/07-endurecimento-lan-amento/07-SMOKE.md production import-safe guidance
    - src/components/admin/AdminGuestImport.tsx final import/report behavior
    - src/components/admin/AdminGuests.tsx manual correction workflow
  </read_first>
  <files>.planning/phases/07-endurecimento-lan-amento/07-LAUNCH-CHECKLIST.md, .planning/phases/07-endurecimento-lan-amento/07-SMOKE.md</files>
  <action>
Pause until the owner supplies the real CSV and explicitly confirms it is the intended list; do not request its contents in chat or source control. Reconfirm the completed/downloaded backup timestamp and checksum before opening the importer.

Through authenticated `/admin`, preview the file, review normalized family/person totals and every ignored/conflicting row locally, then obtain explicit owner confirmation before “Importar válidos.” Correct skipped rows through a revised file or manual CRUD and never overwrite conflicts automatically.

Sample authorized families across imported groups, verify one owner-approved RSVP lookup without recording identity, remove fictitious/example data, and record only aggregate family/person/ignored counts, reviewer, timestamp and pass/fail. This task sends no invitation, email or WhatsApp message.
  </action>
  <acceptance_criteria>
    - Owner-supplied CSV and explicit confirmation exist before import.
    - External backup/checksum is revalidated before the first real write.
    - Every ignored/conflicting row is corrected or explicitly accepted without automatic overwrite.
    - Authorized sampling passes and no real identity is committed.
    - Gate E contains aggregates and sign-off only; no invitation is sent by the executor.
  </acceptance_criteria>
  <verify>
    <automated>npm test && npm run build && git diff --check</automated>
  </verify>
  <done>The real list is recoverably imported and reviewed, making invitation disclosure eligible without exposing personal data.</done>
</task>

</tasks>

<verification>
- `npm test && npm run build && git diff --check`
- Human evidence: owner confirmation, backup revalidation and aggregate-only Gate E sign-off.
</verification>

<success_criteria>
- Real guests are imported/reviewed through protected workflows with conflicts resolved or accepted.
- No PII or secret enters committed evidence.
- Invitation disclosure becomes eligible, but this plan sends nothing.
</success_criteria>
