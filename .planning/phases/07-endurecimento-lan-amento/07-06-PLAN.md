---
phase: 07-endurecimento-lan-amento
plan: 06
type: execute
wave: 5
depends_on: [07-04]
files_modified:
  - .planning/phases/07-endurecimento-lan-amento/07-DEVICE-MATRIX.md
  - .planning/phases/07-endurecimento-lan-amento/07-SMOKE.md
autonomous: false
requirements: [LAUNCH-01, LAUNCH-02]

must_haves:
  truths:
    - "D-09 through D-14: physical tests follow launch, record exact contexts, cover all public flows plus one mobile admin, and classify incidents by severity."
    - "Publication stays green while hardware rows are pending; LAUNCH-01 remains incomplete until direct physical evidence exists."
    - "This follow-up is independently resumable whenever hardware becomes available; the unavailable real CSV in Plan 07-05 cannot block it."
    - "Interrupted, parallel, emulated or inferred runs remain pending and cannot complete another matrix row."
  artifacts:
    - "07-DEVICE-MATRIX independent physical rows"
    - "07-SMOKE device-specific evidence"
  key_links:
    - "physical context -> owned matrix row -> direct journey evidence -> severity response -> LAUNCH-01 progress"
  prohibitions:
    - statement: "LAUNCH-01 MUST NOT classify emulator, simulator, desktop WebKit, interrupted work or another device's evidence as a physical pass."
      status: unverified
      flagged: true
    - statement: "LAUNCH-01/02 evidence MUST NOT commit private images, real guest identities, admin credentials or capabilities."
      status: unverified
      flagged: true
---

<objective>
Accumulate the required physical-device and mobile accessibility evidence independently after launch.

Purpose: validate real handoffs, codecs, WebViews, timezones and mobile-admin behavior without blocking publication or the separate real-list checkpoint.

Output: independently owned device rows with direct evidence, cleanup and severity disposition.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/07-endurecimento-lan-amento/07-CONTEXT.md
@.planning/phases/07-endurecimento-lan-amento/07-VALIDATION.md
@.planning/phases/07-endurecimento-lan-amento/07-DEVICE-MATRIX.md
@.planning/phases/07-endurecimento-lan-amento/07-SMOKE.md
@src/content/event.ts
@src/lib/imageProcessing.ts
@src/lib/wineWhatsApp.ts
@src/components/memories/PhotoPicker.tsx
@convex/uploadValidation.ts
</context>

## Decision coverage

| Decisions | Planned coverage |
|---|---|
| D-09–D-12 | Post-launch, independently resumable rows cover public journey and mobile admin without blocking publication. |
| D-13 | P0/P1 receives immediate incident response; P2/P3 disposition is recorded. |
| D-14, D-28 | Real iOS/Android browsers/WebViews, HEIC and changed timezones validate the fixed `-03:00` instant. |

## Planner assumptions and unresolved edge classification

Hardware is not currently available. This plan remains pending and can resume one row at a time after Plan 07-04, regardless of Plan 07-05 status. LAUNCH-01 cannot close until every required physical context has direct pass evidence; automated tests provide regression support only.

<threat_model>
security_standard: OWASP ASVS
asvs_level: 1
block_on: high

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-07-DEVICE-01 | Repudiation | physical matrix | high | mitigate | Row ownership, exact environment/timestamp and physical-only pass evidence. |
| T-07-DEVICE-02 | Information disclosure | device evidence | high | mitigate | Disposable synthetic data, cleanup and no private media/identity/credential capture. |

Any failed high-severity behavior triggers incident response; unavailable rows remain pending.
</threat_model>

## Artifacts this phase produces

| Path / symbol | Result |
|---|---|
| `07-DEVICE-MATRIX.md` | pending/running/pass/fail/interrupted rows with independent ownership |
| `07-SMOKE.md` | sanitized physical journey results and cleanup |

<tasks>

<task type="checkpoint:human-action">
  <name>Task 1: Execute physical-device rows when hardware becomes available</name>
  <reversibility>easy</reversibility>
  <read_first>
    - .planning/phases/07-endurecimento-lan-amento/07-DEVICE-MATRIX.md pending rows/severity/interruption rules
    - .planning/phases/07-endurecimento-lan-amento/07-SMOKE.md live journey cases
    - src/content/event.ts explicit -03:00 boundaries
    - src/lib/imageProcessing.ts HEIC decode/downscale/fallback contract
    - src/components/memories/PhotoPicker.tsx actionable fallback copy
    - convex/uploadValidation.ts server rejection of unconverted HEIC
    - src/lib/wineWhatsApp.ts wa.me target/message construction
  </read_first>
  <files>.planning/phases/07-endurecimento-lan-amento/07-DEVICE-MATRIX.md, .planning/phases/07-endurecimento-lan-amento/07-SMOKE.md</files>
  <action>
When a physical context becomes available, claim one independent row and record device model, OS, browser/app/version, network, time and evidence class. Parallel runners update only their own rows; interrupted work records the last completed subflow and remains pending/interrupted.

On iPhone Safari test invitation/countdown, RSVP save/edit, wa.me handoff/return and a newly created HEIC through preview/downscale/upload/moderation; require transmitted JPEG no larger than 5 MiB or an actionable JPEG fallback preserving the draft. Test a real iOS embedded WebView separately. On Android test Chrome and a real WebView/Custom Tab with the public journey and JPEG/WebP upload. On at least one phone test `/admin` login, manual CRUD/import preview, dialogs, virtual keyboard, safe area, 200% zoom, focus and long content.

Change a physical device timezone to America/Los_Angeles and Asia/Tokyo and confirm the event instant remains tied to `-03:00`. Use disposable synthetic data/media, delete it, classify P0–P3 and apply D-13. Leave LAUNCH-01 pending until every required row directly passes.
  </action>
  <acceptance_criteria>
    - Publication remains green while rows are pending, but LAUNCH-01 remains incomplete.
    - No emulator/simulator/desktop WebKit row is physical evidence.
    - Interrupted/parallel runs cannot overwrite or complete another row.
    - iPhone Safari/WebView, Android Chrome/WebView, HEIC, both timezone contexts and one mobile-admin context have direct evidence or remain pending.
    - Disposable content is cleaned and committed evidence has no private data.
    - P0/P1 receives immediate response; P2/P3 disposition is recorded.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:release && git diff --check</automated>
  </verify>
  <done>Physical evidence accumulates honestly; LAUNCH-01 closes only after all real required contexts pass.</done>
</task>

</tasks>

<verification>
- `npm run test:release && git diff --check`
- Human evidence: each required physical row with exact context, direct result and cleanup.
</verification>

<success_criteria>
- Required real-device contexts are independently evidenced or explicitly pending.
- No unavailable/interrupted/emulated run is falsely passed.
- P0/P1 findings receive immediate production response.
</success_criteria>
