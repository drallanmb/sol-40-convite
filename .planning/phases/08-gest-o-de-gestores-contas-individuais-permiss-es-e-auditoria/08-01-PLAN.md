---
phase: 08-gest-o-de-gestores-contas-individuais-permiss-es-e-auditoria
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - convex/schema.ts
  - convex/adminAccountModel.ts
  - convex/adminPassword.ts
  - convex/adminPasswordActions.ts
  - convex/adminAuditModel.ts
  - convex/adminSecurity.ts
  - convex/adminModel.ts
  - convex/admin.test.ts
autonomous: true
requirements:
  - ADMIN-01
  - ADMIN-03
  - ADMIN-04
  - ADMIN-05
  - ADMIN-06
must_haves:
  truths:
    - "Uma conta administrativa canônica por e-mail é a identidade primária, com exatamente owner, manager ou seller (D-01, D-07)."
    - "Toda porta protegida resolve no Convex uma conta ativa, sua versão de credencial e seu papel; o cliente não concede autorização (D-11)."
    - "Sessões individuais continuam capabilities opacas com expiração absoluta de sete dias e sem renovação (D-13)."
  artifacts:
    - "convex/adminAccountModel.ts define validadores, normalização, política de senha e matriz fixa de capacidades."
    - "convex/adminPassword.ts define política/parser puros; convex/adminPasswordActions.ts é o módulo Node exclusivo de KDF e contém somente action/internalAction."
    - "convex/adminAuditModel.ts define schema lógico, vocabulário mínimo, redaction/diff e appendAuditEvent para uso atômico desde a Wave 1."
    - "convex/adminSecurity.ts devolve AdminPrincipal e falha fechada."
  key_links:
    - "adminSessions.accountId -> adminAccounts._id -> AdminPrincipal."
    - "credentialVersion da sessão deve coincidir com a versão autoritativa da conta."
    - "Actions Node chamam finalizers Web-runtime; somente o finalizer altera credencial/sessão e chama appendAuditEvent na mesma transação."
  prohibitions:
    - "Persistir senha humana com SHA-256 rápido, texto claro ou envelope sem salt."
    - "Confiar em papel, estado ou accountId fornecido pelo navegador."
    - "Renovar expiresAt durante leitura ou atividade."
---

<objective>
Criar o núcleo seguro de contas, KDF e autorização que suporta toda a fase.

Purpose: substituir a identidade compartilhada por `adminAccount` sem quebrar, ainda, sessões legadas.
Output: schema aditivo, modelos puros, KDF Node isolada, guarda central e primitiva mínima de auditoria testadas.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-gest-o-de-gestores-contas-individuais-permiss-es-e-auditoria/08-CONTEXT.md
@.planning/phases/08-gest-o-de-gestores-contas-individuais-permiss-es-e-auditoria/08-RESEARCH.md
@.planning/phases/08-gest-o-de-gestores-contas-individuais-permiss-es-e-auditoria/08-PATTERNS.md
@convex/schema.ts
@convex/adminSecurity.ts
@convex/adminModel.ts
@convex/admin.test.ts
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Provar conta → sessão → principal em uma rota administrativa</name>
  <files>convex/schema.ts, convex/adminAccountModel.ts, convex/adminAuditModel.ts, convex/adminSecurity.ts, convex/adminModel.ts, convex/admin.test.ts</files>
  <read_first>convex/schema.ts, convex/adminSecurity.ts, convex/adminModel.ts, convex/adminOverview.ts, convex/admin.test.ts, 08-RESEARCH.md §§ Recommended Data Model e Authorization Architecture</read_first>
  <behavior>
    - Conta owner ativa + sessão vinculada e vigente acessa uma query real como AdminPrincipal.
    - Conta disabled, versão divergente, papel adulterado no argumento, token expirado e duplicata falham antes de qualquer dado.
    - Sessão sem accountId segue somente a variante legada explicitamente permitida antes do cutoff.
  </behavior>
  <action>Comece pelos testes vermelhos. Adicione `adminAccounts`, `adminAuthConfig` e `adminAuditEvents` com índices/validators explícitos e campos novos opcionais onde a compatibilidade de documentos existentes exigir. Modele `AdminRole`, `AdminAccountState`, `AdminPrincipal`, `normalizeAdminEmail`, `hasAdminCapability` e helpers `requireOperational`, `requireAnyAdmin`, `requireOwner`, `requireSelfOrOwner`. Crie também o vocabulário mínimo fechado de eventos, redaction deny-by-default, diff limitado e `appendAuditEvent(ctx, event)` Web-runtime, sem query/retention/UI ainda; prove que password/token/hash/link são rejeitados ou omitidos. Evolua `requireAdminSession` para reler conta/estado/role/version a cada chamada. A identidade primária passa a ser `adminAccount`; sessão sem `accountId` é variante temporária promovida pelo cutoff, não um segundo sistema permanente (D-01, D-07, D-11, D-13, D-29–D-31). Preserve a privacidade pré-auth e os contratos ADMIN-01–06.</action>
  <acceptance_criteria>O teste tracer percorre schema, sessão, conta, guarda e uma query real; todos os casos inválidos retornam envelope neutro sem dados, nenhuma leitura renova TTL e o helper mínimo de auditoria persiste somente payload redigido.</acceptance_criteria>
  <verify><automated>npm test -- --run convex/admin.test.ts</automated></verify>
  <done>O caminho individual funciona ponta a ponta e o legado só é aceito pela regra transitória explícita.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implementar política e envelope scrypt resistente a abuso</name>
  <files>convex/adminAccountModel.ts, convex/adminPassword.ts, convex/adminPasswordActions.ts, convex/admin.test.ts</files>
  <read_first>convex/adminAuth.ts, convex/adminSecurity.ts, convex/postImageDecoder.ts, convex/postInternal.ts, 08-RESEARCH.md §§ Password and Credential Architecture e Node action + transactional finalizer</read_first>
  <behavior>
    - Senhas NFC de 15–128 code points, com espaços/Unicode, passam; curtas, longas e contextuais comuns falham.
    - Hashes usam salt aleatório, `N=2^17,r=8,p=1`, derived key de 32 bytes e formato versionado.
    - Parser rejeita versão, parâmetros, base64 e custos fora da allowlist antes de chamar scrypt.
    - Verificação correta/incorreta usa comparação timing-safe e nunca retorna hash.
  </behavior>
  <action>Mantenha `adminPassword.ts` Web-runtime/puro para política, parser e tipos. Crie `adminPasswordActions.ts` com `"use node"` como módulo exclusivo, exportando somente `action`/`internalAction` que usam `crypto.scrypt`, `randomBytes` e `timingSafeEqual`; não coloque query, mutation ou helper importado por mutations nesse arquivo. Use maxmem explícito, envelope versionado, `hashAdminPassword`, `verifyAdminPassword` e rehash via action, enquanto `parsePasswordEnvelope`, `needsPasswordRehash` e `validateAdminPassword` permanecem puros. O SHA-256 existente permanece apenas para capabilities fortes. Faça erros públicos genéricos e mantenha detalhes somente em resultados internos seguros (D-01).</action>
  <acceptance_criteria>Duas hashes da mesma senha têm salts diferentes; vetores correto/incorreto passam; plaintext e senha nunca aparecem no documento, retorno ou snapshot.</acceptance_criteria>
  <verify><automated>npm test -- --run convex/admin.test.ts -t "password|scrypt|email"</automated></verify>
  <done>Senha humana possui KDF lenta versionada e política testável sem dependência nova.</done>
</task>

</tasks>

<threat_model>
ASVS target: L1. Nenhuma ameaça high pode permanecer aceita.

| Threat ID | STRIDE | Boundary/component | Severity | Disposition | Mitigation |
|---|---|---|---|---|---|
| T08-01-S | Spoofing | browser → Convex guard | high | mitigate | capability hash única, conta ativa relida e credentialVersion comparada |
| T08-01-E | Elevation | role/account args → API | high | mitigate | role vem somente de adminAccount e matriz é exercitada em endpoint real |
| T08-01-I | Information disclosure | senha → banco/log | high | mitigate | scrypt salgado, projeções explícitas e ausência de segredo em retornos |
| T08-01-D | Denial of service | envelope scrypt malicioso | high | mitigate | parser allowlisted, limites de tamanho e maxmem antes do KDF |
</threat_model>

<verification>
`npm test -- --run convex/admin.test.ts` e `npm run build`; confirmar que alterações de schema são aditivas e `_generated` não foi editado manualmente.
</verification>

<success_criteria>
- Todas as ameaças high do plano têm teste ou guarda concreta.
- E-mail canônico é único e a matriz fixa tem somente três papéis.
- Sessões inválidas ou contas não ativas não expõem dados das quatro áreas.
</success_criteria>

## Artifacts this phase produces

- `convex/adminAccountModel.ts`: `AdminRole`, `AdminAccountState`, `AdminPrincipal`, validadores, normalização e matriz.
- `convex/adminPassword.ts`: política, parser de envelope e decisão de rehash sem diretiva Node.
- `convex/adminPasswordActions.ts`: actions/internalActions Node exclusivas para hash/verificação scrypt.
- `convex/adminAuditModel.ts`: vocabulário, redaction/diff e `appendAuditEvent` mínimos disponíveis a todos os writers posteriores.
- `convex/schema.ts`: tabelas `adminAccounts`, `adminAuthConfig` e extensão aditiva de `adminSessions`.
- `convex/adminSecurity.ts`: guardas por principal/papel.

<output>Create `.planning/phases/08-gest-o-de-gestores-contas-individuais-permiss-es-e-auditoria/08-01-SUMMARY.md` when done.</output>
