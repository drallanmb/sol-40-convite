---
phase: 05-mural-de-mem-rias-modera-o
plan: 05
type: execute
wave: 4
depends_on: [05-04]
gap_closure: true
files_modified:
  - convex/schema.ts
  - convex/posts.ts
  - convex/posts.test.ts
  - convex/postInternal.ts
  - convex/crons.ts
  - convex/uploadValidation.ts
  - convex/uploadValidation.test.ts
  - convex/_generated/*
  - src/lib/memoryDraft.ts
  - src/lib/memoryDraft.test.ts
  - src/lib/memoryUploadAttempt.ts
  - src/lib/memoryUploadAttempt.test.ts
  - src/components/memories/MemoryForm.tsx
autonomous: true
requirements: [WALL-02, WALL-05]

must_haves:
  truths:
    - "`requestUpload` resolves a token collision through `postUploadReservations.by_token_hash` with a bounded indexed read; neither a fresh token nor denied anonymous traffic can trigger a growing table filter before the limiter (WR-01, WALL-05)."
    - "Terminal upload reservations have an explicit, bounded retention lifecycle. Accepted post media remains owned, rejected/expired orphan bytes remain deleted, old terminal rows are retired in bounded scheduled pages, and capability reuse is only possible after the documented retention window."
    - "Large historical reservation sets prove collision and non-collision behavior without changing limiter semantics: denial still produces no upload URL/reservation, and legitimate burst refill/no-lifetime-cap behavior remains intact."
    - "JPEG, PNG, and WebP are accepted server-side only after bounded format-aware structural validation, including coherent lengths/chunks and non-zero sane dimensions; a magic prefix by itself is never enough (WR-04, WALL-02)."
    - "Structurally valid tiny fixtures replace the old signature-prefixed zero buffers. Truncated payloads, impossible dimensions, missing terminal/image-data structures, inconsistent RIFF/chunk lengths, spoofed MIME, HEIC/HEIF, empty, and over-5-MiB inputs are rejected and never create a post."
    - "Every upload-stage network, abort, HTTP, and invalid-response failure invalidates the short-lived upload URL/reservation transport while preserving the selected file, processed blob, preview, author, and message; the next retry must call `requestUpload` and use a different reservation/upload URL (WR-02, WALL-02)."
    - "A photo claim freezes an immutable normalized author/message/photo snapshot plus deterministic fingerprint before the mutation. Ambiguous claim/status failures retain that snapshot, and retries always resolve the frozen claim rather than silently replacing it with later edits (WR-03)."
    - "When an old frozen claim becomes accepted, the reducer clears the draft only if the current draft fingerprint still matches the accepted snapshot. If the guest edited author/message meanwhile, the old acceptance is reported separately while the newer visible draft and preview stay ready for a fresh submission."
    - "The exact lost-response -> edit -> retry/status-accepted scenario proves that the persisted/confirmed snapshot is the one shown as accepted and that newer text is never cleared or described as received."
    - "The public approved-only projection and pending-first moderation boundary remain unchanged; the gap fix does not add admin APIs, public reservation state, client-side moderation filtering, or a visible lifetime submission quota."
    - "The populated-carousel, real JPEG/PNG/WebP browser interruption, and Safari iOS HEIC checks remain explicit pending human UAT; this code plan must not mark them passed without real evidence."
    - "Phase 4 shared changes are preserved: immediately re-read `convex/schema.ts`, `convex/crons.ts`, generated declarations, package files, and `git status`/targeted diffs before each shared edit or generation."
  artifacts:
    - convex/schema.ts
    - convex/posts.ts
    - convex/posts.test.ts
    - convex/postInternal.ts
    - convex/crons.ts
    - convex/uploadValidation.ts
    - convex/uploadValidation.test.ts
    - src/lib/memoryDraft.ts
    - src/lib/memoryDraft.test.ts
    - src/lib/memoryUploadAttempt.ts
    - src/lib/memoryUploadAttempt.test.ts
    - src/components/memories/MemoryForm.tsx
  key_links:
    - "`requestUpload` -> `by_token_hash` -> bounded collision result -> existing device/global limiter -> upload URL generation."
    - "`expireReservation`/accept/reject -> terminal timestamp -> bounded internal retirement sweep -> cron; posts keep their storage ownership independently of reservation-row retention."
    - "`validatePhoto` -> `validateImageBytes` -> JPEG/PNG/WebP structural parser -> `acceptPhoto` only after a real-image verdict."
    - "`uploadBlobWithProgress` error -> reducer transport invalidation -> retry intent `request_reservation` -> fresh `requestUpload`, while `draft.photo.processed` and preview remain."
    - "`claim_started` -> immutable snapshot/fingerprint stored with uploaded transport -> claim/status retry uses snapshot -> accepted reducer compares snapshot fingerprint with current draft before clearing."
    - "`MemoryForm` -> separate accepted confirmation when fingerprints differ -> current edited draft remains rendered and submit-ready."
  prohibitions:
    - id: P-05-GAP-01
      requirement_id: WALL-05
      provenance: verification-gap-WR-01
      statement: "Anonymous upload traffic MUST NOT force an unindexed or unbounded reservation-table scan before an abuse decision."
      verification: "Schema/source assertion requires `by_token_hash`; a large-history integration test exercises collision and non-collision denial paths; no tokenHash `.filter` remains."
    - id: P-05-GAP-02
      requirement_id: WALL-02
      provenance: verification-gap-WR-02
      statement: "Retry MUST NOT reuse a short-lived upload URL after upload-stage network, abort, HTTP, or invalid-response failure."
      verification: "Reducer/orchestration tests prove processed draft preservation, transport reset, and a second `requestUpload` call with a fresh reservation before upload."
    - id: P-05-GAP-03
      requirement_id: WALL-02
      provenance: verification-gap-WR-03
      statement: "An accepted response for an older immutable claim MUST NOT clear or claim acceptance of author/message edits made after an ambiguous response."
      verification: "Lost-response/edit/retry tests compare frozen fingerprint, persisted snapshot, confirmation content, and retained current draft."
    - id: P-05-GAP-04
      requirement_id: WALL-02
      provenance: verification-gap-WR-04
      statement: "A JPEG/PNG/WebP magic prefix MUST NOT be treated as proof that the uploaded bytes form a structurally valid image."
      verification: "Format-aware valid fixtures pass; truncated, impossible-dimension, missing-data/terminator, and inconsistent-length adversarial fixtures fail before post creation."
---

<objective>
Close the four blocking Phase 5 verification findings without widening the public surface or falsely completing manual UAT.

Purpose: make anonymous upload cost bounded, server validation establish a real JPEG/PNG/WebP structure, and retries converge truthfully across expired upload URLs and ambiguous claims.

Output: indexed/retained reservations, bounded structural media parsers, immutable client claim snapshots, fresh upload transport on retry, and adversarial regression coverage for WR-01 through WR-04.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-mural-de-mem-rias-modera-o/05-CONTEXT.md
@.planning/phases/05-mural-de-mem-rias-modera-o/05-RESEARCH.md
@.planning/phases/05-mural-de-mem-rias-modera-o/05-PATTERNS.md
@.planning/phases/05-mural-de-mem-rias-modera-o/05-VALIDATION.md
@.planning/phases/05-mural-de-mem-rias-modera-o/05-VERIFICATION.md
@.planning/phases/05-mural-de-mem-rias-modera-o/05-REVIEW.md
@.planning/phases/05-mural-de-mem-rias-modera-o/05-02-SUMMARY.md
@.planning/phases/05-mural-de-mem-rias-modera-o/05-03-SUMMARY.md
@.planning/phases/05-mural-de-mem-rias-modera-o/05-04-SUMMARY.md
@convex/schema.ts
@convex/posts.ts
@convex/postInternal.ts
@convex/crons.ts
@convex/uploadValidation.ts
@convex/posts.test.ts
@convex/uploadValidation.test.ts
@src/lib/memoryDraft.ts
@src/lib/memoryDraft.test.ts
@src/lib/uploadBlob.ts
@src/components/memories/MemoryForm.tsx
</context>

## Wave and dependencies

- **Wave:** 4 — verified gap closure after the complete Phase 5 implementation.
- **Depends on:** 05-04 and all summaries through 05-04.
- **Closes:** WR-01 and WALL-05; WR-02, WR-03, WR-04 and WALL-02.
- **Does not close by itself:** populated carousel interaction, real browser upload/interruption, or Safari iOS HEIC human UAT.
- **Parallel Phase 4 rule:** schema, cron registration, generated declarations, package files, and repository HEAD are shared. Re-read live files and targeted diffs immediately before edits/generation; never reset, stash, amend, or stage unrelated Phase 4 changes.

## Artifacts this gap plan produces

| Symbol / path | Kind |
|---------------|------|
| `postUploadReservations.by_token_hash` | bounded capability-collision index |
| terminal timestamp/index + internal bounded retirement sweep | reservation retention lifecycle |
| JPEG segment/SOF/SOS/EOI structural validator | server real-image gate |
| PNG IHDR/IDAT/IEND bounded chunk validator | server real-image gate |
| WebP RIFF/chunk/dimension validator | server real-image gate |
| valid/adversarial binary fixture helpers | structural validation tests |
| immutable `MemorySubmissionSnapshot` + deterministic fingerprint | truthful claim identity |
| pure retry/acceptance decision seam in `memoryUploadAttempt.ts` | orchestration regression harness |
| edited-draft-aware accepted confirmation | old claim success without losing new work |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bound reservation lookup and retire terminal capability rows safely</name>
  <read_first>
    - git status --short and targeted diffs for convex/schema.ts, convex/posts.ts, convex/postInternal.ts, convex/crons.ts, convex/posts.test.ts, convex/_generated
    - convex/schema.ts (live Phase 4 and Phase 5 tables/indexes)
    - convex/posts.ts requestUpload, submitPhotoMemory, getSubmissionStatus, limiter order, state transitions
    - convex/postInternal.ts expireReservation, acceptPhoto, rejectPhoto, sweep pagination/idempotency
    - convex/crons.ts (live registered jobs; preserve Phase 4 jobs)
    - convex/posts.test.ts limiter, collision, expiry, orphan, public-surface tests and harness
    - .planning/phases/05-mural-de-mem-rias-modera-o/05-VERIFICATION.md WR-01
  </read_first>
  <files>convex/schema.ts, convex/posts.ts, convex/posts.test.ts, convex/postInternal.ts, convex/crons.ts, convex/_generated/*</files>
  <behavior>
    - Collision lookup is an indexed `by_token_hash` read capped at one/two rows and contains no table `.filter` on `tokenHash`.
    - With at least 1,000 historical reservations, an existing token conflicts and a fresh token follows the normal limiter path; denial creates neither URL nor row.
    - Existing N-1/N/N+1, refill, concurrency, whole-second retry, and “send more after the burst window” behavior stays unchanged.
    - Accepted/rejected/expired states record a terminal time exactly once; repeated accept/reject/expire remains idempotent.
    - A scheduled internal sweep reads terminal candidates through an index in fixed-size pages, re-reads each row, deletes only rows past the documented retention cutoff, and reschedules until done.
    - Deleting an old accepted reservation never deletes or de-owns its post storage; rejected/expired orphan storage cleanup completes before its row is retired.
    - Rows that predate the terminal timestamp field receive a bounded compatibility path based on terminal state and expiry rather than remaining forever.
  </behavior>
  <action>
Write failing integration/source tests first for WR-01: seed a large historical set directly through the test harness, place a matching token near the end, prove collision and fresh-token denial semantics, and assert the production source uses `withIndex('by_token_hash')` with no tokenHash `.filter`. Add `.index('by_token_hash', ['tokenHash'])` to the live reservation table and replace the scan with a bounded indexed lookup. Do not move storage URL generation ahead of both existing limiter consumptions.

Define a named terminal-retention duration and page size. Add a terminal timestamp/index suitable for a bounded oldest-first sweep; set it when accept/reject/expire first reaches a terminal state without rewriting it on idempotent repeats. Add an internal-only retirement mutation and append its daily cron without replacing existing jobs. Before deleting a row, re-read it and require a terminal state older than the cutoff. For accepted rows, verify the referenced post owns the storage and delete only the reservation row. For rejected/expired rows, finish safe orphan deletion before row deletion. Include old-row compatibility, cutoff -1/0/+1, >page-size pagination, repeated sweep, accepted-media preservation, and no-public-export tests. Run Convex codegen only after re-reading live schema/generated diffs; never hand-edit generated output.
  </action>
  <acceptance_criteria>
    - WR-01 is directly covered by indexed-source and large-history behavior tests.
    - Anonymous denial work is bounded and denial returns no upload URL/insertion.
    - Terminal retention is explicit, indexed, paginated, idempotent, and preserves accepted media.
    - Existing limiter/collision/expiry/orphan/public-surface tests remain green.
    - Phase 4 schema/cron/generated changes remain intact.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- convex/posts.test.ts -t "historical|collision|terminal|retention|rate|limit|expiry|cleanup|public surface" && rg -n "by_token_hash" convex/schema.ts convex/posts.ts && ! rg -U "query\\('postUploadReservations'\\)[\\s\\S]{0,240}filter\\([\\s\\S]{0,160}tokenHash" convex/posts.ts && npx convex dev --once && git diff --check</automated>
  </verify>
  <done>Capability collision cost and terminal table growth are bounded without weakening the existing storage/limiter/moderation boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace signature-only acceptance with bounded structural image validation</name>
  <read_first>
    - convex/uploadValidation.ts detectImageType and validateImageBytes
    - convex/uploadValidation.test.ts current prefix fixtures and 5 MiB boundary cases
    - convex/postInternal.ts validatePhoto action and reject/accept mutations
    - convex/posts.test.ts accepted-format, spoof, metadata, duplicate-finalization, and cleanup cases
    - convex/postModel.ts exact supported MIME and size constants
    - src/lib/imageProcessing.ts output formats/dimension bounds so server checks accept real client output
    - .planning/phases/05-mural-de-mem-rias-modera-o/05-VERIFICATION.md WR-04
  </read_first>
  <files>convex/uploadValidation.ts, convex/uploadValidation.test.ts, convex/posts.test.ts, convex/postInternal.ts</files>
  <behavior>
    - JPEG walks bounded markers, rejects truncated/invalid segment lengths, requires a supported SOF with non-zero sane dimensions, a scan payload, and terminal EOI.
    - PNG requires exact signature, first IHDR length/type, valid supported IHDR fields and non-zero sane dimensions, at least one bounded IDAT, exactly formed chunks, and terminal zero-length IEND without trailing truncation.
    - WebP requires exact RIFF/WEBP form, coherent RIFF total length and padded chunk bounds, and a structurally valid VP8, VP8L, or VP8X dimension header with non-zero sane dimensions.
    - Parser work is linear and bounded by the already enforced 5 MiB byte limit; it performs no unsafe allocation from attacker-declared lengths.
    - MIME equality, HEIC/HEIF conversion rejection, empty/oversize handling, blob deletion, and pending-only post creation remain intact.
  </behavior>
  <action>
Write adversarial tests before implementation. Replace every supposed-valid prefix-only fixture—including the exact-5-MiB zero-filled JPEG—with small structurally valid JPEG/PNG/WebP builders or checked-in byte arrays that satisfy the client output contract. Add truncation at each header/length boundary, zero/impossible dimensions, missing JPEG SOF/SOS/EOI, malformed PNG IHDR/IDAT/IEND/order/trailing chunk, WebP RIFF-size/padding/chunk/dimension inconsistencies, MIME spoof, HEIC/HEIF, empty, and over-limit cases.

Implement format-specific bounded parsers in `uploadValidation.ts` and make `validateImageBytes` accept only their successful verdict. Keep it dependency-free unless a vetted Convex-compatible parser is already present; do not add a native decoder or pretend that magic-byte detection is decoding. Enforce a documented sane dimension ceiling at least as permissive as the client's 2560px output. Preserve safe stable public error codes; internal structural reasons need not be exposed. Extend the Convex integration cases so structurally valid examples create exactly one `pendente` post, malformed examples are deleted, and no malformed payload reaches `acceptPhoto`.
  </action>
  <acceptance_criteria>
    - WR-04's old prefix-only fixtures fail.
    - Real structural fixtures for all three supported formats pass, including a structurally valid exact-size boundary if that boundary is retained.
    - Adversarial lengths/dimensions/terminators/order cases fail before post creation and storage is safely deleted.
    - Parser complexity and allocations are bounded by the 5 MiB gate.
    - Existing MIME/HEIC/cleanup/idempotency/public-projection behavior remains green.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- convex/uploadValidation.test.ts convex/posts.test.ts -t "jpeg|png|webp|structur|truncat|dimension|mime|heic|5 MiB|invalid|pending post" && npm run build && git diff --check</automated>
  </verify>
  <done>Server acceptance now proves bounded JPEG/PNG/WebP structure instead of trusting a signature prefix.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Refresh failed upload transport and make ambiguous-claim success snapshot-aware</name>
  <read_first>
    - src/lib/memoryDraft.ts complete reducer/state/action/effect contracts
    - src/lib/memoryDraft.test.ts preservation, retry, accepted reset, and object-URL tests
    - src/lib/uploadBlob.ts and src/lib/memorySession.test.ts XHR result taxonomy
    - src/components/memories/MemoryForm.tsx complete reservation/upload/claim/status/render flow
    - src/components/memories/SubmissionSuccess.tsx accepted copy/action
    - convex/posts.ts submitPhotoMemory immutable reservation author/message behavior and getSubmissionStatus projection
    - convex/posts.test.ts duplicate/lost-response/idempotency expectations
    - .planning/phases/05-mural-de-mem-rias-modera-o/05-VERIFICATION.md WR-02 and WR-03
  </read_first>
  <files>src/lib/memoryDraft.ts, src/lib/memoryDraft.test.ts, src/lib/memoryUploadAttempt.ts, src/lib/memoryUploadAttempt.test.ts, src/components/memories/MemoryForm.tsx</files>
  <behavior>
    - Upload-stage `network_error`, `aborted`, `http_error`, and `invalid_response` preserve draft/processed blob/preview but set transport to `none`.
    - The next retry decision requests a fresh capability/reservation/upload URL before uploading; no branch can reuse the failed URL.
    - The old ambiguous reservation/blob is not client-deleted or rebound; existing expiry/orphan cleanup owns it.
    - First claim freezes normalized optional author, normalized message, and stable photo identity into an immutable snapshot/fingerprint stored alongside the uploaded transport.
    - Claim retry and status polling remain tied to that frozen snapshot; later field edits never mutate the claimed payload.
    - Acceptance with an unchanged fingerprint follows the existing clean success path: clear photo/message/transport, revoke preview, preserve author.
    - Acceptance with a changed current fingerprint preserves all newer draft fields, processed blob, and preview; clears only the resolved transport; reports the older snapshot as accepted separately; and leaves the form ready to submit the newer draft.
    - Text-only submission keeps its direct accepted behavior and one-memory-per-submit contract.
  </behavior>
  <action>
Write failing pure orchestration/reducer tests first. Add a small dependency-injected decision seam in `memoryUploadAttempt.ts` used by `MemoryForm`, so tests can spy on reservation/upload/claim calls without adding a DOM test framework. Cover every `uploadBlobWithProgress` error kind and prove attempt two calls `requestUpload` once, gets a different URL/reservation, reuses the processed blob, and never posts to URL one.

Add a canonical snapshot builder and deterministic fingerprint that cannot collide through delimiter ambiguity (for example, length-prefixed canonical fields plus the selected processed-photo identity; do not use `JSON.stringify` object-order accident or non-cryptographic random state). Capture it exactly once immediately before the first `submitPhotoMemory` call and retain it through claim network failure, delayed validation, and retry. Retry sends the snapshot's normalized author/message, not the mutable fields currently on screen.

Separate “accepted claim confirmation” from the mutable draft. On `accepted` from either the mutation or reactive status, pass the frozen snapshot into the reducer. If it equals the current draft fingerprint, preserve the established full success screen/author retention. If it differs, render an inline accepted confirmation whose displayed author/message are from the accepted snapshot, keep the edited form visible with its newer values/photo/processed blob, reset transport for a new reservation, and do not revoke its preview. Add the exact regression: claim A is committed but response is lost, guest changes message to B, retry/status resolves A, UI confirmation identifies A, backend/post fixture contains A, B remains in the form, and the next submit starts a fresh reservation for B. Do not expose capability/storage IDs in confirmation or logs.

Finally run all Phase 5 focused tests, full tests/build, live Convex generation/runtime check, public-function/projection scans, and diff validation. Record the three existing human UAT items as still pending unless actually executed with evidence.
  </action>
  <acceptance_criteria>
    - WR-02 has an orchestration test proving fresh `requestUpload`/URL on retry and complete draft/blob preservation.
    - WR-03 has the exact lost-response/edit/accepted regression proving persisted/confirmed A and retained editable B.
    - Unchanged accepted submissions retain the existing clean success experience; text-only behavior is not regressed.
    - Preview cleanup is exact: revoked only when the corresponding draft photo is actually cleared/replaced/unmounted.
    - All Phase 5 focused tests, full suite, build, Convex runtime, public-surface scan, and diff check pass.
    - Manual carousel/real-browser/HEIC items remain pending rather than being converted to automated passes.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- src/lib/memoryDraft.test.ts src/lib/memoryUploadAttempt.test.ts src/lib/memorySession.test.ts convex/posts.test.ts convex/uploadValidation.test.ts src/lib/imageProcessing.test.ts src/lib/stableVisitOrder.test.ts src/content/event.test.ts && npm test && npm run build && npx convex dev --once && ! rg "dangerouslySetInnerHTML|console\\.(log|info|debug).*capability|uploadUrl.*console" src/components/memories src/lib/memoryDraft.ts src/lib/memoryUploadAttempt.ts && git diff --check</automated>
    <human-check>Keep pending for final verification: populated carousel focus/swipe/reduced-motion/200% zoom; real JPEG/PNG/WebP upload with actual network interruption and one pending post; Safari iOS HEIC conversion or actionable fallback with retained draft.</human-check>
  </verify>
  <done>Upload retry always obtains fresh short-lived transport, ambiguous claims resolve against immutable content, and newer guest edits can no longer be cleared or falsely confirmed.</done>
</task>

</tasks>

<threat_model>
## Security baseline

ASVS level 1 is active. Blocking threshold is **high**.

## Assets and boundaries

| Boundary | High risk | Control |
|----------|-----------|---------|
| Anonymous request -> reservation table | uncharged growing scan / cost exhaustion | indexed bounded token lookup, limiter before URL/insert, terminal retention |
| Terminal reservation -> storage/post ownership | deleting accepted media or retaining orphan secrets forever | state/timestamp index, re-read, ownership checks, paginated retirement |
| Uploaded bytes -> pending post | signature-spoofed/truncated content accepted as image | bounded format-aware structure/dimension validation before accept |
| Short-lived upload URL -> retry | permanent stale-URL retry loop / duplicate orphan | invalidate URL transport, preserve blob, fresh reservation, expiry cleanup |
| Mutable form -> immutable backend claim | old persisted text falsely presented as current success | frozen canonical snapshot/fingerprint and acceptance comparison |
| Shared checkout -> Phase 4 | schema/cron/generated loss | live targeted re-read, additive patch, no reset/stash/amend |

## Threat register

| ID | Threat | Severity | Mitigation | Blocking proof |
|----|--------|----------|------------|----------------|
| T-05-05-A | Rotating anonymous tokens force an ever-growing pre-limit table scan. | high | `by_token_hash` bounded lookup plus terminal retention sweep. | Large-history collision/non-collision test and source prohibition on filter scan. |
| T-05-05-B | Retention deletes an accepted post's image or leaves rejected/expired blobs indefinitely. | high | terminal timestamps, state re-read, post/storage ownership checks, bounded idempotent pages. | Cutoff/pagination/repeat tests and accepted-media preservation. |
| T-05-05-C | Magic-prefixed arbitrary/truncated bytes become a pending/approved broken card. | high | JPEG/PNG/WebP structural length/chunk/dimension/data/terminator parsing. | Valid real structures plus adversarial integration fixtures; malformed storage deleted. |
| T-05-05-D | Retry reuses an expired upload URL forever or silently duplicates transport. | high | all upload errors invalidate transport; next attempt requests a fresh reservation; old orphan expires. | Dependency-injected two-attempt orchestration test for every upload error kind. |
| T-05-05-E | Lost claim response plus edit clears newer text as though it was persisted. | high | immutable normalized claim snapshot/fingerprint; accepted-vs-current comparison. | Exact A-committed/B-edited regression across reducer, orchestration, and backend fixture. |
| T-05-05-F | Snapshot/capability details leak into UI, URL, logs, or public query. | high | confirmation contains only accepted human content; secrets remain component memory; public API unchanged. | Public-export/projection tests and source/log scan. |
| T-05-05-G | Parallel Phase 4 work is overwritten during schema/codegen/cron edits. | high | immediate live re-read and additive merge; explicit target staging only. | Targeted diff audit, Phase 4/full tests, live Convex check. |

## Blocking rule

Any failed high-severity proof blocks completion of 05-05. Manual carousel, real-browser interruption, and Safari iOS HEIC remain visible human-verification debt, but they do not waive or replace WR-01 through WR-04 automated closure.
</threat_model>

<verification>
- Task 1 closes WR-01 with indexed-source, large-history, limiter, retention, expiry, storage-ownership, pagination, and public-surface tests.
- Task 2 closes WR-04 with structural positive/adversarial unit and end-to-end pending-post/storage-deletion tests.
- Task 3 closes WR-02/WR-03 with dependency-injected retry orchestration, immutable snapshot reducer scenarios, backend convergence evidence, full Phase 5 suite, build, and Convex runtime.
- Nyquist continuity holds: every implementation task begins with failing automated coverage and ends with focused verification.
- Final verification must re-run `.planning/phases/05-mural-de-mem-rias-modera-o/05-VERIFICATION.md` criteria. It may mark WALL-02/WALL-05 code gaps closed only after all four WR proofs pass.
- Human UAT remains pending: populated carousel interaction/accessibility/zoom, real browser formats/interruption/network inspection, and Safari iOS HEIC.
</verification>

<success_criteria>
- `requestUpload` has no unindexed/growing reservation scan and terminal reservation growth is bounded.
- Only structurally coherent JPEG/PNG/WebP bytes can reach a pending photo post.
- Upload-stage retry always refreshes short-lived transport while preserving the processed draft.
- Ambiguous claim acceptance is tied to an immutable content snapshot and never clears newer edits.
- Existing approved-only projection, pending-first moderation, rate-limit, no-lifetime-cap, cleanup, and text-only behavior stay green.
- Phase 4 concurrent work is preserved and all high ASVS L1 threats pass.
- Manual UAT debt remains explicit and truthful.
</success_criteria>

<output>
Create `05-05-SUMMARY.md` with explicit WR-01 through WR-04 closure evidence, schema/index and retention constants, parser invariants/fixture provenance, fresh-reservation retry call trace, lost-response A/B snapshot trace, exact automated/full/build/Convex results, Phase 4 preservation, and unchanged pending human UAT.
</output>
