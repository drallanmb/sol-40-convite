---
phase: 05-mural-de-mem-rias-modera-o
plan: 04
type: execute
wave: 3
depends_on: [05-03]
files_modified:
  - package.json
  - package-lock.json
  - src/lib/stableVisitOrder.ts
  - src/lib/stableVisitOrder.test.ts
  - src/hooks/useReducedMotion.ts
  - src/components/memories/MemoryCard.tsx
  - src/components/memories/MemoryCarousel.tsx
  - src/components/memories/MemoriesSection.tsx
  - src/content/event.ts
  - src/content/event.test.ts
  - src/routes/Home.tsx
  - convex/schema.ts
  - convex/_generated/*
autonomous: true
requirements: [WALL-01, WALL-02, WALL-03, WALL-04, WALL-05]

must_haves:
  truths:
    - "The home contains one anchorable Memories section after DressCode and before Shell's footer; inside it the approved carousel precedes the composer, and navigation derives from the shared section ID (D-05, D-06)."
    - "The section's query is `api.posts.listApproved`; React performs no moderation filter and never receives pending/hidden/private fields or their URLs (D-10, WALL-04)."
    - "Approved IDs receive cryptographic random ranks once per mounted visit; existing relative order stays stable across rerenders/reactive additions, the source array is never mutated, and no `sort(() => Math.random() - .5)` exists (D-11)."
    - "The carousel uses exact-pinned official Embla React and Autoplay 8.6.0 for drag/swipe/snaps/loop; it advances at about 7 seconds, has previous/next and pause/resume controls, stops after interaction/focus/hover, and does not autoplay under `prefers-reduced-motion` (D-12)."
    - "Carousel semantics follow WAI-ARIA: named region with `aria-roledescription=carrossel`, named slides with position/count, visible rotation control before moving content, 44px controls, keyboard focus, and no live announcement on automatic slide changes."
    - "Cards use one consistent moldura/height: image above and optional recado below; message-only card uses the same surface with centered typographic composition; missing author shows exact fallback from backend (D-02, D-13)."
    - "Empty/loading/error/one/few/many approved-memory states are concrete: form remains usable when album is empty or query fails; loop/autoplay disable when movement is meaningless."
    - "All message/author content renders as ordinary React text, images have contextual non-filename alt treatment, and no raw HTML or private metadata is read."
    - "Composer behavior from 05-03 remains intact: 280 counter, one photo, processing/progress/retry preservation, accepted `aguarda aprovação`, retained author, and no lifetime cap (D-01–D-09, D-14–D-17)."
    - "Phase 4 parallel changes to `package.json`, lockfile, `src/content/event.ts`, `src/routes/Home.tsx`, `convex/schema.ts`, and generated files are read immediately before edits, merged additively, and never overwritten/reformatted from a stale plan snapshot."
    - "The final gate includes focused/full tests, production build, `npx convex dev --once`, interactive carousel/upload smoke, and explicit Safari iOS HEIC result or a recorded outstanding LAUNCH-01 manual gate."
    - "Admin moderation remains Phase 6; telão/QR/Instagram remain v2 and no corresponding UI, route, query, or dependency enters this plan."
  artifacts:
    - package.json
    - package-lock.json
    - src/lib/stableVisitOrder.ts
    - src/lib/stableVisitOrder.test.ts
    - src/hooks/useReducedMotion.ts
    - src/components/memories/MemoryCard.tsx
    - src/components/memories/MemoryCarousel.tsx
    - src/components/memories/MemoriesSection.tsx
    - src/content/event.ts
    - src/content/event.test.ts
    - src/routes/Home.tsx
  key_links:
    - "`MemoriesSection` -> `api.posts.listApproved` -> `stableVisitOrder` -> `MemoryCarousel` — only approved projected views are ordered/rendered."
    - "`MemoryCarousel` -> Embla/Autoplay + `useReducedMotion` -> pause/arrow/drag/focus/hover controls — motion is controllable."
    - "`MemoryCard` -> public view fields only -> ordinary React text/image — no storage ID/status/capability path."
    - "`SECTION_IDS.memories` -> `NAV_LINKS` -> `MemoriesSection#id` -> `Home` position after DressCode — navigation and composition stay synchronized."
    - "`MemoriesSection` -> `MemoryForm` after carousel — D-06 and complete guest flow share one section."
  prohibitions:
    - id: P-05-01
      requirement_id: WALL-01
      provenance: flagged-unverified
      statement: "The mural MUST NOT silently turn an individual memory into a public multi-photo album, auto-published post, or admin/telão/Instagram feature."
      verification: "Final route/dependency/public API inventory and manual review show one-card composer, pending confirmation, and no deferred feature surface."
    - id: P-05-02
      requirement_id: WALL-04
      provenance: flagged-unverified
      statement: "A guest's unapproved contribution MUST NOT be exposed publicly, including its storage URL, moderation state, reservation metadata, or upload capability."
      verification: "Backend projection suite, client type/source scan, and browser network/DOM inspection confirm only approved minimal views."
    - id: P-05-03
      requirement_id: WALL-05
      provenance: flagged-unverified
      statement: "Abuse controls MUST NOT masquerade as guest identity or a visible lifetime submission quota; legitimate guests remain free to send more memories after the burst window."
      verification: "Final copy/UI review shows wait-only throttling, no submission counter/identity language, and another-memory flow after success."
---

<objective>
Complete the public memories fold with stable per-visit approved carousel, accessible controllable motion, final home/navigation integration, and end-to-end validation.

Purpose: connect the secure backend and resilient composer to the invitation without losing Phase 4 parallel work or weakening moderation at the presentation boundary.

Output: exact-pinned Embla carousel, stable-order helper, responsive memory cards/section, additive home/navigation integration, and a full automated/manual Phase 5 gate.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-mural-de-mem-rias-modera-o/05-CONTEXT.md
@.planning/phases/05-mural-de-mem-rias-modera-o/05-RESEARCH.md
@.planning/phases/05-mural-de-mem-rias-modera-o/05-PATTERNS.md
@.planning/phases/05-mural-de-mem-rias-modera-o/05-VALIDATION.md
@.planning/phases/05-mural-de-mem-rias-modera-o/05-03-SUMMARY.md
@src/routes/Home.tsx
@src/content/event.ts
@src/content/event.test.ts
@src/components/layout/Shell.tsx
@src/index.css
</context>

## Wave and dependencies

- **Wave:** 3 — public album and shared-file integration.
- **Depends on:** 05-03 complete composer and 05-02 approved-only query.
- **Completes:** WALL-01 through WALL-05 guest-facing acceptance; Phase 6 owns moderation actions.
- **Parallel Phase 4 rule:** package/lock, `event.ts`, `Home.tsx`, schema, and generated outputs are shared. Re-read each live file plus `git status`/targeted diff immediately before every patch/install/generation. Add only the Phase 5 delta.

## Artifacts this phase produces

| Symbol / path | Kind |
|---------------|------|
| exact `embla-carousel-react@8.6.0` | new runtime dependency |
| exact `embla-carousel-autoplay@8.6.0` | new runtime dependency |
| `StableVisitOrder`, `createStableVisitOrder`, `orderForVisit` | new stable random-rank helper |
| `useReducedMotion` | new media-query hook |
| `MemoryCard` | new consistent public memory card |
| `MemoryCarousel` | new accessible Embla carousel |
| `MemoriesSection` | new approved-album + composer home section |
| `SECTION_IDS.memories`, `NAV_LINKS` memory entry, memory copy constants | additive shared content symbols |
| `Home` MemoriesSection composition | additive route integration |
| `src/content/event.test.ts` Phase 5 anchor/order assertions | extended canonical-content suite |

## Probe assumptions (SPEC-less fallback — unresolved and flagged)

| Requirement | Probe result | Explicit handling |
|-------------|--------------|-------------------|
| WALL-01 | unclassified/manual | Final manual inventory validates one card/submission, pending-first, no admin/v2 expansion. |
| WALL-02 | concurrency/interruption/retry | All backend/reducer concurrency suites remain in final gate; browser smoke interrupts upload once and retries without duplicate card. |
| WALL-03 | unclassified/manual | Final copy/tone/plain-text review plus 280 boundary suite. |
| WALL-04 | unclassified/manual | Approved-only automated suite plus DOM/network inspection for pending/hidden/private fields. |
| WALL-05 | threshold ±1 | Backend N-1/N/N+1 suite included in final full run. |
| WALL-05 | precision/rounding | Retry ceiling tests included; browser confirms positive whole-second countdown/re-enable. |
| WALL-05 | parallel requests | Backend parallel suite plus UI busy-guard smoke; multi-tab remains server-authoritative. |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install exact Embla packages and implement stable visit ordering/reduced-motion primitives</name>
  <read_first>
    - package.json and package-lock.json (READ LIVE NOW; preserve Phase 4 dependencies and exact-pin policy)
    - git status --short and git diff -- package.json package-lock.json
    - .planning/phases/05-mural-de-mem-rias-modera-o/05-RESEARCH.md (package legitimacy, Pattern 5 and Pattern 6)
    - .planning/phases/05-mural-de-mem-rias-modera-o/05-VALIDATION.md (stable-order/reduced-motion verification)
    - src/hooks/useCountdown.ts (hook style/cleanup convention)
  </read_first>
  <files>package.json, package-lock.json, src/lib/stableVisitOrder.ts, src/lib/stableVisitOrder.test.ts, src/hooks/useReducedMotion.ts</files>
  <behavior>
    - Same IDs retain relative order for the mounted visit across rerender/query updates.
    - New IDs receive a rank once without moving existing IDs relative to each other; removed/readded policy is deterministic for the same helper instance.
    - Input arrays are never mutated and no random comparator is used.
    - Randomness uses `crypto.getRandomValues` with an injectable deterministic test source.
    - Reduced-motion hook reflects initial media query, subscribes to change, and removes listeners.
  </behavior>
  <action>
Re-read package files/diff, then install `embla-carousel-react@8.6.0` and `embla-carousel-autoplay@8.6.0` with `--save-exact`. Preserve every existing/parallel dependency and script; do not hand-edit lockfile.

Write stable-order tests first. Implement persistent random ranks in a class/helper held by the component for one mount. Sort a copied array by numeric/lexicographic cryptographic rank plus ID tiebreaker; never mutate query output and never rerank existing IDs. Implement SSR-safe `useReducedMotion` with modern and legacy listener compatibility only if current browser support requires it.
  </action>
  <acceptance_criteria>
    - Both Embla packages are exact `8.6.0` pins and Phase 4 package changes remain.
    - Ordering tests prove stability, reactive addition, no input mutation, deterministic test injection, and no random comparator.
    - Reduced motion subscribes/cleans up correctly.
    - Focused tests/build pass.
  </acceptance_criteria>
  <verify>
    <automated>node -e 'const p=require("./package.json"); process.exit(p.dependencies["embla-carousel-react"]==="8.6.0" && p.dependencies["embla-carousel-autoplay"]==="8.6.0" ? 0 : 1)' && npm test -- src/lib/stableVisitOrder.test.ts && ! grep -R "sort(.*Math\\.random" src/lib/stableVisitOrder.ts && npm run build</automated>
  </verify>
  <done>Official carousel dependencies and per-visit stable ordering/motion primitives are exact, tested, and merge-safe.</done>
</task>

<task type="auto">
  <name>Task 2: Build consistent cards, accessible carousel, and resilient section states</name>
  <read_first>
    - src/lib/stableVisitOrder.ts and src/hooks/useReducedMotion.ts
    - node_modules/embla-carousel-react/ and node_modules/embla-carousel-autoplay/ type declarations for installed 8.6.0 APIs
    - src/components/memories/MemoryForm.tsx and SubmissionSuccess.tsx (05-03 composer)
    - src/components/ui/Card.tsx and Button.tsx (canonical surface/controls)
    - src/index.css (existing focus/reduced-motion/design tokens)
    - .planning/phases/05-mural-de-mem-rias-modera-o/05-CONTEXT.md (D-06, D-10–D-13)
  </read_first>
  <files>src/components/memories/MemoryCard.tsx, src/components/memories/MemoryCarousel.tsx, src/components/memories/MemoriesSection.tsx</files>
  <action>
Implement `MemoryCard` from the `listApproved` return type only. Use canonical Card styling and a consistent responsive frame; image uses `object-contain`/covered reserved region as design dictates without cropping away the full photo, message below; text-only centers message in the same footprint. Render strings through JSX. Use a contextual generic alt such as `Memória enviada para Sol` because no author-provided alt exists; never use storage filename.

Implement Embla with approximately 7000ms autoplay, loop only when enough slides, drag enabled, `stopOnInteraction`, focus and hover stop, explicit pause/resume, previous/next buttons, disabled states, and reduced-motion initialization stopped. Interaction pauses rather than unexpectedly restarting. Use WAI carousel region/slide names and position labels, no auto-slide live region.

`MemoriesSection` queries only `api.posts.listApproved`, orders views once per visit, renders carousel before `MemoryForm`, and keeps the form available during loading/empty/query error. Empty copy invites the first memory without implying auto-publication. Query error affects album only, not submission. Ensure 1/few/many states do not create meaningless autoplay or broken loop.
  </action>
  <acceptance_criteria>
    - Cards exactly implement photo/message/both consistent moldura and plain-text rendering.
    - Approved query feeds ordering/carousel directly; no client status filter or private type exists.
    - Autoplay/control/focus/hover/drag/reduced-motion behavior and ARIA structure are concrete.
    - Empty/loading/error/one/few/many states preserve the composer.
    - Focused tests, backend approved-only suite, and build pass.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- src/lib/stableVisitOrder.test.ts convex/posts.test.ts -t "approved|projection" && npm run build && ! grep -R "dangerouslySetInnerHTML\\|\\.status\\|storageId\\|tokenHash" src/components/memories/MemoryCard.tsx src/components/memories/MemoryCarousel.tsx src/components/memories/MemoriesSection.tsx</automated>
    <human-check>Keyboard, arrows, swipe, pause/resume, focus/hover stop, reduced motion, 1/few/many, and text-only/photo-only/both cards; confirm form remains available in empty/error states.</human-check>
  </verify>
  <done>The album is an accessible, controllable, stable-per-visit approved carousel above the resilient composer.</done>
</task>

<task type="auto">
  <name>Task 3: Merge home/navigation integration and execute the complete Phase 5 gate</name>
  <read_first>
    - src/content/event.ts and src/content/event.test.ts (READ LIVE NOW; Phase 4 may have added gift IDs/nav/copy)
    - src/routes/Home.tsx (READ LIVE NOW; preserve Phase 4 section position/content)
    - convex/schema.ts and convex/_generated/ (READ LIVE NOW before final generation; preserve Phase 4)
    - package.json and package-lock.json plus targeted git diffs
    - src/components/memories/MemoriesSection.tsx
    - .planning/phases/05-mural-de-mem-rias-modera-o/05-VALIDATION.md (all automated/manual gates)
    - git status --short and git diff -- src/content/event.ts src/routes/Home.tsx convex/schema.ts package.json package-lock.json convex/_generated
  </read_first>
  <files>src/content/event.ts, src/content/event.test.ts, src/routes/Home.tsx, convex/schema.ts, convex/_generated/*</files>
  <action>
Immediately before each shared patch, re-read the live target and targeted diff. Add `SECTION_IDS.memories`, nav link/canonical memory copy, and content tests without replacing Phase 4 gift additions. Import and render `MemoriesSection` immediately after `DressCodeSection`; if Phase 4 has concurrently inserted a wine section, preserve it according to its locked context while still satisfying the user's exact D-05 boundary “after dress code and before footer” and record the resolved relative order in the summary. Do not reconstruct whole arrays/objects/routes from this plan snapshot.

Run all focused Phase 5 tests, full tests/build, then re-read schema/generated diff and run `npx convex dev --once`; never hand-edit generated output. Use the local app with the connected dev backend to smoke: album empty/error does not block form; message-only and JPEG/PNG/WebP photo submission; network interruption preserves draft and retry creates one post; success waits for accepted, preserves author, and says approval; approved fixture alone appears; pending/hidden and private metadata do not appear in DOM/network; carousel interaction/reduced-motion/320–1024px/200% zoom.

On Safari iOS, choose a HEIC photo and record conversion success or actionable fallback with retained text. If a real device is unavailable, do not mark that row automated or silently green: record it as the explicit LAUNCH-01 manual gate while all code-side fallback tests must still pass. No admin moderation is required until Phase 6.
  </action>
  <acceptance_criteria>
    - Shared files retain every live Phase 4/RSVP addition and add only Phase 5 content/composition.
    - Memory anchor/nav/section are synchronized and the carousel precedes form after dress code.
    - D-01 through D-17 each have an automated assertion, source inspection, or recorded manual result.
    - Backend N±1/precision/concurrency, upload validation/cleanup, approved projection, client reducer/image/order suites all pass.
    - `npm test`, `npm run build`, `npx convex dev --once`, and `git diff --check` exit 0.
    - Interactive upload/carousel/accessibility checks pass; HEIC iOS is either recorded pass/fallback or explicitly carried to LAUNCH-01, never falsely claimed.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- convex/posts.test.ts convex/uploadValidation.test.ts src/lib/imageProcessing.test.ts src/lib/memoryDraft.test.ts src/lib/memorySession.test.ts src/lib/stableVisitOrder.test.ts src/content/event.test.ts && npm test && npm run build && npx convex dev --once && git diff --check</automated>
    <human-check>Complete browser/device matrix from the action: D-01–D-17, approved-only DOM/network, retry/accepted-only success, controls/swipe/focus/reduced motion, 320/360/640/1024+/200%, and Safari iOS HEIC or explicit LAUNCH-01 carry.</human-check>
  </verify>
  <done>Phase 5 is integrated without clobbering Phase 4, all automated/runtime gates are green, and manual/HEIC evidence is explicit.</done>
</task>

</tasks>

<threat_model>
## Security baseline

ASVS level 1 is active. Blocking threshold is **high**.

## Assets/boundaries

| Boundary | High risk | Control |
|----------|-----------|---------|
| Approved query -> carousel DOM/network | pending/hidden/private metadata leak | backend projection only; client minimal type |
| Auto-rotating UI -> guest accessibility | loss of control/context | explicit rotation control, pause conditions, reduced motion |
| Shared worktree -> integration | Phase 4/RSVP data/code loss | immediate live re-read and additive merge |
| Composer -> backend/storage | abuse/spoof/retry races | retain all prior blocking suites in final gate |
| Guest content -> React | executable text/metadata leak | ordinary text, generic alt, no private fields |

## Threat register

| ID | Threat | Severity | Mitigation | Blocking proof |
|----|--------|----------|------------|----------------|
| T-05-04-A | Client filtering or broad query exposes pending/hidden URLs/private fields. | high | `listApproved` only, minimal inferred type, no client status logic. | Backend projection/url-spy tests plus source/DOM/network inspection. |
| T-05-04-B | Guest text executes as HTML or image metadata/filename leaks. | high | JSX text, generic alt, reencoded media, no private/raw filename field. | source scan and browser DOM review. |
| T-05-04-C | Autoplay removes user control or violates reduced motion. | high | visible pause first, focus/hover/interaction stop, media query disable. | stable-order tests and manual keyboard/motion matrix. |
| T-05-04-D | Final integration weakens upload limits/idempotency/cleanup. | high | full backend security suite and real Convex gate remain required. | focused/full tests and `convex dev --once`. |
| T-05-04-E | Parallel Phase 4 shared-file changes are lost. | high | immediate targeted re-read, additive patch/install, no stale whole-file rewrite. | targeted diff review, Phase 4 tests/full build, live schema smoke. |
| T-05-04-F | Rate-limit UI implies identity/lifetime exclusion or leaks thresholds. | high | wait-only generic copy, no total counter, server-derived retry. | manual copy review and boundary/refill suite. |
| T-05-04-G | HEIC is advertised as supported when the real device codec fails. | high | capability attempt, actionable fallback, explicit manual iOS gate. | adapter tests plus recorded Safari iOS result/carry. |
| T-05-04-H | Deferred admin/telão/QR/Instagram functionality expands public attack surface. | high | no route/control/API/dependency for deferred features. | final public route/function/dependency inventory. |
</threat_model>

<verification>
- Task 1 samples package/order primitives; Task 2 samples order + approved-only backend + build; Task 3 runs every phase test, full suite/build, real Convex, and diff check.
- Nyquist continuity holds: every implementation task has automated verification.
- Interactive upload/carousel/accessibility checks are mandatory.
- Safari iOS HEIC remains manual by design and is carried visibly if unavailable.
- API coverage detector is run on the final plan scope; Convex is internal platform, so no `COVERAGE.md` is created unless the typed detector explicitly returns `detected: true`.
</verification>

<success_criteria>
- The public home has the memories fold at the locked position with carousel before composer.
- Only approved minimal views enter a stable-per-visit, controllable, reduced-motion-safe carousel.
- Cards preserve consistent photo/text composition and plain-text safety.
- All composer/backend/rate/cleanup/concurrency contracts remain green end-to-end.
- Phase 4 shared-file work is preserved.
- All high ASVS L1 threats pass; manual HEIC state is truthfully recorded.
</success_criteria>

<output>
Create `05-04-SUMMARY.md` recording exact dependency pins, final home order/Phase 4 merge, carousel timing/control/ARIA behavior, D-01–D-17 evidence, automated/full/Convex results, interactive matrix, HEIC iOS outcome or LAUNCH-01 carry, and confirmation that admin/v2 stayed deferred.
</output>
