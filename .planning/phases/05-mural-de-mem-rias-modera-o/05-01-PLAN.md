---
phase: 05-mural-de-mem-rias-modera-o
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - convex/schema.ts
  - convex/postModel.ts
  - convex/postSecurity.ts
  - convex/postRateLimits.ts
  - convex/uploadValidation.ts
  - convex/postTest.ts
  - convex/posts.test.ts
  - convex/uploadValidation.test.ts
  - convex/_generated/*
autonomous: true
requirements: [WALL-01, WALL-02, WALL-03, WALL-04, WALL-05]

must_haves:
  truths:
    - "The Wave 0 harness exists before the public upload pipeline: deploy-safe Convex test registration, pure model/security/limiter/binary validation, and additive schema boundaries have executable tests; handler, finalization, approved-projection, concurrency, and cleanup groups are added beside their implementations in 05-02 (WALL-01–WALL-05)."
    - "`posts` represents exactly one memory containing a photo, a message, or both, and every public-created row begins as `pendente`; `aprovado` and `oculto` remain schema states for Phase 6 without exposing an admin writer here (D-01, D-03, WALL-01)."
    - "Omitted author is stored as `undefined`; the literal `De alguém que te ama` is presentation/projection fallback only. Author is bounded to 60 Unicode code points and message to 280 Unicode code points after CRLF normalization/trim (D-02, D-14, WALL-03)."
    - "The schema is additive and future-compatible: `by_status`, `by_storage_id`, and `by_upload_reservation` support approved-only public reads, Phase 6 moderation, ownership cleanup, and v2 display without implementing admin, telão, QR, or Instagram (WALL-01, WALL-04)."
    - "Final image policy is exact and shared: JPEG/PNG/WebP only, metadata and real signatures must agree, final server limit is 5 MiB with exactly-limit accepted and +1 rejected, and raw HEIC/HEIF is identified for a specific rejection path (D-15, D-16, WALL-02)."
    - "A 256-bit reservation capability is canonical unpadded base64url, SHA-256 hashed at rest, expires after exactly 24 hours, is single-reservation scoped, and never authenticates a person or admin."
    - "Rate limits are explicit integer policies: upload reservation device token bucket 10 per 10 minutes with capacity 4, upload global fixed window 300/hour, text device token bucket 20/hour capacity 5, text global fixed window 600/hour, and validation retry cooldown 15 seconds (D-17, WALL-05)."
    - "Limiter boundaries are tested at N-1, N, N+1; expiry/cooldown at threshold-1ms, exactly threshold, threshold+1ms; retry-after is a positive integer computed with ceiling from milliseconds; all applicable buckets are checked before any are consumed."
    - "Parallel reservation/finalization retries are modeled as one serializable state machine: one reservation can own one storage ID and create at most one post even under interruption, duplicate action delivery, or concurrent claims (WALL-02)."
    - "Invalid and orphaned blobs are never silently retained: known invalid blobs are deleted on rejection, reservation expiry owns cleanup after 24 hours, and a defensive sweep preserves any blob referenced by either a post or live reservation."
    - "Phase 4 parallelism is respected: `convex/schema.ts` and `convex/_generated/*` are read immediately before mutation/generation, additions are merged into the live worktree, and no RSVP, wine, or generated declaration is overwritten from a stale snapshot."
  artifacts:
    - convex/postModel.ts
    - convex/postSecurity.ts
    - convex/postRateLimits.ts
    - convex/uploadValidation.ts
    - convex/postTest.ts
    - convex/posts.test.ts
    - convex/uploadValidation.test.ts
    - convex/schema.ts
  key_links:
    - "`convex/postModel.ts` -> `convex/schema.ts`, future `convex/posts.ts`, and `convex/postInternal.ts` — one validator/limit contract governs schema and writers."
    - "`convex/uploadValidation.ts#detectImageType` -> `convex/uploadValidation.test.ts` and the internal action in plan 05-02 — client MIME is never authoritative."
    - "`convex/postRateLimits.ts` -> the already registered `components.rateLimiter` — no second component registration or custom counter table."
    - "`convex/postTest.ts` -> `convex/posts.test.ts` — every Phase 5 Convex test registers the official rate-limiter test adapter."
  prohibitions:
    - id: P-05-01
      requirement_id: WALL-01
      provenance: flagged-unverified
      statement: "The mural MUST NOT silently turn an individual memory into a public multi-photo album, auto-published post, or admin/telão/Instagram feature."
      verification: "Judgment review plus schema/public-surface inspection confirms one card per submission, initial `pendente`, and no admin/telão/QR/Instagram public function."
    - id: P-05-02
      requirement_id: WALL-04
      provenance: flagged-unverified
      statement: "A guest's unapproved contribution MUST NOT be exposed publicly, including its storage URL, moderation state, reservation metadata, or upload capability."
      verification: "Backend projection tests in plan 05-02 recursively reject private keys/URLs for pending and hidden fixtures."
    - id: P-05-03
      requirement_id: WALL-05
      provenance: flagged-unverified
      statement: "Abuse controls MUST NOT masquerade as guest identity or a visible lifetime submission quota; legitimate guests remain free to send more memories after the burst window."
      verification: "Policy/source review confirms random device key is described only as a fairness key, global breaker remains present, and no product-count ceiling exists."
---

<objective>
Create the tested Wave 0 domain and security foundation for the moderated memories feature before any anonymous upload handler exists.

Purpose: freeze the independently executable model, size/type/text/rate, capability, and schema boundaries first; the stateful reservation/concurrency/projection/cleanup tests land task-by-task with their real handlers in 05-02 so no task depends on unimplemented behavior.

Output: additive `posts`/reservation schema, shared model/security/limiter/sniffing modules, Convex harness, binary fixtures, and a focused test suite ready for the public pipeline.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-mural-de-mem-rias-modera-o/05-CONTEXT.md
@.planning/phases/05-mural-de-mem-rias-modera-o/05-RESEARCH.md
@.planning/phases/05-mural-de-mem-rias-modera-o/05-PATTERNS.md
@.planning/phases/05-mural-de-mem-rias-modera-o/05-VALIDATION.md
@convex/schema.ts
@convex/rsvpRateLimits.ts
@convex/rsvpSecurity.ts
@convex/rsvpTest.ts
@convex/rsvps.test.ts
</context>

## Wave and dependencies

- **Wave:** 0 — tests, contracts, and additive schema.
- **Depends on:** Phase 1 Convex foundation and Phase 3's official limiter/test harness.
- **Unblocks:** 05-02 public upload/text/query backend, then 05-03 composer and 05-04 carousel integration.
- **Parallel Phase 4 rule:** stop and re-read `git status`, `git diff -- convex/schema.ts convex/_generated`, and the complete live `convex/schema.ts` immediately before its patch and immediately before Convex generation.

## Artifacts this phase produces

| Symbol / path | Kind |
|---------------|------|
| `postStatusValidator`, `uploadStateValidator`, `mediaTypeValidator` | new Convex validators |
| `MESSAGE_MAX_LENGTH`, `AUTHOR_MAX_LENGTH`, `MAX_FINAL_IMAGE_BYTES`, `UPLOAD_RESERVATION_TTL_MS`, `VALIDATION_RETRY_MS` | new shared constants |
| `normalizeMemoryText`, `countUnicodeCodePoints` | new pure server-domain helpers |
| `posts`, `postUploadReservations` | new additive schema tables |
| `by_status`, `by_storage_id`, `by_upload_reservation`, `by_expires_at` | new indexes |
| `validatePostCapability`, `hashPostCapability`, `hashPostDeviceKey` | new capability helpers |
| `POST_RATE_LIMITS`, `postRateLimiter`, `toPostRetryAfterSeconds` | new rate-limit policy/helpers |
| `detectImageType`, `validateImageBytes`, binary fixture builders | new pure magic-byte validation |
| `makePostTest` | new Convex test harness factory |
| `convex/posts.test.ts`, `convex/uploadValidation.test.ts` | new Wave 0 suites |
| `convex/_generated/*` | regenerated tool-owned declarations; never hand-edited |

## Probe assumptions (SPEC-less fallback — unresolved and flagged)

| Requirement | Deterministic probe result | Explicit handling |
|-------------|----------------------------|-------------------|
| WALL-01 | `unclassified — review manually` | Assume the three valid shapes (photo, message, both), one-card cardinality, initial pending state, three moderation literals, future indexes, and no Phase 6/v2 surface. Manual source/schema review remains required. |
| WALL-02 | `concurrency — guarantee under interruption/parallel retry` | This plan freezes the state literals, capability, ownership indexes, cooldown constant, and idempotency invariants. Plan 05-02 adds the executable duplicate claim/action/finalize, stuck-processing, expiry, and parallel-race tests in the same tasks that implement those handlers. |
| WALL-03 | `unclassified — review manually` | Assume plain text only, author optional, at least one content field, 0/1/280/281 message matrix, 60/61 author matrix, Unicode/CRLF/control cases. Manual tone/content review remains required. |
| WALL-04 | `unclassified — review manually` | This plan creates the `by_status` schema seam only. Plan 05-02 Task 1 adds the executable approved-only projection tests with the real query; manual public-payload inspection remains required. |
| WALL-05 | `boundary exactly at threshold and ±1` | Test N-1/N/N+1 and window/cooldown -1ms/at/+1ms for every device/global/reservation bucket. |
| WALL-05 | `precision/rounding contract` | Retry milliseconds are converted with `Math.ceil(ms / 1000)` and clamped to a positive whole second; tests cover subsecond and exact-second values. |
| WALL-05 | `concurrency under parallel requests` | This plan tests policy definitions and retry precision only. Plan 05-02 Tasks 1–2 add parallel check/consume and no-URL-on-denial tests with the real mutations. |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Establish the deploy-safe Phase 5 harness smoke</name>
  <read_first>
    - .planning/phases/05-mural-de-mem-rias-modera-o/05-VALIDATION.md (Wave 0 gaps, per-task map, exact manual-only limits)
    - .planning/phases/05-mural-de-mem-rias-modera-o/05-RESEARCH.md (Test Architecture Details and minimum backend groups)
    - convex/rsvpTest.ts (deploy-safe dependency-injected harness pattern)
    - convex/rsvps.test.ts (test-only `import.meta.glob`, fake timer, limiter registration conventions)
    - vite.config.ts (confirm both `src` and `convex` tests are already included; do not change without need)
  </read_first>
  <files>convex/postTest.ts, convex/posts.test.ts</files>
  <behavior>
    - `makePostTest` can instantiate the current live schema and register the official rate-limiter test adapter without importing test-only module discovery into deploy code.
    - `posts.test.ts` contains a green harness/component smoke that exercises only infrastructure created in this task.
    - No public-handler, post schema, finalization, cleanup, concurrency, or public-projection test is authored before the corresponding implementation task.
  </behavior>
  <action>
Create the deploy-safe `makePostTest` factory using injected `convexTest`, modules, and official `rateLimiterTest.register`, following Phase 3 without importing `import.meta` into deploy code.

Author a minimal `posts.test.ts` harness smoke using the existing live schema and official limiter test adapter. It must be meaningful (the instance can execute a trivial `t.run` and the component is registered) but must not import planned `postModel`, `posts`, `postInternal`, new schema tables, or other symbols that do not exist yet. Do not create `uploadValidation.test.ts` until Task 2 creates its real pure implementation. Do not commit `.skip`, `.todo`, placeholder assertions, or mock public-handler behavior.
  </action>
  <acceptance_criteria>
    - The harness registers the existing official limiter component and has no test-only import in a deploy module.
    - The deploy-safe factory and official limiter adapter are exercised by a non-skipped smoke test.
    - No test imports or asserts behavior scheduled for Task 2, Task 3, or plan 05-02.
    - `npm test -- convex/posts.test.ts` is green at this task boundary.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- convex/posts.test.ts && ! grep -En "\\.(skip|todo)|it\\.only|test\\.only" convex/posts.test.ts && git diff --check</automated>
  </verify>
  <done>The deploy-safe Phase 5 harness and official component registration have a real green smoke without anticipating unimplemented domain or public-handler behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement bounded text, capability, limiter, and real-byte validation primitives</name>
  <read_first>
    - convex/posts.test.ts (Task 1 harness smoke; extend only with pure tests supported by this task)
    - .planning/phases/05-mural-de-mem-rias-modera-o/05-PATTERNS.md (domain constants, capability shape, rate-limit sequence, magic-byte rules)
    - convex/rsvpSecurity.ts (Web Crypto/base64url/hash/retry conventions; do not couple post types into RSVP)
    - convex/rsvpRateLimits.ts (single registered component usage)
    - /Users/allanmesquitabrito/Documents/Site Sol 40 anos/sol-40-integrado/lib/upload-core.mjs (signature reference only)
    - /Users/allanmesquitabrito/Documents/Site Sol 40 anos/sol-40-integrado/tests/upload-core.test.mjs (historical adversarial byte cases)
  </read_first>
  <files>convex/postModel.ts, convex/postSecurity.ts, convex/postRateLimits.ts, convex/uploadValidation.ts, convex/posts.test.ts, convex/uploadValidation.test.ts</files>
  <action>
Implement the constants and validators listed in Artifacts. Normalize CRLF to LF, trim, reject C0/C1 controls except newline/tab, count Unicode code points with `[...value].length`, keep omitted author undefined, and distinguish whether a future storage ID satisfies the at-least-one-content invariant. Treat all text as plain text; add no HTML parser/sanitizer.

Implement post-local capability/device-key helpers using Web Crypto and canonical 32-byte unpadded base64url validation. Hash with purpose-separated prefixes and never persist raw keys. Do not edit RSVP security unless a truly generic extraction preserves Phase 3 byte-for-byte behavior and tests; prefer isolated post helpers.

Define the exact rate policies from must_haves using the already registered component. Export a retry conversion that takes millisecond precision, uses ceiling, returns a positive integer, and never discloses thresholds in user-facing text. Tests in this task cover exact policy constants and retry precision only; parallel check/consume behavior is added with the real handlers in 05-02.

Implement pure byte sniffing for JPEG, PNG, WebP, HEIC/HEIF recognition, truncation, mismatch, and size. JPEG/PNG/WebP may be accepted only when declared storage MIME and detected bytes agree and size is `<= 5 * 1024 * 1024`; raw HEIC is a typed rejection because conversion belongs in the browser when supported.

Add only the pure tests now supported by these modules: text 0/1/280/281, Unicode/CRLF/controls, author 60/61, canonical/malformed capability and purpose-separated hashes, exact limiter policy constants and retry ceiling, and the binary matrix. Do not add mutation, storage, scheduler, approved-query, cleanup, or parallel-request tests yet; plan 05-02 owns those alongside the real handlers.
  </action>
  <acceptance_criteria>
    - Text/code-point normalization is identical for photo and text paths and passes 0/1/280/281 plus Unicode/control cases.
    - Raw capability/device keys are never returned by hash helpers or embedded in limiter keys.
    - Limiter policies use the existing component registration and exact assumed values.
    - Retry rounding tests prove subsecond ceiling and exact-second behavior.
    - Magic-byte validation rejects spoofed/truncated/mismatched/oversized input and accepts the three required formats at the exact size limit.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- convex/posts.test.ts convex/uploadValidation.test.ts && ! grep -R "dangerouslySetInnerHTML\\|Math\\.random" convex/postModel.ts convex/postSecurity.ts convex/postRateLimits.ts convex/uploadValidation.ts && npm run build</automated>
  </verify>
  <done>All pure security/domain primitives are bounded, exact at edges, and green before public mutations consume them.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Merge the additive post/reservation schema and regenerate safely</name>
  <read_first>
    - convex/schema.ts (READ THE LIVE FILE NOW; Phase 4 may have added `wines` since this plan was authored)
    - convex/postModel.ts (Task 2 validators)
    - convex/posts.test.ts (schema/cross-field expectations)
    - convex/_generated/ (inspect current generated state; never hand edit)
    - git status --short and git diff -- convex/schema.ts convex/_generated (parallel Phase 4 reconciliation)
  </read_first>
  <files>convex/schema.ts, convex/posts.test.ts, convex/_generated/*</files>
  <action>
Immediately before patching, re-read the complete live `convex/schema.ts` and current diff. Add `posts` and `postUploadReservations` to the existing `defineSchema` object without replacing, reordering away, or weakening any RSVP or Phase 4 wine tables/indexes. Use the model validators and the exact indexes in must_haves. Extend `posts.test.ts` now with schema-only valid/invalid document cases and index availability. Schema validators cover field shapes; tests and later centralized writers enforce cross-field groups and initial `pendente`.

Run the schema-focused suite. Then re-read `git status`/diff again before `npx convex dev --once`. Generated files are tool output: never patch or copy them. If another phase changed schema while this task ran, merge its live additions and rerun rather than restoring a snapshot.
  </action>
  <acceptance_criteria>
    - Live RSVP tables/indexes and any live Phase 4 wine additions remain intact.
    - Both new tables use explicit validators and all required indexes.
    - Invalid status/source/media shapes fail; valid photo/message/both documents pass.
    - Generated declarations come only from Convex tooling.
    - Focused tests, full tests/build, real Convex smoke, and diff check pass.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- convex/posts.test.ts convex/uploadValidation.test.ts && npm test && npm run build && npx convex dev --once && git diff --check</automated>
  </verify>
  <done>The live schema contains additive Phase 5 tables without losing parallel work, and tests plus the real backend accept the generated model.</done>
</task>

</tasks>

<threat_model>
## Security baseline

ASVS level 1 is active. Blocking threshold is **high**: any failed or unproved high-severity row blocks plan completion.

## Assets and trust boundaries

| Boundary | Untrusted side | Trusted side |
|----------|----------------|--------------|
| Anonymous text/model input -> schema/writers | Arbitrary strings and cross-field combinations | Bounded normalized plain-text domain |
| Public device/capability -> reservation model | Rotatable/forged key material | Canonical validation, purpose-separated hashes, expiry |
| Storage metadata/bytes -> image classification | Client-controlled MIME and blob | Size plus real magic-byte agreement |
| Parallel requests/actions -> one reservation/post | Retries, interruption, duplicate delivery | Serializable idempotent state machine contract |
| Phase 4 worktree -> shared schema/generated output | Concurrent legitimate edits | Live additive merge, tool-only generation |

## Threat register

| ID | Threat | Severity | Mitigation | Blocking proof |
|----|--------|----------|------------|----------------|
| T-05-01-A | Anonymous callers reserve unlimited storage before any cost control. | high | Device and global limits are modeled before URL generation; check-all-before-consume contract. | N-1/N/N+1 and parallel policy tests; plan 05-02 proves no URL/reservation on denial. |
| T-05-01-B | Declared MIME disguises active, malformed, HEIC, or oversized bytes. | high | 5 MiB metadata bound plus MIME/magic-byte agreement; HEIC typed rejection. | Binary matrix including exact/+1 and spoof/truncation. |
| T-05-01-C | Retry/interruption creates duplicate posts or steals storage ownership. | high | Hashed single-reservation capability, explicit states, one post/storage owner, idempotent finalization contract. | This plan proves capability/state/index primitives; concurrent claim/finalize proof is blocking in 05-02 Tasks 2–3. |
| T-05-01-D | Invalid/orphan blobs accumulate or cleanup races a live owner. | high | Rejection deletion, 24-hour expiry, owner indexes, age+ownership sweep contract. | Cleanup tests in 05-02 at -1/at/+1 and owned/unowned states. |
| T-05-01-E | Pending/hidden data or bearer URLs leak through future public projection. | high | `by_status` backend boundary and minimal projection contract; no public code yet. | Approved-only/forbidden-key tests are created with `listApproved` in 05-02 Task 1; no premature reader/mock exists here. |
| T-05-01-F | Text becomes executable markup. | high | Plain text only, bounded normalization, no HTML APIs. | Control/length tests and source scan. |
| T-05-01-G | Device key is mistaken for identity or bypasses cost defense when rotated. | high | It is fairness-only; global breaker remains; no auth/owner fields. | Schema/source inspection and both-scope policy tests. |
| T-05-01-H | Parallel Phase 4 edits are overwritten by stale schema/generated assumptions. | high | Mandatory immediate re-read, additive patch, no hand-edited generated output. | Live diff review, full suite, `convex dev --once`. |
</threat_model>

<verification>
- Task 1 establishes only the green deploy-safe harness smoke.
- Task 2 creates and immediately verifies the pure model/security/limiter/binary suites.
- Task 3 creates and immediately verifies schema-only integration cases.
- Task 2 runs focused domain/security suites and build.
- Task 3 runs focused suites, the full suite/build, `npx convex dev --once`, and `git diff --check`.
- No three implementation tasks occur without automated verification; every task has a narrow command.
- HEIC browser decoding is not claimed here; only server recognition/rejection is automated.
</verification>

<success_criteria>
- Wave 0 is complete and green for the independently executable WALL-01–WALL-05 foundations; stateful handler/finalization/projection/cleanup proofs are explicitly owned by 05-02.
- Additive tables/indexes and shared constants precisely encode the one-memory, pending-first, approved-only-ready model.
- Text, file size/type, capability, expiry/cooldown constants, retry rounding, limiter policies, and schema ownership indexes are executable; request concurrency and cleanup behavior become executable with their real handlers in 05-02.
- Phase 4 and RSVP schema/generated work remain intact.
- Every high ASVS L1 threat has a blocking proof or the plan remains incomplete.
</success_criteria>

<output>
Create `.planning/phases/05-mural-de-mem-rias-modera-o/05-01-SUMMARY.md` when executed, recording exact limits, validators/indexes, rate policies, concurrency/cleanup contract, Phase 4 merge state, generated-file provenance, and the `npx convex dev --once` result.
</output>
